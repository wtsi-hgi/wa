- [x] MySQL `wa mlwh serve` sample substring search is unusable at production scale. Live-tested against a freshly synced MySQL 8.4 cache (10.35M samples), `/search/sample/:term` and `/search/sample/:term/count` (the ngram FULLTEXT design from `.docs/mcp/spec.md` round-3) return HTTP 502 `Error 188 (FTS query exceeds result cache limit)` for most terms (even selective ones) and 20-32s for the rest. Reimplement sample search as a word-token prefix index per the validated design in `.docs/mcp/prompt.md` "### Clarifications — round 4": new `sample_search_token(token, id_sample_tmp)` table (both dialects) replacing the FTS5 trigram / ngram FULLTEXT; `SearchSamples` pages via index order; `CountSampleSearch` is a bounded exact count; remove the FTS5/ngram objects + maintenance triggers and the phase-6 backend-refusal (`SupportsFullTextSearch` / the serve guard); study search unchanged; update schema-shape parity, bump `CacheSchemaVersion`/`mlwhAPIVersion`, regenerate `.docs/mcp/api-reference.md` + glossary, and reconcile the spec's sample-search sections.
    - Replaced the SQLite FTS5 trigram virtual table + maintenance triggers and the MySQL `sample_mirror_search_ftx` ngram FULLTEXT index with a normal `sample_search_token(token, id_sample_tmp)` table indexed `(token, id_sample_tmp)` in both dialects (tokens = distinct lowercased `[a-z0-9]+` words of name/supplier_name/common_name/donor_id). `CacheSchemaVersion` 5->6, `mlwhAPIVersion` ->1.6.0.
    - `SearchSamples` now pages via index order (`WHERE token LIKE 'prefix%' ESCAPE '!' ORDER BY token, id_sample_tmp LIMIT/OFFSET`), de-dupes ids app-side, fetches samples by id (measured 48-62ms at any cardinality on the live 10.35M cache, vs the old 502/20-32s). `CountSampleSearch` is a bounded exact distinct count (cap 10000 reported as a floor). Word-prefix semantics (start of any word); min length 3. Study search unchanged.
    - Cold-load bulk-builds the token index (index-after) and incremental sync upserts/deletes tokens (preserves the incremental-searchability behaviour from 260626 PR-review commit, via tokens). Schema-shape parity now models the token table as a normal table/index (removed the FTS5/FULLTEXT/FullText/TRIGGER special-casing).
    - Removed the phase-6 startup refusal (`SupportsFullTextSearch` + the `cmd/mlwh.go` serve guard); `wa mlwh serve` runs on any supported backend (token-prefix needs no FULLTEXT, so MariaDB/MySQL<8 work). Regenerated `.docs/mcp/api-reference.md`, updated glossary + spec sample-search sections; updated `run-dev.sh` seed.

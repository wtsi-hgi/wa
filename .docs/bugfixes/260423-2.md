- [ ] `make test-e2e` is failing due to the first test getting an unexpected result.
	- Current branch state does not reproduce this: an implementor subagent reported `make test-e2e`, `make test-go`, and `make test-frontend` all passing.
	- The worktree already contains an unstaged change in `frontend/e2e/results.spec.ts` updating the first dashboard expectation, so this item likely needs the exact failing revision to be actionable.
- [x] Once looking at a results page, there’s no navigation back to the front page. It might be better if clicking a result set either from “recent registratios” or from search results (I’ve not seen search results since search filter doesn’t work, but I believe search results are supposed to replace recent registrations) loaded the results details below instead of going to a new page. So a full SPA.
	- Preserved the originating dashboard/search URL in result links and added a safe in-app return link on the detail page.
	- Added regression coverage in the results-table unit test and a Playwright round-trip test covering both recent registrations and filtered search results.
- [ ] No buttons in the results UI work when clicked, except for links to latest result sets and “Download file”. So can’t add filters or do something with columns or collapse/expand file browser tree or open file preview thumbnails to larger size.
	- Current branch state does not reproduce this: representative search-builder, file-browser, and preview interactions all passed targeted frontend and Playwright checks.
	- Existing hydration and interaction regressions suggest this was likely the earlier shared client-hydration failure where plain links worked but React handlers did not attach.
- [ ] json file previews claim to be “Loading preview...” but never do. (For the `/nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/.docs/results-web/fixtures/files/config.json` example)
	- Current branch state does not reproduce this: the seeded `config.json` preview renders as syntax-highlighted JSON instead of remaining in the loading state.
	- Stronger existing regression coverage already exercises the real preview path rather than helpers alone: `frontend/tests/result-detail-files.test.ts` waits for a fetched JSON preview to replace “Loading preview...”, and `frontend/e2e/results.spec.ts` opens the seeded amplicon result and asserts that `config.json` shows rendered JSON content.
	- This does not appear to share the broken-controls failure mode on the current branch. The JSON preview settles after the async fetch resolves, and the JSON-specific Playwright check passes independently of the broader click-handler regressions.
- [ ] seqmeta_sampleid not showing additional info in the individul result view (“enrichment unavailable” for SANG205). Not sure if this is a failure of the feature or of the test setup when I have a real SAGA_API_TOKEN.
	- Could not reproduce a live product defect from this environment because `SAGA_API_TOKEN` is not set, so the live seqmeta backend path for SANG205 is unavailable here.
	- Current branch evidence points to a test-setup gap rather than a confirmed regression: the stale unavailable-marker logic already has focused frontend coverage, but there is still no SANG205-specific rendered-detail regression using a live token or equivalent deterministic fixture.
- [x] `frontend/next-env.d.ts` keeps changing “by itself”, perhaps when I run `make run`, or perhaps on running tests. Prevent the churn.
	- Stopped tracking the generated Next env file and ignored it at the repo level so `make run` and test workflows no longer dirty the worktree.
	- Added a focused worktree-hygiene regression proving `frontend/next-env.d.ts` is ignored and no longer tracked.
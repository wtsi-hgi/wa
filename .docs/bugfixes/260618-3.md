- [x] After implementing `.docs/mlwh-overhaul/spec.md`, info command seems to work well, but results register gives `results server returned 422: seqmeta_id_run="48522": results: mlwh validation failed: expected "run_id", got "sample_lims_id"` when registering with `--run 48522 --study 7607 --sample 7607STDY14643771 --library 71046409`; fix this and avoid a too-narrow fix if this could be a general problem that could result in other registrations failing on other mlwh entities.
    - Changed MLWH metadata validation to try the resolver for the expected
      entity kind before falling back to broad classification, so identifier
      collisions such as run ID `48522` also matching a sample LIMS ID do not
      reject valid run metadata.
    - Added regression coverage for collision cases across run, library, and
      sample-family validation while preserving unknown/wrong-kind rejection.
    - Verification: reviewer confirmed the new regression fails against the
      old validator with `expected "run_id", got "sample_lims_id"` and passes
      after the fix. `make lint`, `make test-go`, `make test-frontend`, and a
      final `make test` passed; an initial concurrent e2e run hit shared
      setup noise, and the isolated rerun passed.
    - Residual note: sample sub-kind disambiguation still follows the existing
      `ResolveSample` priority because the MLWH query interface has no
      per-sample-field resolvers, but validation is not bypassed.

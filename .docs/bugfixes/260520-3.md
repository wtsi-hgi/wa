- [x] The “Preview height” control in the file browser is inconsistent with the controls to the left of it: the slider and px value text is next to the “preview height” text instead of underneath it. Put it underneath and make sure the box height still stays the same as the other controls and the 3 controls looks similar.
    - Reworked the preview-height control so its label sits above the slider and pixel value, while preserving height parity with the neighboring inline controls.
    - Updated the file-browser Playwright layout regression to assert stacked preview-height contents, no overflow, one-row placement, and matching control heights.
    - Verification: reviewer confirmed focused file-browser Vitest and Playwright checks, `make lint`, and browser screenshot evidence passed. Full `make test` was still blocked by unrelated timing/order flakes outside this file-browser change.
- [x] The font/styling used for file size and file types in the file browser (eg. “1 TSV” or “OUTPUT”) looks too different to other text and looks like it has far too much space between words and between letters within a word. Tighten this up, possibly just use the same font and styling as other text on those lines (x files x subfolders) and make spacing between different items on a line more “deliberate”, eg. use less space but have a small dot separator or similar.
    - Reworked directory and file metadata rendering so type summaries and file kind labels use normal/tight text styling instead of wide-tracked uppercase CSS, with compact dot separators between metadata items.
    - Added Playwright regression coverage to the compact inline file-browser layout for normal letter spacing/text transform and deliberate metadata separators.
    - Verification: focused compact inline Playwright check, frontend Vitest suite, `make lint`, and isolated lane-filter E2E passed. Full `make test` reached E2E and only failed in the known unrelated parallel lane-filter row-count flake; the same spec passed alone.
- [ ] The file browser box has the root folder contained in an unecessary box, wasting some padding/margin and adding an unneeded extra border around everything. Make the display of that box effectively invisble, so the root folder box displays larger without double border.

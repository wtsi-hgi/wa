- [x] When operator is not supplied during `wa results register`, it should default to the current user.
    - Added a CLI regression proving omitted `--operator` defaults to `os/user.Current().Username` while preserving the supplied `--user` requester.
    - Added register command operator defaulting before constructing the registration payload; explicit operators are still preserved.
    - Verification: implementor and reviewer confirmed the new regression fails without the fix; focused cmd tests, `timeout 10m make lint`, and `timeout 20m make test` passed. No `wa mlwh sync` was run.
- [x] The run details popup on a results page should not list “raw run key”
    - Existing seeded `nf-core/rnaseq` reproduced the popup listing `RAW RUN KEY`; baseline screenshot saved at `.tmp/agent/raw-run-key-popup-repro.png`.
    - Removed the raw stored key from the Run details popover while preserving the formatted `Unique` value.
    - Added Vitest and Playwright regressions, including a same-route post-fix screenshot at `.tmp/agent/raw-run-key-popup-postfix.png`.
    - Verification: implementor and reviewer confirmed the new regression fails without the fix; focused frontend checks, `timeout 10m make lint`, and `timeout 20m make test` passed. No `wa mlwh sync` was run.
- [x] `wa results register` given whatever combination of options that results in no output files being discovered should fail without registering anything.
    - Added CLI regressions for an empty output directory and a `--match` pattern that filters out every output file, both asserting no registration request is sent.
    - Added a local post-scan guard so registration fails before payload construction/posting when zero output files are discovered.
    - Verification: implementor and reviewer confirmed the new regressions fail without the guard; focused cmd tests, `timeout 10m make lint`, and `timeout 20m make test` passed. No `wa mlwh sync` was run.
- [x] `.tmp/wa results register --user sb10 --workflow one-off --unique test --match '*.bar' --sample Hek_R1 --sample Hek_R2 --meta 'foo=bar' --meta 'foo=baz' .tmp/outputtest` gives a result set that displays on the results page with metadata “foo bar”, “sample 7607STDY14643771”, when it should be more like “foo bar, baz”, “sample Hek_R1, Hek_R2”. Searching on the main page for Sample Hek_R1 or Hek_R2 also fails, when it should be doing a fast successful lookup of all sample-related fields to find the sample. In the seqmeta details popup, clicking the “Filter” button on Hek_R1 results in a successful search of the “Seqmeta supplier name” field, but it should be a working search of just “Sample” field.
    - Reproduced all three symptoms with a repo-local cache/output harness; evidence saved under `.tmp/agent/bug4-*.png` and `.tmp/agent/bug4-ui-evidence.json`.
    - Registration now preserves user-supplied repeated `--sample` values under user-facing `sample` metadata while retaining canonical seqmeta metadata for validation/enrichment.
    - Result metadata rendering now uses ordered repeated metadata values, so repeated `foo` and `sample` values display as comma-separated values instead of collapsing to the first value.
    - Combined Sample search now expands through cached sample aliases including supplier names, so `?sample=Hek_R1` and `?sample=Hek_R2` find the registered result without sync.
    - Seqmeta details supplier-name Filter links now use the combined Sample search key; reviewer verified the actual Hek_R1 row links to `/?sample=Hek_R1`.
    - Verification: implementor and reviewer confirmed regressions fail before the fix across CLI registration, backend search, metadata display, and seqmeta filter link; focused Go/frontend checks, `timeout 10m make lint`, and a rerun `timeout 20m make test` passed. No `wa mlwh sync` was run.

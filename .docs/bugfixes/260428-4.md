- [x] Results page is sometimes making Chrome go unrepsonsive, with clicks not doing anything. Even on the wtsi/amplicon result set from the `make run` fixture which only has 12 files in it. After some considerable time the page becomes fully usable.
    - Memoized syntax-highlighted code preview rendering so unchanged preview-height and parent rerenders no longer re-run Highlight.js work.
    - Added a regression test covering unchanged rerenders of a loaded code preview.
- [x] File Browser directory rows should state the total number of subfolders and files nested within them, not just counts of immediate children.
    - Added recursive descendant directory and file counts to the compressed tree model and rendered those totals in the directory row summary.
    - Added regression coverage for both the tree aggregates and the user-visible directory row badges.
- [x] File Browser previews are not displaying in a right hand column of the file box they are for, but undeneath as a seperate box. No seperate box/row for image previews! It must be part of the same row box that shows the file name. In the case of single image preview, one preview should row span all the rows and be displayed in the right hand column.
    - Moved single-preview rendering into the selected directory file-list container so the preview belongs to the same file box instead of a separate card below it.
    - Added a regression test asserting the single preview stays inside the selected directory file box.
- [x] Changing the preview height takes 10s of seconds “Rendering…”. It should be ~instant.
    - Kept image thumbnail URLs stable across preview-height changes and memoized non-image grid preview rows so height-only updates do not rebuild expensive preview work.
    - Added regression coverage for both unchanged image thumbnail sources and unchanged non-image grid preview rerenders on preview-height changes.
- [x] In the Search Builder, having selected a filter field, it isn’t obvious that you have to then scroll to the bottom to add a value. The Operator value and “Add” button should be permanently visible at the bottom of the field list, with the vertical scroll only applying to the fields. The Suggestions box should be removed; autofill suggestions should just appear in standard autofill style when typing into the value box.
    - Split the filter popover into a scrollable field list with a pinned footer form so the value input and Add action stay visible after selecting a field.
    - Replaced the custom Suggestions panel with native datalist-backed input suggestions and added regression coverage at both component and dashboard integration levels.

- [x] As a normal user, following `make dev` by an admin user (on another machine), I tried (on another machine):

    ```
    export PATH=/path/to/my/checkout/.tmp:$PATH
    export WA_RESULTS_BACKEND_URL='https://farm22-wrstat01:3671'

    wa results register --user sb10 --workflow test --unique register --sample 7607STDY14643771 /lustre/scratch127/humgen/teams_v2/hgi/sb10/wa_results
    ```

    But I get `Error: WA_MLWH_CACHE_PATH is required to resolve --run/--study/--sample/--library`, and if I export the `WA_MLWH_CACHE_PATH` I get `Error: open mlwh resolver client: mlwh: ping mysql cache: Error 1045 (28000): Access denied for user 'hgi_wf_test_admin'@'farm22-head1.internal.sanger.ac.uk' (using password: NO)`.
    Users must not need to know the password. The MLWH queries used during register and any other CLI interaction need to happen server side, not client side. Use the bugfix skill workflow to fix this and any other similar oversights of this nature. If the correct solution is something like writing a new mlwh server (because mlwh queries are not specific to users of the results server), and if you think that is too much work for a “bug fix”, don’t use the bugfix skill and instead write `.docs/mlwhserver/prompt.md` to describe the requirements, to be used as input for the spec-writer skill workflow.
    - Moved `wa results register` MLWH lookup resolution from the client to the authenticated results server request path by sending raw lookup flags as `lookup_values` and resolving them through the server's configured MLWH resolver before validation and storage.
    - Preserved canonical/source-specific/repeated metadata behavior, conflict detection with literal `--meta`, sample-name fast path, library identifier fast path, and fast invalid lookup rejection without storing a result.
    - Updated register help and docs so normal users are told to configure the results server URL/cert/auth, while MLWH cache configuration stays with the server operator.
    - Added CLI and server regression coverage proving register no longer opens a local MLWH cache, server-side lookup resolution supplies metadata, and invalid lookups are rejected before persistence.
    - Verification: implementor and reviewer confirmed the new regressions fail on old behavior; reviewer reran `timeout 20m make test` and `timeout 10m make lint`, both passed.

- [x] Why are 3 playwright tests skipped? And why isn’t it obvious more tests are skipped when I don’t have any mlwh database details in my .env.development.local? Aren’t there e2e integration tests anywhere that use real mlwh database?
    - Root cause: `frontend/e2e/seqmeta-lane-filter.spec.ts` still had one unconditional `test.skip(...)` plus two body-level `test.skip()` calls from before the Playwright seqmeta harness existed. The spec also never navigated to a result detail page, and the stub server only knew about old fake identifiers (`SANG5993`) instead of the seeded `nf-core/sarek` sample (`WTSI_wEMB10524782`), so the skipped test was hiding both stale navigation and stale fixture wiring.
    - Fixed by converting the skipped spec into a real browser regression against the seeded `nf-core/sarek` result, removing the silent runtime skips, and updating `frontend/e2e/seqmeta-stub.mjs` to serve a sample-detail enrichment payload for `WTSI_wEMB10524782` that includes lane data and the expected `seqmeta_lane` filter link.
    - Updated `DEVELOPING.md` to state explicitly that Playwright e2e is hermetic: `make test-e2e` runs `run-dev.sh --mode test`, rejects `WA_MLWH_DSN`, and uses `frontend/e2e/seqmeta-stub.mjs` rather than `.env.development.local` or a live MLWH database.
    - Current live-MLWH browser coverage remains absent by design; restoring real `WA_MLWH_DSN`-gated integration coverage is the larger follow-up in item 2, not part of this local Playwright visibility fix.
- [x] `wa mlwh sync --env development` fails with “Error: mlwh: query sample sync source: Error 1054 (42S22): Unknown column 'iseq_flowcell.id_study_lims' in 'field list’”. How are there not failing tests that reveal this issue. Make sure there are real integration tests that work with real mlwh database (details taken from .env.development.local, skipped if not available) that raise bugs like this.
    - Root cause: the sync and cold-cache sample resolver SQL still expected `id_study_lims` to exist directly on upstream `iseq_flowcell`, but the current MLWH schema links `iseq_flowcell` to `study` through `id_study_tmp`; the package also lacked a live-MLWH query-compilation test that exercised the exact production SQL against development credentials.
    - Fixed by routing the production query builders through `study.id_study_lims` joined on `iseq_flowcell.id_study_tmp`, and by making the sync tests assert those shared production query builders rather than copied SQL strings.
    - Added schema-faithful regression coverage in `mlwh/sync_real_schema_test.go` plus a live-MLWH integration test in `mlwh/integration_test.go` that loads `WA_MLWH_*` from the process or development dotenv files and skips cleanly with an explicit reason when unavailable.
    - Review passed after `make lint` and `make test`; the new live-MLWH integration test also ran successfully in this workspace.
- [x] Is there an issue with exising cache databases not coping with mlwh schema changes? Develop a tested solution for this if so that is more resiliant to upstream schema changes. Worst case, since it’s just a cache, we drop the cache tables and start over with the new schema.
    - Root cause: `OpenCache` previously trusted any cache whose recorded `schema_version` already matched the embedded version, so a stale or partially drifted cache schema could survive indefinitely and be reused even when the live table or index shape no longer matched the embedded DDL.
    - Fixed by validating the actual cache schema shape during cache open and dropping/recreating the cache tables whenever the embedded schema and on-disk schema diverge, while preserving the existing version-based reset path.
    - Added `TestOpenCacheSQLiteSchemaShapeMismatchResetsSchema` to prove that a same-version stale SQLite cache is rebuilt instead of reused, and tightened the cache bootstrap expectations so the schema-inspection path is exercised in tests.
    - Review passed after `make lint` and `make test`.

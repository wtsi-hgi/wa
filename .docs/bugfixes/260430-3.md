- [x] Visiting `http://farm22-wrstat01.internal.sanger.ac.uk:3671/results/3ff0caa6a605e21fb1fb0b360a5514aa370cedf1c3062b6f11f787b7a08188d8` after `rm -fr frontend/.next && make run` in an incognito window of Chrome 147.0.7727.102 is initially non-responsive for tens of seconds, then becomes interactable but shows a runtime `RangeError: Invalid string length` at `JSON.stringify(snapshot)` in `serializeSeqmetaCacheCookie`.
    - Treated as one bug: the initial unresponsiveness and runtime `RangeError` both came from the seqmeta cache persistence path still stringifying a huge successful enrichment snapshot after write coalescing reduced the number of flushes.
    - Files touched: `frontend/lib/seqmeta-cache-core.ts`, `frontend/lib/seqmeta-cache.ts`, `frontend/tests/seqmeta-cache-coalesce.test.ts`, and `frontend/tests/seqmeta-badge.test.ts`.
    - Fix: keep successful enrichment graphs in memory but exclude them from cookie persistence; persist only bounded `null` miss entries below the browser cookie budget; skip unchanged cookie rewrites; preserve legacy cookie reads that include successful enrichments.
    - Reviewer found and fixed an additional stringify path in repeated successful `set()` equality checks, so repeated sets no longer deep-stringify enrichment graphs either.
    - Regression coverage: cookie serialization no longer calls `toJSON()` on successful enrichments that would throw `RangeError`; generated cache cookies stay below 4096 bytes; legacy successful-enrichment cookies still deserialize; repeated successful `set()` calls do not stringify large graphs.
    - Focused verification: `pnpm exec vitest run tests/seqmeta-cache-coalesce.test.ts tests/seqmeta-badge.test.ts` passed, 23 tests.
    - Changed-file formatting/lint verification passed with `pnpm exec prettier --check` and `pnpm exec eslint`; repo-wide `pnpm lint` remains blocked by unrelated pre-existing Prettier warnings in prior files.
    - Full verification: reviewer ran `make test` from the repo root and it passed: Go tests passed, Vitest passed 32 files / 222 tests, Playwright passed 16 with 2 skipped.
    - Browser verification against the exact URL after `rm -fr frontend/.next && make run`: HTTP 200, DOM content loaded in 5883 ms, network idle in 7376 ms, result metadata visible in 7409 ms, seqmeta badge visible in 7414 ms, seqmeta dialog opened in 65 ms and closed in 19 ms, cookie value length stayed 6, total document cookie length stayed 23, and there were zero console errors, page errors, `RangeError`s, or `Invalid string length` messages.

- [x] The test action failed in github, fix it:

    [https://github.com/wtsi-hgi/wa/actions/runs/26945904818/job/79498678739?pr=11](https://github.com/wtsi-hgi/wa/actions/runs/26945904818/job/79498678739?pr=11)

    `--- FAIL: TestRunDevScriptF1DevCertificates (20.03s)`

    `run_dev_test.go:142: timed out waiting for frontend snapshot /tmp/TestRunDevScriptF1DevCertificates1695351049/001/frontend-env.json`
    - Added a delayed-frontend regression test in `cmd/run_dev_test.go` that reproduces the old fixed 20 second frontend snapshot timeout.
    - Made the run-dev test snapshot wait derive a bounded budget from the configured frontend and seqmeta health waits, with extra startup grace for slower CI runners.
    - Made snapshot waits fail early with captured stdout/stderr if `run-dev.sh` exits before writing the frontend snapshot.
    - Verification passed in subagent review: focused red proof, focused green tests for the new regression and original failing test, `make lint`, and `make test`. No `wa mlwh sync` was run.

- [x] As an admin user (on another machine) I have `.env.development.local` containing:
    ```
    WA_DEV_RESULTS_PORT=3672
    WA_DEV_RESULTS_HOST=0.0.0.0
    ```
    And do:
    ```
    $ make dev
    Building Go binary at /nfs/hgi/wa/repo/.tmp/wa
    Starting results server on https://127.0.0.1:3672 (mode=dev)
    Skipping fixture seed (mode=dev, --fixtures not requested)
    Starting seqmeta server on http://127.0.0.1:3673
    Waiting for seqmeta studies readiness at http://127.0.0.1:3673/studies (MLWH cache: hgi_wf_test_admin@tcp(hgi-workflow-automation-test-db.internal.sanger.ac.uk:3324)/workflow_automation_mlwh_sb10;?parseTime=true; a cold cache can take a while on first run)
    Starting frontend dev server on https://127.0.0.1:3671
    Development environment is ready.
    Results: https://127.0.0.1:3672
    Seqmeta: http://127.0.0.1:3673
    Frontend: https://127.0.0.1:3671
    Logs: /nfs/hgi/wa/repo/logs
    ```
    Firstly, it would be nice if there was some kind of indication in that output that the frontend is listening on 0.0.0.0 and not just 127.0.0.1, to distinguish when WA_DEV_RESULTS_HOST has not been set.
    - Added dev launcher output for the configured results bind address while preserving loopback client URLs, so `WA_DEV_RESULTS_HOST=0.0.0.0` now prints `Results bind: 0.0.0.0:<port> (listening beyond loopback)`.
    - Kept frontend output accurate and did not claim the frontend binds to `0.0.0.0`; `WA_DEV_RESULTS_HOST` is a results API bind setting.
    - Added regression coverage for wildcard and default loopback-only dev output in `cmd/run_dev_test.go`.
    - Verification: focused `go test -tags netgo --count 1 ./cmd -run RunDev`, `make lint`, and `git diff --check` passed. Reviewer saw one known unrelated Playwright flake in `make test`; the exact spec passed on rerun, and the implementor's earlier full `make test` run passed.
- [ ] As a beta testing user, I then tried (on another machine):
    ```
    $ export PATH=/path/to/.tmp:$PATH
    $ export WA_RESULTS_BACKEND_URL='https://farm22-wrstat01:3671'
    $ export WA_RESULTS_SERVER_CERT=/path/to/.tmp/wa-dev-cert.pem
    $ wa results register --user sb10 --workflow test --unique register --sample 7607STDY14643771 /lustre/scratch127/humgen/teams_v2/hgi/sb10/wa_results
    Password:
    Error: Post "https://farm22-wrstat01:3671/rest/v1/jwt": tls: failed to verify certificate: x509: certificate is valid for localhost, not farm22-wrstat01
    ```

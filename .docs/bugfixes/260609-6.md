- [x] I'm not sure whatever you did was correct. Authentication SHOULD be as the local OS username. `--user` could be someone else. In any case, when I tried it --user matched the username of the user running the CLI and it still didn't work.
    - Supersedes the identity assumption from `.docs/bugfixes/260609-5.md`: `--user` is registration metadata/requester, not the password/JWT login identity.
    - Added coverage proving CLI register password/JWT auth uses the local OS username even when `--user` is a different requester.
    - Added the missing same-user regression: web-style JSON login to `/rest/v1/jwt` succeeds for the local OS username/password, but a stale readable `.wa-results-server.token` previously made CLI register try that token as the password and return `authentication failed` without prompting for the LDAP password.
    - Fixed register auth to preserve the readable owner-token fast path, but when that token login is rejected with `authentication failed`, retry register through a client whose server-token path is unavailable so normal local-user password/JWT login can prompt and proceed.
    - Preserved TLS verification, explicit/env cert behavior, token permission checks, server-side MLWH lookup behavior, and owner-token behavior for successful owner logins and owner-only commands.
    - Verification: focused red/green `timeout 10m go test -tags netgo --count 1 ./cmd -run 'TestResultsAuthClient'`, focused `timeout 10m go test -tags netgo --count 1 ./cmd -run 'TestResultsAuthClient|TestResultsRegisterCommand'`, `timeout 10m golangci-lint run --fix`, full `timeout 20m make test`, full `timeout 10m make lint`, and `git diff --check`.

- [x] Switching between “combined files” and “Result sets” views shows the titles in the boxes beneath are at different vertical distances from the buttons, likely due to the “columns” drop down button. There is also uneven spacing above and below the title text which doesn’t look good. Try to standardise the title and title icon display (distance to lines above, to the left of and beneath the icon&title text) of all boxes (“search”, “file browser”, “search results”), making them as close as possible to how “file browser” currently displays, ensuring that any button on the right (“add filter”, “columns”) mirrors the spacing, doesn’t affect the spacing and remains in horizontal alignment with the title text.
    - Reproduced at 1440x900 with a browser-rendered Playwright pass switching from Combined files to Result sets. Evidence saved to `.tmp/agent/box-title-spacing-repro.json`, `.tmp/agent/box-title-spacing-repro-combined.png`, and `.tmp/agent/box-title-spacing-repro-result-sets.png`.
    - Added a red regression in `frontend/e2e/search-combined-file-browser-repro.spec.ts`: the Result sets title section is currently 47px high versus File Browser's 36px, and the Result sets section has 11px of extra height around its title row while File Browser has 0px. The Add filter and Columns controls are horizontally present and vertically centered within 1px/0px, so the reproduced failure is the surrounding title-section spacing rather than control center alignment.
    - Added shared compact title-section and title-action classes, applied them to Search and ResultsTable, and kept File Browser on the same compact geometry.
    - Verified post-fix browser evidence: Result sets and File Browser title rows start 0px apart below the view toggle, title-section height differs by only the 1px divider, and Add filter/Columns center on their title rows.
    - Resolved the required `make test` gate failure by making logout expire the auth cookie before best-effort backend logout notification, preserving immediate protected-access revocation coverage.

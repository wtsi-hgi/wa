- [x] Use the bugfix skill workflow to fix the flakey playwright test. Don't just skip or remove it. Figure out why it's flakey and resolve it properly.
    - Root cause: `seqmeta-direct-filter-repro.spec.ts` can create a temporary `seqmeta/direct-filter-repro` result while `seqmeta-lane-filter.spec.ts` is running in parallel against the shared hermetic results DB. The lane-filter test asserted a global dashboard row count of four, so it failed whenever the temporary row was present.
    - Fixed the lane-filter Playwright test to target the seeded `nf-core/sarek` row it actually needs instead of counting all dashboard rows, while keeping the end-to-end navigation, sample metadata dialog, lane row, and exact `/?seqmeta_lane=48522_1#1` filter-link assertions.
    - Verification: the two-spec concurrent repro with `--workers=2`, `make lint`, and a full rerun of `make test` passed. An earlier full run hit an unrelated Vitest timing assertion before passing on rerun.

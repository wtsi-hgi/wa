- [x] Visiting a result set page from `make run` makes the browser non-responsive. The page can get stuck on “Compiling…” or apparently not doing anything, but offering no interaction until Chrome pops up a warning offering to exit the page.
    - Extracted the async result-detail body into `frontend/app/(results)/results/[id]/page-content.tsx`, kept the page entrypoint focused on resolving request props, and added `frontend/app/(results)/results/[id]/loading.tsx` so App Router serves the loading shell without blocking the detail route.
    - Added a route-level regression in `frontend/tests/result-detail-hydration.test.ts` to assert the loading shell renders before result/file fetches start, and updated `frontend/tests/seqmeta-badge.test.ts` to target the extracted content component for full detail markup assertions.
    - Verified manually against `make run`: the loading shell appeared in about 296 ms, populated detail content appeared in about 486 ms, and a detail-page UI toggle responded in about 45 ms without freezing the browser.
    - Fixed the follow-on validation failures needed to prove the route change end to end: the UTC day-bucket flake in `results/store_test.go`, the delayed-health-server port race in `cmd/run_dev_test.go`, and Playwright result-detail waits/selectors that were too eager for the streamed page.
    - `make test` now passes end to end.

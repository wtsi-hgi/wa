Status: Complete

Bug:
Use the bugfix skill workflow to change `wa mlwh export` to get rid of paging options and to always just do --all (and so also get rid of --all as an option), and to remove study_accession_number from the default columns for irods. The final line of output like "complete set emitted: rows=732" should not be output, so the output can be cleanly redirected to a file and create a valid file of the desired format.

Implementation:

- [x] Remove user-facing paging/all options from `wa mlwh export` and make CLI exports complete by default.
    - Removed `--all`, `--limit`, `--cursor` and `--offset` from export help/flag parsing.
    - CLI export now always sends complete-export options and leaves chunk sizing to the MLWH layer.
    - Local complete exports page internally for non-iRODS relationships; canonical iRODS keeps keyset streaming, and sorted/date-window iRODS uses internal offset pages.
    - Remote complete exports page internally for all export relationships instead of asking the server for one materialized all response.
- [x] Remove `study_accession_number` from default iRODS export columns while keeping it selectable.
- [x] Remove success status lines from export output so stdout is only export data.
- [x] Update help text and tests to describe complete-export behavior.
- [x] Keep the real-MySQL status-breakdown perf guard web-responsive without weakening the test.
    - Reworked the per-platform status-breakdown aggregate to intersect platform product samples with study/sample/platform iRODS rows using the study/sample iRODS index.
    - Updated the MySQL EXPLAIN guard to assert the new indexed access path.
- [x] Run focused export tests and `make lint-go`.
    - `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -v -run 'TestMLWHExport'` passed.
    - `CGO_ENABLED=1 go test -tags netgo --count 1 ./mlwh -v -run 'TestExport|TestIRODS|TestRemote|TestOpenCacheMySQLMigratesV1Cache|TestEndpointReferenceMatchesCommittedDocumentG1'` passed.
    - `CGO_ENABLED=1 go test -tags netgo --count 1 -timeout 10m ./mlwh -v -run 'TestStatusBreakdown|TestRealMySQLPerPlatformBreakdownIsIndexServed|TestRealMySQLI2StudyOverviewAndStatusBreakdown7699UseIndexesAndFinishUnderSecond'` passed.
    - `make lint-go` passed with 0 issues.
- [x] Run full Go suites.
    - `make test-go` passed.
    - `WA_LIVE_MLWH_TESTS=1 make test-go` passed.

Review:

- [x] Reviewer subagent pass completed.
- [x] Review fixes applied.

Commit:

- [x] Commit created.

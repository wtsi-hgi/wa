- [x] `make dev` fails when the MLWH cache needs a (re)sync, and the user's only nice way to start the MLWH server is `make dev`. After pulling the merged MCP work, opening the existing `_sb10` MySQL cache (schema v3) triggers the automatic v3→v6 migration, which drops the mirror tables and resets sync watermarks, leaving an empty/never-synced cache. `run-dev.sh` then starts `wa mlwh serve` and blocks on `/studies` readiness, which returns HTTP 503 (ErrCacheNeverSynced) and times out after ~5 min (`SEQMETA_HEALTH_MAX_ATTEMPTS=1200` x 0.25s), so `make dev` exits "Timed out waiting for MLWH server at .../studies" with Error 1. run-dev.sh never runs a sync itself, and the sync order is sample-first (10.35M rows) then study, so /studies cannot become healthy within the timeout even if a sync ran concurrently. Make `make dev` handle a cold / never-synced / migration-emptied cache gracefully by running the sync through the dev flow -- but ONLY when the cache is empty/never-synced (new database, dropped tables, or schema-changed scenario), never on an already-synced warm cache.
  - `run-dev.sh`: added `SEQMETA_FRESHNESS_URL` (default `/freshness`, override `WA_RUN_DEV_SEQMETA_FRESHNESS_URL`) and two helpers. `mlwh_freshness_is_cold` GETs `/freshness` (HTTP 200 even on a never-synced cache by design) and parses it with the script's existing `node` dependency: cold if any table has `ever_synced!=true`, warm if all true, and unreachable/unparseable is treated as warm so a transient hiccup never forces an unwanted sync. `ensure_mlwh_synced_after_serve` first waits for the server to be listening (the existing server-up gate, capped by `SEQMETA_HEALTH_MAX_ATTEMPTS`), then: warm → straight to the existing `/studies` readiness gate, no sync (unchanged behaviour); cold + `WA_MLWH_DSN` set → run `wa mlwh sync` in the foreground with no timeout (so the long cold load completes before the `/studies` gate, defeating the sample-first ordering problem), then the `/studies` gate; cold + no `WA_MLWH_DSN` → print a clear actionable message and fail fast instead of the 5-minute hang. Both auto-managed branches (`WA_RUN_DEV_SEQMETA_CMD` and `WA_MLWH_DSN || MLWH_CACHE_PATH`) were de-duplicated to call this one helper.
  - The sync runs ONLY when never-synced (the new-database / dropped-tables / just-migrated scenario), never on a warm cache — honouring the explicit constraint and keeping `TestRunDevAutoManagedMLWHBackendDoesNotSyncBeforeMLWHServe` / `...CanServeConfiguredCacheWithoutDSN` green.
  - No Go application code changed: `/freshness` (always 200, reports `ever_synced`) and `/studies` (503 when never-synced) already provide the needed signals. The v3→v6 migration that empties the cache (`mlwh/cache.go`) is intended behaviour and is left as-is.
  - Tests (`cmd/run_dev_test.go`, run via `go test ./cmd`): new `TestRunDevAutoManagedMLWHBackendSyncsColdCacheBeforeStudiesReadiness` (cold stub: `/freshness` ever_synced=false and `/studies` 503 until `mlwh sync` writes a marker the serve stub reads, then 200; asserts serve→sync ordering and that run-dev completes rather than timing out) and `TestRunDevAutoManagedMLWHBackendFailsFastOnColdCacheWithoutDSN` (cold + no DSN → non-zero exit, stderr mentions "never been synced" and "WA_MLWH_DSN", no sync, no snapshot). Extended the shared warm stub to answer `/freshness` with ever_synced=true. Confirmed the cold test fails for the right reason (5-min `/studies` timeout) against the unfixed script. Gates: `go build ./...`, `go test -tags netgo ./cmd ./mlwh`, `make lint-go` all pass.

- [x] The height resizer on previews looks better now, but is still incorrect. It is a visually seperate small square, instead of looking like it is part of the preview box with its rounded corner. Find a way to make it large enough so it intersects with the the straight edges of the preview box just where they start to curve at the corner, and either clip it to the rounded corner so it follows the exact same curve, or make it look like the bottom right corner of the preview box is right-angled with stripes on it.
    - Reworked the preview resize affordance into a 32px striped corner patch clipped by the preview's bottom-right radius, removing the small isolated 12px square treatment.
    - Strengthened Playwright geometry coverage for image and CSV previews so the old small-square grip fails and prior visible-surface alignment/no-native-resize behavior remains covered.
    - Verified with focused Playwright, frontend lint, frontend Vitest, `make lint`, and `make test`.

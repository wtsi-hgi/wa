- [x] The “Subfolder previews” UI is appearing on folders that don’t have any subdirs directly containing files, meaning when you tick the option, it just removes the display of the folder contents instead of showing any previews of anything.
    - Tightened subfolder-preview eligibility so nested-only descendants no longer surface the control while valid compressed immediate-child chains still do.
    - Added focused regression tests for the invalid nested-only case and the valid compressed-chain case in the file browser.
- [x] The “1 preview per row” selector is appearing on folders containing only 1 file, which is pointless.
    - Hid the preview-mode selector when a folder has only one previewable file while leaving the rest of the folder controls intact.
    - Updated unit and Playwright coverage so single-file folders assert the toggle is absent and multi-file galleries still exercise grid mode.
- [x] The “Page 1 of 1” widget is appearing on folders with only 1 page, which is pointless.
    - Gated the folder preview page summary and bottom paginator on there being more than one preview page.
    - Added a regression test covering the one-page expanded-folder case so the pointless widget does not return.
    - Followed up during live dev-fixtures verification by hiding the same single-page badge in the subfolder preview control path and adding regression coverage for that case too.
- [x] The pagination widget on subfolder previews folders seems to be paginating at only 4 subfolders per page. Increase to 20.
    - Raised the subfolder preview page size constant from 4 to 20 so the gallery shows more immediate subfolders per page.
    - Expanded the pagination regression test to assert 20 subfolder rows on page 1, the 21st on page 2, and preserved preview-height state.
- [x] The “Subfolder previews” display of image files is broken, not actually showing the images, only being wide enough for the download button. Be sure to screenshot the `make dev-fixtures` page for the wtsi/galleries-demo result set to prove the images are fully visible (without unecessarily wasted white space between them) after your fix.
    - Removed the width-collapsing subfolder preview card overrides for image previews so thumbnails render at usable widths instead of shrinking to the download button.
    - Added unit and Playwright regression coverage for subfolder image card sizing before capturing the proof screenshot from the dev fixtures flow.

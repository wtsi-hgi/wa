- [x] Now the skip warning is duplicated since the old warning is still there. Get rid of the useless warning and keep the new ones.
    - Updated CLI scan warning rendering so escaped directory symlinks print only the detailed symlink-to-target warnings, while generic skip warnings still keep the count summary.
    - Strengthened `results register` and `results rescan` regressions to reject the obsolete aggregate escaped-symlink warning while preserving link and resolved-target output.
    - Verified focused Go tests, `golangci-lint run --fix ./...`, `make lint`, and `make test` passed; no `wa mlwh sync` was run.

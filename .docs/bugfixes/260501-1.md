- [x] `make test-e2e` is failing. Probably the tests are in some way wrong, because in reality the things the failing tests are testing seem to work fine. Fix the tests, don’t just bypass or remove them.
	- Updated `frontend/e2e/results.spec.ts` to assert the rnaseq fixture's actual `seqmeta_studyid` metadata value (`6568`) instead of stale sample metadata.
	- Kept the existing e2e coverage intact: metadata display and seqmeta dialog scrollability are still asserted without bypassing tests.
	- Reviewer confirmed `make test` passes.
- [x] In the file browser, the preview height selector should be minimised in size and placed next to the “1 preview per row” toggle that appears on folders containing files.
	- Moved the preview height control into the expanded folder controls beside the `1 preview per row` toggle.
	- Added unit and e2e regressions covering compact placement and preserving folder-control behavior.
	- Reviewer confirmed `make test` passes.
- [x] The preview height choice should affect all file type previews. Currently a 20 row csv is previewing all rows, making the "preview" very tall. It should instead show as many rows as will fit in the preview height.
	- Modified `CsvPreview` to accept `maxHeight` prop and calculate visible rows based on estimated row height (~52px per row + ~100px overhead).
	- CSV previews now respect the preview height slider constraint, showing only rows that fit within the chosen height (minimum 3 rows).
	- Added 2 regression tests: one verifying row count is constrained (4 rows at 220px vs. prior 51 rows), another confirming larger heights show more rows.
	- Files touched: `frontend/components/file-preview.tsx`, `frontend/tests/file-preview.test.ts`.
	- All 237 frontend tests pass; lint clean.
- [x] In the result metadata section for nf-core/rnaseq from the `make run` fixture, the key is “seqmeta_studyid”, but the value isn’t an id. It displays as “HCA Embryo Foetal WSSS Dev RNA Sanger”, which is the study_name. Either this is a bug in the fixture (supply 6568 as the value of seqmeta_studyid), or a bug on the frontend (the result metadata section should only display keys and values that were directly defined for the result set).
	- Fixed the frontend seqmeta badge label to display the raw result metadata value rather than substituting enriched study names.
	- Updated seqmeta badge/enrichment regressions to assert raw direct metadata values while preserving enriched detail content.
	- Reviewer confirmed `make test` passes.
- [x] When you click on a seqmeta_* value it opens a seqmeta details popup. This has redundant and contradictory info. The title is the value clicked on with the key underneath, but then the first bit of direct metadata listed is the same key but a different (correct) value in the case of nf-core/rnaseq (may be due to previous bug). If the first value and the title are fixed to match each other, the first value would be a duplicate and not need to be listed. The “resolved seqmeta type” doesn’t need a row of its own, just put it in brackets after the key under the title. The row for the resolved type doesn’t need to be there since the value for that should match the title. The box containing summary and resolution doesn’t need to be there since it just duplicates values we can see elsewhere.
	- Simplified the seqmeta dialog so the title is the raw clicked value and the subtitle includes the metadata key plus resolved type in brackets.
	- Removed duplicate selected-value/type rows and the redundant summary/resolution aside while preserving loading, partial, and error messaging inline.
	- Added unit regressions and updated the scrollability e2e to use a stable dialog-body selector at a viewport that genuinely overflows.
	- Reviewer confirmed `make test` passes.
- [ ] Related data in sequmeta details may need an overhaul at the api query layer (see issues in following 2 bugs). A key insight to understand is that relations of types are heirachical. A study contains one or more libraries, which contains one or more samples, which were sequenced on one or more lanes. Queries and dispalyed results should clearly reflect the heirarchy.
- [ ] The “related data” in seqmeta details is displayed inconsistently. In the nf-core/rnaseq from the `make run` fixture, there are 3 “library type” rows, presumably because the study has 3 libraries in it. But it also has a single row with a handful of comma separated sample names in it. It should instead be changed to a grouping by key, ideally in a heirarchy. So for a study the related data would be “libraries” and under that it would list the 3 names (without duplicating the “library type” and “seqmeta_library” strings), each with the individual copy and filter buttons as now. Nested under each library it could list the samples belonging to that library, collapsed by default, expandable (only spending time getting sample details on-expand-click), with each sample name on its own row with own copy and filter button. Likewise, in the wtsi/amplicon library example, the related data should be the list of samples each on their own row. It should also include a study section showing the study the library belongs to.
- [ ] The “related data” in seqmeta details for the nf-core/sarek sample example from the `make run` fixture seems broken. Direct metadata only shows sampleid when there should be lots of other direct metadata, such as name and other different kinds of identifers. Related data has 3 rows related to the same study (name, identifier, accession), and 3 rows for different samples. It should have just one row showing the library the sample came from, then 1 row showing the the study the library came from. It shouldn’t have any linked samples, since a sample is not directly related to other samples. It should have a section for lanes that the sample was sequened on.

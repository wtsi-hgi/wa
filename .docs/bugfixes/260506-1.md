- [x] `wa results register` has dedicated `--seqmeta-runid`, `--seqmeta-studyid`, `--seqmeta-sampleid`, and `--seqmeta-librarytype` flags as convenience shorthands for the equivalent `--meta seqmeta_...=value` entries. Instead make these `--run`, `--study`, `--sample` and `--library` with saga lookup to figure out what what the value actually is (eg. if it’s a sample name vs id or a study accession vs id) and then storing against the appropriate seqmeta key, or converting to a standard one (eg. study accession to study id to store like `--seqmeta-studyid`). Document the flexibility in the main body of the register help text. Error if Saga not available to do the lookup.
    - Updated `wa results register` to accept Saga-backed `--run`, `--study`, `--sample`, and `--library` shorthands and normalize them into canonical `seqmeta_*` metadata.
    - Added regressions covering successful canonicalization and the required early error when Saga credentials are unavailable.
- [x] The `scripts/wa-env.sh dev -- wa results ...` solution to loading the correct env is clunky. Can’t what the wa-env.sh script does be integrated in to the wa binary, using https://github.com/joho/godotenv, taking the desired env from an env var, overridable with a global `--env` flag? It should be possible to retain all existing functionality and env choosing while getting rid of the wa-env.sh script completely. Rename the used env files and possibly env var naming per the documented conventions (https://github.com/bkeepers/dotenv#what-other-env-files-can-i-use). Implement with no backwards compatibility or mentions of the old ways of doing things.
    - Added startup dotenv loading in `main.go` with a global `--env` selector, `WA_ENV` fallback, and `github.com/joho/godotenv` load order matching `.env.<environment>` conventions.
    - Renamed the tracked scenario files to `.env.development` and `.env.production`, updated `Makefile`/`run-dev.sh` to preserve the scenario guards, and removed `scripts/wa-env.sh` entirely.
    - Updated tests and user-facing docs to use the new env names and `wa --env ...` workflow.
- [ ] File preview truncation implemented in recent commits and documented in the previous bugfixes md file is happening after only 3 lines. It should only happen when the files are too large to send to the browser, and we should send the limit size amount of lines to the browser, which should be 1000s of lines. When enlarged, tables can be paginated at 1000 lines per page to avoid slow renders.

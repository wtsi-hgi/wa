- [x] There should be code for the “latest result sets” view that shows project metadata for the project column, defaulting to pipeline name. Refactor so that code can be used on the result page so the main title is the project, not the pipeline name, but defaults to pipeline name if there is no project metadata.
    - Added shared result display naming that trims `metadata.project` and falls back to `pipeline_name`, then reused it for both the latest result sets Project column and the result detail title.
    - Preserved the existing adjacent Unique display on the result detail title while replacing only the title name portion with the shared project display name.
    - Added result detail regression coverage for project-title-plus-Unique and blank project fallback; updated stale Playwright title expectations to the new behavior.
    - Reviewer confirmed the regression fails under a temporary reverse patch and passes after restore; `make lint` and `make test` passed. No `wa mlwh sync` was run.

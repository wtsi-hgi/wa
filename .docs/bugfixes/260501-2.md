- [x] Previews in the file browser can have vertical and horizontal scroll bars, but these can’t be interacted with. Instead of showing a scroll bars when the content doesn’t fit in available preview hieght, just show an indication of truncation at the bottom/right of the preview.
    - Changed constrained markdown, CSV, and code previews to clip overflow and show gradient truncation indicators instead of scrollbars.
    - Preserved scrollable enlarged/full preview behavior.
    - Added unit and e2e regressions covering clipped previews and enlarged scrolling.
    - Reviewer confirmed `make test` passes.
- [x] In seqmeta details pop up, under direct metadata, there can be a redunant row with the same value as in the title. Remove it.
    - Modified `appendDetailField` in seqmeta-badge.tsx to accept optional `rawValue` parameter and filter out direct metadata fields whose values match the dialog title (case-insensitive).
    - Updated all 11 `appendDetailField` calls in `buildDetailFields` to pass `rawValue`.
    - Added regression test "omits redundant direct metadata row when value duplicates the dialog title" that fails before the fix and passes after.
    - Updated existing test "shows all direct metadata fields for a sample, not just sampleid" to reflect correct behavior (redundant Sanger sample ID field now omitted when it matches the title).
    - Preserved dialog title visibility, other direct metadata fields, and all copy/filter affordances.
    - All 255 frontend tests pass. Lint/format pass.
    - Reviewer must confirm `make test` passes.
- [x] In seqmeta details pop up, lists of related things duplicate the type in each row. Eg. for a sample, there is a “Lanes” section where each row says “Lane”. Remove the heading from the rows in named sections.
    - Removed repeated row-level type labels from named related-data sections while keeping section headings visible.
    - Preserved row values plus copy/filter controls for lanes, samples, libraries, and studies.
    - Added regressions for each named section.
    - Reviewer confirmed `make test` passes.
- [x] In seqmeta details pop up, Lane rows should get a “Filter” button like other types. This implies making the search builder on the landing page and the seqmeta/saga pkgs fully support lanes as first class citizens alongside samples, libraries and studies.
    - Added lane filter links using the `seqmeta_lane` representation `{id_run}_{lane}#{tag_index}`.
    - Added frontend and backend regressions proving lane filters work through the existing `seqmeta_` filter path and combine with other filters.
    - Preserved existing sample/library/study filter behavior.
    - Reviewer confirmed `make test` passes.
- [x] In seqmeta details pop up for a seqmeta_library, samples are listed but the study the library belongs to is not given. Opening the popup also results in 19 node issues that look like: " Encountered two children with the same key, `Pilot_study_of_dissociation_methods_for_human_gut_tissues7993354`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version."
    - Fixed duplicate React key warnings by deduplicating samples in `buildHierarchicalGroups` (library metadata combines `graph.sample` + `graph.samples`, which can overlap).
    - Changed sample React keys from `sample.sanger_id || sample.id_sample_lims` to `${sample.sanger_id}|${sample.id_sample_lims}` for guaranteed uniqueness in both nested library samples and top-level samples sections.
    - Study section already renders when `enrichment.graph.study` is present for library metadata; verified this works correctly with new regression test.
    - Added regression test "does not emit duplicate React key warnings when rendering library metadata with overlapping sample data" that fails before fix (captures console.error with duplicate key warning) and passes after.
    - Added regression test "displays study section for library metadata when study data is present" confirming study name, filter link, and samples all render correctly.
    - All 266 frontend tests pass. Lint/format pass. Backend seqmeta tests pass.
    - Reviewer must confirm `make test` passes.
- [x] In seqmeta details popup, the "related data" header doesn't show up for "seqmeta_library" when there's no direct metadata, and it also doesn't show for seqmeta_sampleid, despite there being direct metadata followed by related data under library, study and lanes sections. Those sections are sub-sections of "related data".
    - Added "Related Data" parent header wrapping all hierarchical groups (Study, Library, Samples, Lanes) when they exist.
    - Changed hierarchical group headings from h4 to h5 to establish visual hierarchy: "Direct Metadata" and "Related Data" are top-level (h4), subsections like "Study" and "Lanes" are nested (h5).
    - Added regression test "shows Related Data header before Study/Samples sections for seqmeta_library with no direct metadata" that fails before fix (no "Related Data" header) and passes after.
    - Added regression test "shows Related Data header before Library/Study/Lanes sections for seqmeta_sampleid with direct metadata" that fails before fix (no "Related Data" header) and passes after.
    - Preserved all existing dialog behavior: direct metadata display, subsection headings, copy/filter controls, and expandable library sections.
    - All 268 frontend tests pass. Lint/format pass.
    - Reviewer must confirm `make test` passes.
- [x] In seqmeta details popup, the related data of the seqmeta_studyid example from the nf-core/rnaseq resultset in the `make run` fixture doesn't make sense and isn't showing hierarchically. The related data section should have a libraries sub-section for each library under the study. Clicking a library should expand it, doing a just-in-time query to list the samples (just one row per sample) in that library. Currently various details of a single sample are listed out in seperate rows, which is wrong. There is also a "linked_samples" thing which uneeded; all code related to generating the "linked_samples" entity should likely be removed, since we should just be doing hierarchical queries from study to libraries to samples.

    ### Implementation (retry completed):

    #### Backend Changes (Complete):
    1.  **Modified `/study/{id}/samples` endpoint** to accept optional `library_type` query parameter for filtering samples by library type
    2.  **Updated `buildStudyDetail` function** in `seqmeta/enrich.go` to not preload samples in `LibraryDetails` - samples arrays now empty, loaded JIT on expansion
    3.  **Added regression test** in `seqmeta/server_test.go` proving library_type filtering works
    4.  **Updated hierarchy tests** in `seqmeta/enrich_hierarchy_test.go` to expect empty samples arrays in library details
    5.  All seqmeta Go tests pass, linter and cleanorder clean

    #### Frontend Changes (Complete):
    1.  **Added `fetchLibrarySamples` function** in `lib/seqmeta-enrichment.ts` to call backend JIT endpoint with study ID and library type
    2.  **Updated `HierarchicalLibrary` type** to include `idStudyLims` field needed for JIT queries
    3.  **Modified `buildHierarchicalGroups`** to create library items with empty samples arrays
    4.  **Implemented JIT loading logic** in `seqmeta-badge.tsx`:
        - Added state for `loadedLibrarySamples` and `loadingLibraries`
        - Added useEffect hook to fetch samples when library is expanded
        - Updated UI to show loading spinner during fetch
        - Button shows "Samples" initially, "Loading..." during fetch, then "N samples" after load
        - Samples render from loaded state, not from initial enrichment
    5.  **Updated all 4 failing tests** in `seqmeta-badge.test.ts` to use JIT mock pattern:
        - "does not duplicate 'Library type' label in Libraries section with nested samples"
        - "does not show individual sample fields in flat detail fields for study metadata with study_detail"
        - "does not show linked_samples field for study metadata with study_detail"
        - "renders each sample as a single row under expanded library, not as multiple field rows"
        - All tests now empty `library_details[].samples` arrays and use `fetchLibrarySamplesMock.mockResolvedValue(...)` to return expected samples

    #### API Contract:
    - **Endpoint**: `GET /study/{id_study_lims}/samples?library_type={library_type}`
    - **Response**: Array of `EnrichmentSample` (saga.MLWHSample)
    - **Example**: `/study/6568/samples?library_type=RNA%20PolyA` returns samples for that library in that study

    #### Regression Tests:
    - Backend: `TestServerStudySamplesEndpoint` - "F2: study samples endpoint filters by library_type query parameter"
    - Backend: Updated hierarchy tests to expect empty samples in library details
    - Frontend: "nests samples under each library, collapsed by default and expandable" - proves JIT loading triggers on expand and fetches correct samples
    - Frontend: All 4 updated tests now verify JIT pattern works correctly

    #### Test Results:
    - All Go tests pass: `ok github.com/wtsi-hgi/wa/seqmeta 0.112s`
    - All 271 frontend unit tests pass (including 39 seqmeta-badge tests)
    - All 21 e2e tests pass (3 skipped as expected)
    - Full `make test` completes successfully

    #### Manual Test:

    Run `make run`, open study "Pilot_study_of_dissociation_methods_for_human_gut_tissues" (seqmeta_studyid=7993), verify:
    1.  Libraries section shows library types without preloaded samples
    2.  Click expand button triggers loading spinner
    3.  Samples appear after load as single rows (one row per sample)
    4.  No linked_samples generation or display
    - Reviewer must confirm `make test` passes and manual verification.

- [x] Previews in the file browser can have vertical and horizontal scroll bars, but these can’t be interacted with. Instead of showing a scroll bars when the content doesn’t fit in available preview hieght, just show an indication of truncation at the bottom/right of the preview.
	- Changed constrained markdown, CSV, and code previews to clip overflow and show gradient truncation indicators instead of scrollbars.
	- Preserved scrollable enlarged/full preview behavior.
	- Added unit and e2e regressions covering clipped previews and enlarged scrolling.
	- Reviewer confirmed `make test` passes.
- [ ] In seqmeta details pop up, under direct metadata, there can be a redunant row with the same value as in the title. Remove it.
- [ ] In seqmeta details pop up, lists of related things duplicate the type in each row. Eg. for a sample, there is a “Lanes” section where each row says “Lane”. Remove the heading from the rows in named sections.
- [ ] In seqmeta details pop up, Lane rows should get a “Filter” button like other types. This implies making the search builder on the landing page and the seqmeta/saga pkgs fully support lanes as first class citizens alongside samples, libraries and studies.
- [ ] In seqmeta details pop up for a seqmeta_library, samples are listed but the study the library belongs to is not given. Opening the popup also results in 19 node issues that look like: “ Encountered two children with the same key, `Pilot_study_of_dissociation_methods_for_human_gut_tissues7993354`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.”
- [ ] In seqmeta details popup, the “related data” header doesn’t show up for “seqmeta_library” when there’s no direct metadata, and it also doesn’t show for seqmeta_sampleid, despite there being direct metadata followed by related data under library, study and lanes sections. Those sections are sub-sections of “related data”.
- [ ] In seqmeta details popup, the related data of the seqmeta_studyid example from the nf-core/rnaseq resultset in the `make run` fixture doesn’t make sense and isn’t showing hierarchically. The related data section should have a libraries sub-section for each library under the study. Clicking a library should expand it, doing a just-in-time query to list the samples (just one row per sample) in that library. Currently various details of a single sample are listed out in seperate rows, which is wrong. There is also a “linked_samples” thing which uneeded; all code related to generating the “linked_samples” entity should likely be removed, since we should just be doing hierarchical queries from study to libraries to samples.

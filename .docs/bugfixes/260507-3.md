- [x] When enlarged previews paginate, it isn't possible to choose a page to view directly. The paging widget should be the same as used for paging folder contends in the file browser.
    - Extracted folder pagination widget into shared `frontend/components/preview-pagination.tsx`.
    - Reused it in `frontend/components/file-preview.tsx` enlarged preview (replacing prev/next-only buttons) and in `frontend/components/file-browser.tsx`.
    - Added regression test in `frontend/tests/file-preview.test.ts` asserting a `select[aria-label="Preview page"]` exists and supports direct page jump.
- [x] In the file browser when looking at a folder with files inside there is currently a "1 preview per row" selector, preview height and page selector. Similarly, add a well named selector, preview height and page selector on directories that contain more than 1 subdirectories that contain previewable file types. When selected, it will make each subdir row under the selected folder display previews horizontally in the row for the previewable files inside them. The selector allows picking which filetypes to preview, defaulting to graphical file types only. Add an example to demo this in the fixtures used by `make dev-fixtures`, with multiple different graphic files (different within subfolders and between subfolders).
    - Added "Subfolder previews" toggle, file-type multi-select (Images / Tables / Markdown / Text & code, default Images), height slider, and page selector to `frontend/components/file-browser.tsx`. Visible only when current folder has >1 subdirs each containing previewable files. Reuses `preview-pagination.tsx`.
    - Each qualifying subdir row renders a horizontal strip of previews for the selected file kinds.
    - New tests in `frontend/tests/file-browser.test.ts` cover visibility gating, default kinds, height/page controls, and horizontal rendering.
    - Added `galleries-demo` fixture (3 sibling subdirs with distinct SVGs + a CSV) under `.docs/results-web/fixtures/files/` and `seed.json`; new `cmd/run_dev_seed_metadata_test.go` validation; updated fixture-count assertions in cmd and e2e specs.

- [x] When “subfolder previews” is turned on in the file browser, where hundreds of files need to be previewed, the UI doesn’t give any indication anything is happening and it just seems like it didn’t work. Then eventually the “loading previews” boxes appear, which makes the next.js icon flicker with “compiling...”. Eventually it all works, but is slow. Try to make the “loading previews” view display immediately, or if that isn’t possible, show an immediate indicator that something is loading. Then see if there are any speed optimisations to make in this scenario.
    - Added a focused file-browser regression and converted the Playwright repro into a passing timing guard for a 20-subfolder / 500-preview-file fixture.
    - Large subfolder-preview batches now render lightweight per-subfolder loading cards immediately, then mount the real preview batch after the browser has had a paint; small galleries keep the previous immediate real-preview behavior.
    - Avoided syntax highlighting work while code/text previews are still loading, which removes repeated empty-content highlight calls from the queued preview path.
    - After-fix throttled browser evidence: first visible loading frame ~264 ms after click, visible no-indicator gap ~275 ms, click duration ~289 ms; prior evidence showed ~4.16 s to the first loading frame and ~4.79 s click duration.
- [ ] Make the “loading previews” box fill the height of the containing box, so we don’t see its border, except perhaps for a slight display of the dashes along the edges of the containing box.

- [x] I still need a stable executable to use after `make dev &`. If producing `.tmp/wa-run-dev-$$` is really the only good fix for tests, at least have `make dev` update a `.tmp/wa` symlink that points to the new exe.
  - `run-dev.sh` keeps the per-process `.tmp/wa-run-dev-$$` binary, then updates `.tmp/wa` as a symlink to it under a small lock.
  - Cleanup removes the per-run binary and only removes `.tmp/wa` when it still points at that run's binary, so newer dev runs are not clobbered.
- [x] Stabilize the run-dev focused gate found by review: `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -v -run TestRunDevScript` must pass reliably with the new symlink tests in place.
  - MLWH failure diagnostics can use a per-test log directory, so neighboring run-dev cases cannot truncate the short-lived helper's `mlwh.log` before the tail is surfaced.
  - The run-dev harness cleanup now terminates through the existing `runDevProcess` wait channel instead of calling `exec.Cmd.Wait` a second time from cleanup.
  - The fixture harness clears stale test-mode server-token state before each `run-dev.sh` start and waits for the current token to succeed for owner login.
- [x] Fix the live MySQL `make test-go` MLWH failures without bypassing the tests: the programme studies EXPLAIN proof must be deterministic, and the study 7699 overview/status proof must still demonstrate sub-second web-responsive reads without failing on one noisy timing sample.
  - Programme studies reads now use a dialect-aware MySQL `FORCE INDEX (study_mirror_programme_idx)` hint on `study_mirror`; SQLite keeps the plain table name.
  - The public `StudiesForProgramme` path and programme export list/count path share that helper, so the EXPLAIN proof checks the query shape actually used by reads.
  - The 7699 performance proof still enforces the 1s SLA, but measures three real reads and asserts the best successful read is sub-second to avoid failing on a single transient wall-clock spike.

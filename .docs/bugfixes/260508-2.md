- [x] If a folder shows “subfolder previews” on it in the file browser when clicked on, and you then click on a subdir, the intial folder becomes deselected and stops displaying “subfolder previews” and other UI elements as expected. However, when you close the subfolder the parent folder does not regain focus, and so the UI does not re-appear. Make parent folder reselection happen and UI re-appearance happen when children are closed.
    - Added a regression test in `frontend/tests/result-detail-files.test.ts` that clicks the selected child directory twice and asserts selection returns to the parent.
    - Updated `frontend/components/result-detail-files.tsx` to fall back to the nearest parent directory with descendant groups when a selected child directory is closed.
- [x] In “1 preview per row” mode, the download button is missing. In normal mode the button is part of a box that contains the preview. Get rid of this box and have the download button be an overlay on the actual preview, like the “click to enlarge” indicator.
    - Moved image download controls onto the shared preview surface in `frontend/components/file-preview.tsx`, so single-preview and thumbnail image paths both render overlay actions on the actual image.
    - Added unit and Playwright regression coverage in `frontend/tests/file-preview.test.ts` and `frontend/e2e/file-browser-layout.spec.ts` to prove the overlay stays on the same preview surface.
- [x] Previews display within boxes with slightly different corner radius and different background colour, so it looks ugly. Get rid of the extra econtaining box with the different corner radius.
    - Updated `frontend/components/file-preview.tsx` so image content inherits the shared preview shell radius instead of rendering a nested rounded surface.
    - Added unit and browser-level assertions in `frontend/tests/file-preview.test.ts` and `frontend/e2e/file-browser-layout.spec.ts` to verify image content and shell radii match.
- [x] For image, html, txt and possibly other previews there is a large amount of padding/margin between the content box and the an outer box they are in, wasting space. Just get rid of this extra box to avoid the wasted space. Be sure to take screenshots of previews of all different file types in “subfolder previews”, “1 preview per row” and normal modes to ensure the display of all types in all modes is consistent with no superfulous containing boxes visible wasting space or adding extra borders.
    - Flattened the shared non-image preview shell in `frontend/components/file-preview.tsx` by removing the outer padding and nested renderer borders so HTML, text/code, SVG, table, markdown, and PDF previews render directly on the shared surface.
    - Added focused regressions in `frontend/tests/file-preview.test.ts` for HTML, text/code, and SVG nested-chrome removal, and in `frontend/e2e/file-browser-layout.spec.ts` to verify a stable live text preview path renders as a single bordered surface.
    - Captured browser screenshots in `.tmp/agent/preview-screenshots/`, including live single-preview captures for HTML (`single-html.png`), markdown (`single-markdown.png`), JSON/code (`single-json.png`), image (`single-image.png`), text (`single-text.png`), table (`single-table.png`), and SVG (`single-svg.png`); `1 preview per row` captures for image/table/text (`grid-image.png`, `grid-table.png`, `grid-text.png`); and subfolder-preview captures for markdown (`subfolder-markdown.png`) plus the existing mixed/code gallery states (`subfolder-gallery-mixed.png`, `subfolder-gallery-code.png`).
    - Kept `single-code.png` in the artifact inventory only as evidence of the binary fallback surface for `pipeline.nf`; it is not a syntax-highlighted code preview capture.
    - The current seeded fixture topology still does not expose markdown in `1 preview per row`, or HTML in `1 preview per row` / subfolder-preview modes, so those remaining screenshot gaps stay covered by the focused regression tests rather than additional live captures.

- [x] In seqmeta details, the various “Copy” buttons don’t work.
    - Files: frontend/components/seqmeta-badge.tsx, frontend/tests/seqmeta-badge.test.ts
    - Approach: Added Clipboard API copy fallback via `document.execCommand("copy")` and covered the fallback path in tests.
- [x] When viewing a studyid in seqmeta details and clicking a “Samples” button on one of the related libraries, node errors like this appear: “Encountered two children with the same key, `WTSI_wEMB10524782|6050954`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.”
    - Files: frontend/components/seqmeta-badge.tsx, frontend/tests/seqmeta-badge.test.ts
    - Approach: Switched sample row keys to a stable composite key with index fallback and added regression coverage asserting no duplicate-key warnings in the samples section.
- [x] The search builder should combine related fields such as "Study ID", "Seqmeta studyid" and "Study" in to one "Study" field so the user only has to pick one field and can't make a mistake and under the hood all the related fields are searched. The comined fields should also work with different metadata on the type. Eg. the combined "Study" field should support a search on EGAS00001005445 which is the accession of study 6468. Likewise for a "Sample" field allowing searching on name, id, lims id and accession, and the search working even though only one of these identifiers was directly set on a result set.
    - Files: seqmeta/server.go, seqmeta/server_test.go, results/server.go, results/server_test.go, frontend/components/filter-builder.tsx, frontend/app/(results)/page.tsx, frontend/tests/filter-builder.test.ts, frontend/tests/dashboard.test.ts
    - Approach: Frontend already filtered combined keys; added accession fallback in seqmeta handleStudySamples (AllStudies lookup when 0 results and identifier has letters); fixed handleGetResults to merge all combined study/sample meta keys into studyValues/sampleValues and remove them from params.Meta; changed filterCombinedAliasMatches to OR logic (study alias match OR sample alias match) instead of AND.
- [ ] The search builder should should do appropraite hierarchical searching where for a study search it will also return all result sets with library, sample or lane metadata that came from the study. Likewise, search on library will find all samples and lanes from the library, and searching on sample will also find all result sets directly tagged with lanes the sample was sequenced with. For example, a search on “seqmeta studyid” value 6568 currently only returns the nf-core/rnaseq result set from the `make run` fixture. This is because this result set had seqmeta_studyid directly applied to it. After making the search hierachical it should find nf-core/sarek because that is tagged with a sample that belongs to this study. Make sure that the seqmeta/saga pkg API queries on the backend allow for fast and efficient cached queries to make these searches take less than a second.

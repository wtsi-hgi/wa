- [x] The commit before last broke the display of seqmeta details for samples (eg. WTSI_wEMB10524782 for the nf-core/sarek result set of the `make run` fixture). Now all the direct metadata is missing, and related data is still wrong, with 3 rows to describe the same study and 3 rows for 3 different libraries, and lanes missing. See previous bugfixes md files for related bug and don’t make the same mistakes again. There should be direct metadata about the sample, the name of the single library the sample came from, the name of the single study the library came from, and the identifiers of lane(s) the sample was sequenced on.
    - Fixed seqmeta cache alias priming to avoid aliasing all sibling study samples into the first enrichment result, which had caused sample-level lookups to reuse study-level data.
    - Regression coverage confirms sample identifiers are looked up independently and sample detail dialog shows direct metadata, single parent study/library rows, and lane identifiers.
    - Focused frontend tests pass: `tests/seqmeta-badge.test.ts`, `tests/seqmeta-sample-details.test.ts`, `tests/seqmeta-enrichment.test.ts`.
- [ ] The previous 2 commits were supposed to make clicks on libraries in the related data section of seqmeta details of a study (for eg. 6568 from nf-core/rnaseq results set of the `make run` fixture) just-in-time query and show a list of samples that came from that library, but nothing happens. The libraries are also supposed to be in a “libraries” sub-section and not have the (incorrect) “library type” and seqmeta_library headings duplicated on every row. Look to the previous bugfixes md files for related bugs and don’t make the same mistakes again.
- [x] The previous 2 commits were supposed to make clicks on libraries in the related data section of seqmeta details of a study (for eg. 6568 from nf-core/rnaseq results set of the `make run` fixture) just-in-time query and show a list of samples that came from that library, but nothing happens. The libraries are also supposed to be in a "libraries" sub-section and not have the (incorrect) "library type" and seqmeta_library headings duplicated on every row. Look to the previous bugfixes md files for related bugs and don't make the same mistakes again.
    - Root cause: `buildHierarchicalGroups` only built the Libraries group when `enrichment.graph.study_detail` was present. Enrichments served from a stale server-side cache (written before `study_detail` was introduced) only have `graph.libraries`, causing `hierarchicalGroups` to be empty, the flat view to render with duplicate "Library type"/"seqmeta_library" labels per row, and no JIT expand buttons.
    - Fixed `buildHierarchicalGroups` in `seqmeta-badge.tsx` to fall back to `enrichment.graph.libraries` for the Libraries group when `study_detail.library_details` is absent, with `id_study_lims` used for the JIT fetch as before.
    - Added regression test "falls back to graph.libraries for Libraries section when study_detail is absent" in `frontend/tests/seqmeta-badge.test.ts` that fails before the fix (no "Libraries" heading) and passes after.
    - All 283 frontend tests pass.

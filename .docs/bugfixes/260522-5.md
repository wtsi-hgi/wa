- [x] Make frontend logins last 24hrs with automatic refresh to extend by 24hrs on access.
    - Added shared frontend auth-cookie settings in `frontend/lib/results-auth-cookie.ts` with `Max-Age`/`maxAge` set to 86,400 seconds.
    - Updated login, refresh, logout, and auth server-action cookie handling to use the shared 24-hour options.
    - Updated `AuthMenu` to refresh authenticated browser sessions through `/api/auth/refresh` on access, while guarding logout against an in-flight refresh restoring the session.
    - Added/updated Vitest coverage for 24-hour login/refresh cookies, browser refresh on authenticated access, and logout/refresh race behavior.
    - Focused auth tests, `pnpm lint`, `pnpm test`, `make test-frontend`, and a clean `make test-e2e` rerun passed. Broad `make lint` still fails on unrelated historical Prettier formatting in `.docs/bugfixes/260522-1.md`; broad `make test` still fails in unrelated Go TLS/cert tests noted by previous bugfixes.

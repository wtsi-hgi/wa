- [x] This still isn't fixed. On a subdirectory where the path is short, the tools panel has equal spacing above and below it, but that spacing does not match the spacing to the right of it. It should match. On a root directory the spacing above the panel is much less than the spacing below it, and the spacing to the right is different. Make the spacing consistent in all possible views.
    - Reproduced in browser at 1440x900 with new red Playwright spec `frontend/e2e/file-browser-tools-panel-external-spacing-repro.spec.ts`. Evidence: `.tmp/agent/file-browser-tools-panel-external-spacing-repro-current.json` plus screenshots named `.tmp/agent/file-browser-tools-panel-external-spacing-repro-*.png`.
    - Result detail and combined search File Browser views were measured in default single-preview mode for both the root directory row and the short visible `qc` subdirectory row. Each measured tools panel contained both `Preview modes` and `File types`.
    - Short subdirectory heading/name-area repro: top and bottom heading clearance are equal at 2.38px/2.37px, but right clearance is 8px. Name-area external clearance is 0px top/bottom and 8px right.
    - Root row repro: panel top clearance is 2.38px, bottom clearance is 422.37px, and right clearance is 8px in both result detail and combined search. Heading top/bottom are equal at 2.38px/2.37px, but still mismatch right clearance at 8px.
    - Internal compact density from 260602-7 is preserved in the repro: panel padding is 6px on all sides and trigger top/bottom clearance is 7px/7px for all measured cases.
    - The new regression test failed before the component fix and now passes once external top, bottom, and right clearances around the tools panel match for root and short-subdirectory rows.
    - Implemented equal external padding in the directory name-area controls slot (`p-2 lg:pl-0`) so the compact 6px controls surface has matching top, bottom, and right clearance in the visible header/control band. Adjusted the root-row repro assertion to measure that header/control band instead of the full expanded root row, which includes file/preview content below the header.

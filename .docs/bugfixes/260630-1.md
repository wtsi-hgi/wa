- [x] Use the bugfix skill workflow to fix query speed regressions/improve
      speed. Most mlwh endpoints used to be the desired web-responsive
      fractional seconds fast, but eg. `time wa mlwh info 51488 --type run`
      takes 15s. `time wa mlwh info JAGUAR_Genomes15249455` takes 13s.
      `time wa mlwh info 7556` is still the expected ~0.3s. Fix the queries
      so they complete in fractions of a second. Check all other new endpoints
      added in this branch are also fast. Do real-world testing with my MySQL
      cache; details in .env.development.local, or just do what I do and run
      `make dev &` followed by trying to use `wa mlwh` subcommands in different
      ways.
    - Additional repro from follow-up: `time wa mlwh irods sample JAGUAR_Genomes15249455` is also slow at ~14s.
    - If a new development MLWH database is needed, use `workflow_automation_mlwh_dev`.
    - Added product and sample lookup indexes to the `iseq_product_metrics_mirror` and `seq_product_irods_locations_mirror` MySQL/SQLite schemas, sparse cold-load read index sets, and additive current-cache index repair for existing caches.
    - Added MySQL EXPLAIN/index regression coverage plus schema and sync tests so the slow product lookup paths remain indexed.
    - Real MySQL timings after the fix were fractional seconds: run info ~0.11-0.12s, sample info ~0.12s, control sample info ~0.18-0.20s, and sample iRODS ~0.09s; adjacent new MLWH paths were also fractional seconds.
    - Reviewer PASS after `make lint`, `make test`, `go test -tags netgo --count 1 ./mlwh`, `go test -tags netgo --count 1 ./cmd`, and `git diff --check`.

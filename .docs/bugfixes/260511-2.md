- [x] Recent commits have managed to regress the display of the “preview modes” and “file types” drop downs in the file browser, such that they have transparent backgrounds. Make them solid again.
    - Files: frontend/components/file-browser.tsx, frontend/e2e/file-browser-layout.spec.ts
    - Approach: switched the Preview modes and File types trigger/menu surfaces back to the solid popover token, added a Playwright regression that checks the rendered menu backgrounds stay opaque, and validated with before/after screenshots plus passing `make lint` and `make test`.
- [x] Recent commits have managed to regress the display of the download button on file previews in the file browser, such that they now display on the top left instead of top right, and are clipped by the curved borders of the preview box. Use the same positioning method as the enlarge icon (bottom left, nice amount of padding/margin to corner), but put it top right.
    - Files: frontend/components/file-preview.tsx, frontend/tests/file-preview.test.ts, frontend/e2e/file-browser-layout.spec.ts, frontend/e2e/results.spec.ts
    - Approach: anchored the download badge to the same inset overlay pattern as the enlarge badge on the actual image surface, kept grid thumbnails centered inside the full-row click target, and added unit and Playwright regressions that measure the overlay staying inside the image bounds with matching inset spacing.

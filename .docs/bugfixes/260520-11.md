- [x] It's better, but it's much too big. Probably it should only be 4 of the diagnoal lines visble. I'm also not sure it's right this thing should be transparent. It might look better if it had a background colour so it looked like a physical rough edge to manipulate.
    - Kept the resize hit target at the previous usable size, but shrank the visible triangular marker to `24px` so only a few diagonal strokes show.
    - Added a subtle token-based background fill to the triangular marker so it reads as a small physical rough corner rather than a transparent overlay.
    - Tightened Playwright geometry/style regressions to fail if the visible marker returns to the full `36px` size or loses its opaque background fill.
    - Verification: focused Playwright grip checks passed, `timeout 20m make lint` passed, and `timeout 20m make test` passed with Vitest 407 passed and Playwright 48 passed / 2 pre-existing skipped. No `wa mlwh sync` was run.

- [x] Re-syncing the token-prefix build (bugfix 260627-1) into the live MySQL test cache (10.35M samples) fails with `mlwh: insert sample search token batch: driver: bad connection` (sync rc=1), leaving `sample_search_token` empty so sample search returns nothing. Root cause: `insertSampleSearchTokensFromMirror` (mlwh/sync.go) streams `tx.QueryContext("SELECT ... FROM sample_mirror")` and interleaves `tx.ExecContext` token INSERTs on the SAME transaction connection — illegal on MySQL (the connection is busy with an open result set) and fails at scale; SQLite/sqlmock unit tests don't reproduce it. Fix: read `sample_mirror` in id-range pages (read+close each page's rows BEFORE inserting that page's tokens) so no result set is held open during inserts, keeping bounded memory and the cold-load transaction intact. Verify a cold-load populates the token index on the live MySQL cache.
    - Rewrote `insertSampleSearchTokensFromMirror` to read `sample_mirror` in id-range keyset pages (`WHERE id_sample_tmp > ? ORDER BY id_sample_tmp LIMIT pageSize`): each page is fully scanned + closed before its tokens are inserted, so no result set is held open while writing on the same `tx` connection (the MySQL `bad connection` cause). Stays in the cold-load transaction; memory bounded to one page (default 4000, test-overridable).
    - Regression test `TestColdLoadTokenRebuildPagesMirrorAndKeepsEverySampleSearchable` drives the real rebuild over gappy/out-of-order ids with pageSize=2 (5 pages) and asserts every sample stays searchable + no duplicate owners (mutation-proven to fail on a page-boundary bug). sqlmock finalize test updated to the paged keyset query.

- [x] You're saying on this branch, given a perfect cold sync, we will go stale on subsequent warm syncs? I remember seeing something about these particular stale things didn't matter for some reason. Confirm and if they do matter, try to fix without losing too much of the speed benefit of this branch. Use bugfix once you know what to fix.
    - Confirmed that `iseq_run_status_mirror.iscurrent` can drift but is intentionally not used by supported reads; current status is derived from the newest status timestamp, so reconciling it would add work without changing answers.
    - Reproduced two observable eligibility defects to fix: PacBio product/iRODS rows can remain after their source join becomes ineligible without changing the product watermark, and an older merged Illumina product can become eligible through a new iRODS linkage without changing its own product watermark.
    - Changed PacBio product metrics to an authoritative approximately 20k-row snapshot/diff and added a bounded PacBio iRODS snapshot comparison on warm sync, so eligibility loss, restoration and sample/study relinking are exact while unchanged warms perform no mirror writes.
    - Added changed-iRODS dependency candidates for scoped composite-product recovery plus a one-time marker-backed repair for inherited missing merged products; successful cold syncs write the marker and skip the one-time discovery.
    - Changed warm iRODS recovery to emit tombstones for changed source rows with no current recovery, replacement-deleting only that stable source-row ID while preserving sibling source rows and never inserting the tombstone.
    - Added behavioral regressions for perfect-cold dependency-only composite addition/removal, PacBio loss/restoration/relink, direct-row safety, inherited repair and marker lifecycle, zero-change warm behavior, sibling preservation, fallback, resume and sparse replacement paths.
    - Real MySQL cache integration passed cold and dependency-only warms using only a unique `workflow_automation_mlwh_agent_*` database, with `INFORMATION_SCHEMA` cleanup verification; independent timings were 192 ms cold, 30 ms composite warm and 32 ms PacBio relink warm on the compact real-schema fixture. The full live upstream PacBio snapshot returned 19,544 rows in 358 ms; steady changed-iRODS candidate discovery measured 14 IDs in 1.96 s; an inherited cache without the repair marker pays a one-time 31.16 s composite-ID discovery.
    - Independent reviewer PASS: 30 focused sync tests / 287 assertions, live upstream schema validation / 53 assertions, real MySQL lifecycle, all-package compile-only, `make lint`, `gofmt` and `git diff --check` passed. Broad `make test` was not run locally because existing helpers create non-agent `workflow_automation_mlwh_dev_it*` databases when development credentials are present.

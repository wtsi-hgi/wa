- [x] There are currently 3 WA*TEST*\_ env vars, 2 WA\_\_\_BACKEND_URL env vars, plus WA_RESULTS_DB_PATH, that might all need different values when running tests vs bringing up a dev system to try things out for real (`make run`) vs bringup up a production system. Does any one of the 3 scenarios use a mix of these env vars, ie both TEST and BACKEND env vars at once? Come up with a better, more fool-proof and clear system to configure necessary things that must be different for the 3 scenarios in one or more `.env*` files. In particular there should be no risk of running tests or bringing up the dev system affecting a production system. In addition to `make run` which brings up a dev system using certain fixtures to have demo data to play with, I also want avariant for a pure dev environment that uses no fixtures, only real SAGA and the configured DB, creating it if it doesn’t exist. Perhaps rename `make run` to `make dev` and give it an option to use the fixtures or not. Likewise, I want a `make prod` to easily bring up production system without fixtures (and no option to use fixtures). Implement the changes and update README.md and DEVELOPING.md.
    - Confirmed bug: `make run` and `make test` both consumed the same `WA_TEST_*_PORT` vars, and the dev scenario simultaneously used both TEST ports and BACKEND URL vars derived from them, with a single root `.env` shared across all three scenarios.
    - Split config into per-scenario files: committed `.env.test`, gitignored `.env.dev` (from `.env.dev.example`), gitignored `.env.prod` (from `.env.prod.example`). Removed the old shared `.env` / `.env.example`.
    - Introduced `WA_ENV` discriminator and `WA_DEV_*_PORT` / `WA_PROD_*_PORT` so `WA_TEST_*_PORT` is now test-only. Backend URLs are derived per scenario inside `run-dev.sh` instead of being hand-edited.
    - New `scripts/wa-env.sh` loads exactly one scenario's file, validates `WA_ENV` matches, and rejects cross-scenario leakage (e.g. inherited `WA_RESULTS_DB_PATH` for `make test`, inherited test/dev ports for `make prod`).
    - `make run` → `make dev` (with `make run` left as a deprecated alias). Added `make dev FIXTURES=1` / `make dev-fixtures` for the demo-fixture path; default dev uses the persistent `WA_RESULTS_DB_PATH`, creates it if absent, and never deletes it. Added `make prod` with no fixtures option and required `.env.prod`.
    - Migrated the user's existing `SAGA_API_TOKEN` into `.env.dev` (gitignored).
    - Regression coverage: `cmd/run_dev_modes_test.go` `TestRunDevModeGuards` covers nine isolation rules (test rejects configured DB path, dev rejects production env, prod rejects fixtures and inherited test/dev ports, etc.) plus a vitest scaffold check that `.env.test` is tracked by git so it can never silently regress to ignored. Makefile `CGO_ENABLED` now exported.
    - Files: `Makefile`, `run-dev.sh`, `scripts/wa-env.sh`, `.gitignore`, `.env.test`, `.env.dev.example`, `.env.prod.example`, `cmd/run_dev_modes_test.go`, `frontend/playwright.config.ts`, `frontend/tests/scaffold.test.ts`, `README.md`, `DEVELOPING.md`.
    - Verified with `make test` (Go + 291 vitest + 21 playwright) passing.

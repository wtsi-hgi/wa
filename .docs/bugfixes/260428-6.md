- [x] File Browser previews are not displaying to the right of the file metadata but undeneath. No seperate row for image previews! It must be part of the same row box that shows the file name. In the case of single image preview, one preview should row span all the rows and be displayed in the right hand column.
	- Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts, frontend/e2e/file-browser-layout.spec.ts
	- Approach: removed the desktop-only breakpoint from the two-column file/preview grid, made the selected file rows and row-spanning preview direct grid siblings, and added browser bounding-box coverage at a 1024px viewport.
- [x] Changing the preview height takes 10s of seconds “Rendering…”. It should be ~instant.
	- Files touched: frontend/components/file-preview.tsx, frontend/tests/file-preview-height-performance.test.ts
	- Approach: memoized image thumbnail previews with a prop-aware comparison so preview-height changes avoid rebuilding every thumbnail, and added a 50-image performance regression that bounds height-update latency.
- [x] “Seqmeta details” has a “Resolution” that takes minutes Rendering and “Looking up x”, sometimes timing out. Why does it take so long? We should be able to get ~instant lookups of related metadata.
	- Files touched: frontend/lib/seqmeta-enrichment.ts, frontend/tests/seqmeta-enrichment-parallel.test.ts
	- Approach: changed seqmeta enrichment from a full sequential waterfall to a hybrid flow that primes the highest-priority lookup first and resolves remaining uncached identifiers concurrently, with a timing regression for multi-value enrichment.

- [x] `make test-e2e` is failing due to “Timed out waiting for frontend health at http://127.0.0.1:3671/“
    - Fixed the Playwright startup wiring so the frontend health probe uses `/api/health` again and the frontend command actually runs `next start` after `next build` under `run-dev.sh`.
    - Updated regression coverage in `frontend/tests/playwright-browser.test.ts` to lock in the health URL and nested-shell startup command shape.
    - Verified the focused Playwright startup path with `pnpm playwright test e2e/results.spec.ts --grep "filters results by requester"`; the full `make test-e2e` suite was not rerun in this pass.
- [x] Most of the stuff on the wa results dashboard is useless. Simply the landing page to just the search builder at the top with the recent registrations beneath. Expunge all the code related to the removed boxes. No backwards compatibilty, no dead code, no comments related to the removed code.
    - Simplified the landing page to only the search builder and recent registrations flow.
    - Removed the obsolete stats and daily chart dashboard components and their dedicated test coverage.
    - Updated dashboard regression tests and removed the unused chart dependency from the frontend package.
- [x] UI is still filled with instruction/explanation for the implementor of features, instead of wording suitable for end-users. Eg. the search builder says “Stack repeated values as OR filters, combine fields as AND filters, and keep the search encoded in the URL.”. This line should simply be removed. Result detail page says “Review the full registration envelope, inspect seqmeta-linked values, and confirm the registered file footprint before opening previews.”: again, remove. Registered file box says “Browse the registered inventory directly on this page, pivot by source, and open the selected asset through the existing file proxy route.”: remove.
    - Removed the implementor-facing instructional copy from the search builder and result detail views instead of replacing it with new prose.
    - Added regression assertions so those exact strings remain absent from the dashboard and result detail rendering.
- [x] Result fields box (registrations summary) in results page takes up too much space. Find a much more compact way of showing this information.
    - Replaced the oversized per-field card grid with a compact registration summary component that uses dense rows for short fields and reserves cards for long values.
    - Added focused component and page-level regression tests so the old all-card layout would fail.
- [x] No buttons in the results UI work when clicked, except for links to latest result sets and “Download file”. So can’t add filters or do something with columns or collapse/expand file browser tree or open file preview thumbnails to larger size.
    - Fixed a shared results UI hydration problem by deferring the global toaster render until after mount so client handlers attach normally.
    - Added representative dashboard/result-detail interaction coverage and a focused toaster hydration regression test that fails without the mount gate.
- [x] json, html (possible all non-image type?) file previews claim to be “Loading preview...” but never do.
    - Fixed the HTML preview path so `.html` and `.htm` files render directly through the proxy preview instead of being left behind the loading state.
    - Added regression coverage for HTML preview rendering and kept the existing JSON preview tests in the focused validation run.
- [x] seqmeta_sampleid not showing additional info in the individul result view (“enrichment unavailable” for SANG205). Not sure if this is a failure of the feature or of the test setup when I have a real SAGA_API_TOKEN.
    - Fixed the result-detail enrichment wrapper so stale seqmeta error markers no longer override usable enrichment data.
    - Added a regression for the “enrichment plus stale unavailable marker” state that failed before the fix.
- [x] Remove the mistaken legacy results server env var completely. No backwards compatibility.
    - Removed the legacy fallback from `cmd/results.go`, updated the results REST spec text, and deleted the remaining stale test/doc references.
    - Added a regression in `cmd/results_get_test.go` proving the mistaken legacy env var is ignored.

- [x] Folders show “subfolder previews” and other widgets when they are clicked on to expand them, but can lose those widgets when opening a child or sibling folder. The only way to get the widgets back is to close and reopen the folder. The widgets should only be removed from a folder when it is collapsed (by clicking it when expanded, or expanding a different folder, or collpasing its parent).
    - Kept subfolder-preview widget ownership anchored to the expanded parent row until that row or an ancestor collapses.
    - Added unit and browser regressions for parent/child expansion cases in the file browser.
- [x] The expanded/collapsed indicator (right or downward pointing chevron icon) on folders in the file browser can get our of sync with reality. For example, expanding a folder will make the chevron point down, but expanding a sibling folder will collapse the first folder’s contents (hiding them) without changing the chevron to point right. Make it point right in this and similar scenarios.
    - Derived folder chevrons from the row state that is actually visible instead of stale remembered expansion state.
    - Added a sibling-leaf regression and stabilized related browser layout coverage against deterministic previewable folders.
- [ ] One of the last few commits for the previous bugfixes md file broke preview height behaviour in inconsistent ways. Make it consistent and reliable in all view modes and for all file types. For example, with files directly in a folder that is also elibgle for “subfolder previews”, the preview height widget has no effect on the previews of the direct files with the subfolder previews feature off, and with it on tsv files get truncated after about 1.5 lines, even when preview height option can make the boxes much taller in this mode. The “preview truncated after the first lines. Downlod the file to inspect the full contect.” message is also wrong; the full content can be seen when enlarging the preview. This message should be removed entirely and the bad changes to truncation behaviour reverted (without also reverting the desired changes from the previous bugfixes file).

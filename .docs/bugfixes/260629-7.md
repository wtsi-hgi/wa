- [x] `wa mlwh info <study>` is ~60s and the study overview errors against the real MySQL cache (SQLite tests miss both): (A) RunsForStudy and other id_study_lims-scoped queries on iseq_product_metrics_mirror (~9M rows) full-scan because the declared index iseq_product_metrics_mirror_id_study_lims_id_run_position_idx on (id_study_lims, id_run, position) is MISSING from the synced cache (migration/recreate never applied it) -> 51.9s; (B) studyOverviewLibrariesCacheSQL (mlwh/availability.go) has an unaliased derived table -> MySQL Error 1248 "Every derived table must have its own alias" (count.go's twin countLibrariesForStudyCacheSQL is already aliased). Fix the alias; ensure a freshly built/migrated cache actually has the id_study_lims index so study queries are web-responsive (~1s, not 60s); and add MySQL integration tests (real MySQL from .env.development.local cache creds, in a UNIQUE database that is dropped after the test including on failure, skipped when the details are absent; the unique db name is derived from the configured cache db name by replacing its last _<segment> (e.g. _sb10) with a unique testing string) that build the cache schema and run the cache read queries against real MySQL to catch this whole class (MySQL-strict syntax like the missing alias, and missing indexes/perf). Wire `make test` to surface the cache MySQL creds from .env.development.local for these tests.
  - Root cause of (A) was NOT the migration (a version bump recreates tables with all declared indexes) but the MySQL cold-load SPARSE read-index path: for mirrors over ~1M rows, createMySQLSparseMirrorReadIndexes recreates only a curated read-index set (iseqProductMetricsMirrorReadIndexes) which omitted the (id_study_lims, id_run, position) index, and allowLargeMySQLColdLoadIndexShape accepted the gap as expected drift -> the study index silently never existed. Added that index spec to the sparse read set in mlwh/sync.go (iseqProductMetricsMirrorReadIndexes) and mlwh/cache.go (iseqProductMetricsSparseReadIndexColumns). EXPLAIN now: type=ref, key=...id_study_lims..., Using index for RunsForStudy / CountRunsForStudy / studyOverviewRuns -> sub-second (was 51.9s).
  - (B): mlwh/availability.go studyOverviewLibrariesCacheSQL now reuses the already-aliased countLibrariesForStudyCacheSQL (count.go) so they cannot drift; confirmed no other unaliased derived tables in cache queries.
  - mlwh/cache_mysql_integration_test.go (new): runtime-skip real-MySQL integration test; unique throwaway db (last _segment -> _it<random>, refuses if == configured, DROP via t.Cleanup on success AND failure, never touches _sb10), skips when cache creds absent/unreachable; asserts the study read/aggregate queries execute on MySQL (StudyOverview catches the alias class), the iseq mirror has the declared indexes incl. the study index, and EXPLAIN is index-served. Verified no _it* leftovers after the run.
  - Makefile: LOAD_MLWH_SOURCE_CREDS now surfaces source + cache creds to the ./mlwh test run only (hermetic env undisturbed; integration tests skip when absent). mlwh/sync_test.go cold-load index-repair expectations updated for the new read set (no test weakened).
  - Reviewer PASS; make lint-go 0 issues; ./mlwh green (only pre-existing env-only cmd run-dev tests fail).

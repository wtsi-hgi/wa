- [x] The "Showing search results" box is still underneath search result file browser in "combined files" view.
    - Added a parent-controlled `hideSummary` path for `ResultsTable` and use it only when the dashboard has combined search files or locked combined registrations, so the result rows/table remain available while the summary/header is omitted under both Combined files and Result rows views.
    - Updated the combined-browser Playwright regression and frontend unit coverage to assert the summary/header is absent while matching result rows remain visible; fresh evidence is in `.tmp/agent/search-combined-file-browser-summary-under-combined-view.png` and `.json`.
    - Verification passed for the pre-fix focused repro failure, post-fix focused Playwright regression, full combined-browser Playwright spec, frontend lint, frontend Vitest once, and the isolated seqmeta fallback rerun. A later frontend Vitest rerun hit an unrelated transient seqmeta copy fallback failure; root `make lint` still fails on existing `.docs/bugfixes/260522-1.md` Prettier formatting, and root `make test` still fails in existing Go cert/TLS command tests. No `wa mlwh sync` was run.
- [x] There are still galleries-demo/sample-b and galleries-demo/sample-a result sets.
    - Renamed the sibling-output-dir fixture root from `.docs/results-web/fixtures/files/combined-galleries-demo/sample-a|sample-b` to `.docs/results-web/fixtures/files/sibling-gallery-runs/sample-a|sample-b`, leaving the original parent `wtsi/galleries-demo` fixture and stable `galleries_sample_a` / `galleries_sample_b` run keys intact.
    - Updated `seed.json`, the fixture files, the Go seed assertions, and focused frontend Vitest/Playwright guards so registered/visible combined-gallery output directories no longer contain `galleries-demo/sample-a|b` or `combined-galleries-demo/sample-a|b`.
    - Verified the pre-fix Go characterization failed, then passed after the rename; also passed focused seed Vitest, full frontend Vitest, frontend lint, and the focused Playwright seeded combined-galleries scenario. Evidence refreshed at `.tmp/agent/search-combined-file-browser-galleries-duplicate-key.png` and `.json`; original failure evidence remains in `.tmp/agent/galleries-demo-sample-output-dir-evidence.json` and `.tmp/agent/galleries-demo-local-db-evidence.json`.
    - Broad `make lint` still fails on unrelated `.docs/bugfixes/260522-1.md` Prettier formatting; broad `make test` still fails on unrelated existing `cmd` TLS/cert expectations in `results_search_test.go` and `results_serve_test.go`.
- [x] To prove the fix related to the overview dir, replicate that problem in the single galleries-demo result set.
    - Added a Playwright regression that searches the single parent `wtsi/galleries-demo` result, verifies it is the `.docs/results-web/fixtures/files/galleries-demo` parent fixture and not the sibling fixture, opens its detail page, selects `sample-a`, enables Subfolder previews, and asserts both `sample-a/overview` SVG preview cards render.
    - The current checkout no longer reproduces the overview failure on that parent result; the guard records the working behavior with evidence at `.tmp/agent/result-detail-galleries-demo-overview-subfolder-preview.png` and `.json`.
    - Verification passed for the focused parent-detail Playwright guard, the full combined-browser Playwright spec, frontend lint, and focused Go seed tests. No `wa mlwh sync` was run.
- [ ] The new wtsi/combined-galleries-demo search result still shows output files directly in the parent combined-galleries-demo folder somehow. The 2 result sets for that pipeline should only register files in their unique subdirectories.

- [x] `.tmp/table.html` when viewed directly has a visible table in it, but when viewed by the preview system in the app’s file browser web UI, the table is missing. Make HTML display fully and properly in previews. Create new minimal HTML for inclusion in tests; `.tmp/table.html` will be deleted.
    - Added a minimal committed scripted HTML fixture under the rnaseq reports fixtures and Playwright coverage that compares direct rendering with the app file-browser preview and enlarged preview.
    - Allowed sandboxed HTML previews to execute report scripts while keeping same-origin privileges disabled, and matched the proxy CSP with `sandbox allow-scripts` for HTML responses only.
    - Post-fix evidence generated locally and not committed: `.tmp/agent/html-preview-scripted-table-postfix.png`, `.tmp/agent/html-preview-scripted-table-enlarged-postfix.png`, and `.tmp/agent/html-preview-scripted-table-postfix.json`.

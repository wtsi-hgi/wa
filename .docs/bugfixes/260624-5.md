- [x] It still doesn't work. What kind of testing is being done here? It should be obvious that a required test to register a result set with the tiffs in it, then screenshot the file browser view. Confirm it says "preview unavailable" right now, then fix it and confirm the tiff is visible afterwards. This should become part of a committed integration test, but using a small generated multichannel tiff. Outside of what is committed you should personally confirm the system really works when you register my .tmp/\*tiff and keep cycling until you can see my tiffs properly in the file browser.
    - Fixed the Go results file route to support `HEAD` on public and authenticated file endpoints, reusing the same auth, existence, registration, and `X-WA-Resolved-File-Path` checks while returning no body and preserving `GET` streaming behavior.
    - Added backend `HEAD .../file?download=true` coverage and a committed Playwright integration that registers a generated nested small multichannel OME-TIFF, opens the file browser, requires a decoded preview image with reasonable rendered dimensions, rejects `Preview unavailable`, and verifies OME controls appear only after enlarging.
    - Updated TIFF inline preview behavior so the file browser shows a plain first-channel/Z/T derived plane image immediately, without inline metadata pills, channel dropdowns, or sliders. Enlarged image preview still fetches metadata and exposes channel/Z/T controls.
    - Fixed the visual fit by rendering the inline derived plane through the same image-preview shell with `object-contain` sizing and stable thumbnail dimensions.
    - Before-fix evidence: `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-preview-unavailable.png`; focused generated OME-TIFF e2e failed before the backend `HEAD` route was added.
    - Fresh proof screenshots after the inline/enlarged visual fix: generated fixture `/home/ubuntu/wa/.tmp/agent/ome-tiff-file-browser-generated-visible.png`; real inline `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-inline-ome.png`; real collapsed inline `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-inline-collapsed.png`; real enlarged controls `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-enlarged-controls.png`.
    - Verification passed: focused `go test -tags netgo --count 1 ./results -v -run TestServerGetFile`, focused `pnpm exec vitest run tests/file-preview.test.ts`, focused Playwright generated OME-TIFF repro, temporary real-fixture Playwright proof, `make lint`, and `make test`; no `wa mlwh sync` was run.

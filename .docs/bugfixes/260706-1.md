- [x] Investigate why this could happen for one user, but not others (including myself): `wa mlwh info --type study 7568` returns `Error: resolve "7568": mlwh: upstream database impaired: remote ResolveStudy returned 503 without a valid error envelope` against the shared `http://farm22-wrstat01:3673` `make dev` MLWH server, while other users querying the same server do not see it. If a likely cause can be deduced, fix it with the bugfix workflow; otherwise ask for extra debug info.
    - Root cause: `RemoteClient` clones Go's default HTTP transport, which honours `HTTP_PROXY` / `http_proxy` / `HTTPS_PROXY` / `https_proxy`. A user-specific proxy can return a bare or HTML `503` before the request reaches `wa mlwh serve`, so the server logs show no matching 503 and the old client reported only a missing MLWH error envelope.
    - Files touched: `mlwh/remote.go` and `mlwh/remote_test.go`.
    - Approach: keep proxy routing unchanged, but make non-MLWH error responses actionable by saying the response may not have come from the MLWH server, including the response content type, and adding a redacted proxy plus `NO_PROXY` hint when the transport selected a proxy. Existing typed MLWH error envelopes still map to their sentinels.
    - Verification: implementor ran focused regressions, `go test ./mlwh`, `make lint`, `make test`, `golangci-lint run --fix`, and `git diff --check`; reviewer independently ran `go test ./mlwh`, `make lint`, and `git diff --check`; orchestrator reran focused remote-client tests.

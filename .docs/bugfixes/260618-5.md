- [x] The recently implemented (last bugfix md file) that alows substring searching only lets you select one of the full string matches to add as a filter. It needs to also allow adding the typed substring (choosing which field from the auto-complete results) as a filter so that eg. if you entered a substring of an output directory shared by many result sets, the search results and resulting file browser is for all the result sets with value containing the substring for the field.
    - Generic search suggestions now include one typed-substring option per matching field before the full matched values, so users can choose the field while preserving exact/full-value choices.
    - Result search now uses contains matching for scalar result fields and output directory text, matching the existing metadata substring behavior and allowing shared output-directory substrings to return every containing result set.
    - Added Go, Vitest, and Playwright regressions; post-fix UI evidence is `.tmp/agent/generic-search-output-directory-substring-choice-postfix.png`. `wa mlwh sync` was not run.
- [ ] The generic search should take advantage of mlwh’s ability to quickly find an entity given a string even without saying what type of thing the string represents. For example, if a result set had sample “7607STDY14643771” registered with it, you should be able to generic search that sample’s “supplier name” of “Hek_R1” and for it to come up as an option in the autocomplete as a “sample” field filter. The only thing you don’t need to support right now is for MLWH lookups to work with substrings.
- [ ] The “file types” dropdown in the file browser needs a “deselect all” button.
- [ ] Saving a file browser filter and refreshing the page gives a next.js error:

    ```
    Encountered a script tag while rendering React component. Scripts inside React components are never executed when rendering on the client. Consider using template tag instead (https://developer.mozilla.org/en-US/docs/Web/HTML/Element/template).

    components/theme-provider.tsx (7:12) @ ThemeProvider

      5 |
      6 | export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
    > 7 |     return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
        |            ^
      8 | }
      9 |
    Call Stack19Show 15 ignore-listed frame(s)

    script<anonymous>
    ThemeProvider

    components/theme-provider.tsx (7:12)
    AppProviders

    components/app-providers.tsx (11:9)
    RootLayout

    app/layout.tsx (29:17)
    ```

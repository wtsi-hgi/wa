- [x] `make test` and `make lint` arenot currently passing. Fix them to pass without bypasses.
    - Root causes:
        1. Go lint failed staticcheck QF1001 on `seqmeta/server.go` because the cached-enrichment guard used `!(a && b)`.
        2. The scaffold build regression still ran the real Next build, but its 30s Vitest timeout was too low under the full suite's parallel load; final runs measured the build assertion at 44-52s.
        3. Playwright lane-filter e2e failed because the frontend seqmeta contract rejected already-normalized `sample_name` sample payloads from the test seqmeta backend, so lane details were shown as an impaired upstream response.
    - Fixes:
        - Rewrote the seqmeta cached-enrichment guard to the equivalent `!a || !b` form.
        - Kept the scaffold build assertion enabled and bounded, but raised its test and child-process timeout to 120s and disabled Next telemetry for the child build.
        - Added a contract regression for normalized sample enrichment payloads with `sample_detail.lanes`, then updated the parser to accept both raw MLWH `name` samples and normalized frontend/cache `sample_name` samples.
    - Verification:
        - Failed before fix: `timeout 15m make test`, `timeout 10m make lint`, and focused `timeout 2m pnpm exec vitest run tests/contracts.test.ts -t "accepts normalized sample enrichment payloads with sample detail lanes"`.
        - Passed after fix: focused seqmeta Go tests, `make lint-go`, the full Vitest suite, focused Playwright lane-filter e2e, and final `timeout 15m make test` plus `timeout 10m make lint`.

- [x] `export WA_ENV=development`, `make dev &` then `.tmp/wa results register --operator fs18 --user dvs --runid 48522 --additional-unique random_exon --run 48522 --study 7607 --sample 7607STDY14643771 --library Custom --nextflow-workflow /lustre/scratch126/gengen/projects_v2/lehner_splicing/nf_splicing/main.nf --input-file /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/input_files/random_exon/run_48522/sample_sheet.tsv --command "nextflow run -resume /lustre/scratch126/gengen/projects_v2/lehner_splicing/nf_splicing/main.nf -with-trace run_48522.nf_trace.txt --sanger_module true --sample_sheet /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/input_files/random_exon/run_48522/sample_sheet.tsv --outdir /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/output_files/random_exon/run_48522 --library random_exon --do_spliced_products true" /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/output_files/random_exon/run_48522` takes a minute to run. Should be less than a second. What is the slow part? Make it fast. (The paths do not exist on this server, so just come up with your own paths in `.tmp` to test with.)
    - Root cause: `results register` opened the full MLWH client whenever `WA_MLWH_DSN` was set. That parsed and pinged the upstream MySQL source before using the already-synced cache, so development registration waited on source DB connectivity even though register only needs cache-backed resolver reads.
    - Timing evidence: with a `.tmp/agent` output tree and a fake local MySQL listener that accepts but never speaks, the pre-fix command spent `elapsed=2.01` in source ping and failed with `open mlwh resolver client: mlwh: ping source db: invalid connection`. After the fix, the same command returned in `elapsed=0.02` and failed immediately on the intentionally empty cache instead, proving the source ping was avoided.
    - Fix: changed the register resolver opener to use `mlwh.OpenCacheOnly` unconditionally, matching the cache-read behavior documented for `mlwh info` and preserving metadata resolution/validation through the same resolver methods.
    - Regression: added a CLI registration test with a warm MLWH cache row and a poisoned `WA_MLWH_DSN`; it fails before the fix and passes only when `register` resolves from cache without opening/parsing the upstream source DSN.

- [ ] Following the above commands, the web frontend says “Upstream services were unavailable while resolving this sanger sample id value.” in seqmeta details for the sample. Likewise for study. Library type and runid get “No enrichment matched this runid value.”. Despite `.tmp/wa mlwh info 7607STDY14643771` working perfectly and in less than a second.

- [ ] Using `make dev-fixtures`, getting seqmeta details for the example results with the sampleid set gives “No enrichment matched this sanger sample id value.” despite `.tmp/wa mlwh info WTSI_wEMB10524782` working perfectly and in less than a second. “Upstream services were unavailable while resolving this study identifier value.” for the studid example. The library example just hangs the browser.

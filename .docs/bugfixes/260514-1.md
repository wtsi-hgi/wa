- [x] `make test` and `make lint` arenot currently passing. Fix them to pass without bypasses.
    - Root causes:
        1. Go lint failed staticcheck QF1001 on `seqmeta/server.go` because the cached-enrichment guard used `!(a && b)`.
        2. The scaffold build regression still ran the real Next build, but its 30s Vitest timeout was too low under the full suite's parallel load; final runs measured the build assertion at 44-52s.
        3. Playwright lane-filter e2e failed because the frontend seqmeta contract rejected already-normalized `sample_name` sample payloads from the test seqmeta backend, so lane details were shown as an impaired upstream response.
    - Fixes:
        - Rewrote the seqmeta cached-enrichment guard to the equivalent `!a || !b` form.
        - Kept the scaffold build assertion enabled and bounded, but raised its test and child-process timeout to 120s and disabled Next telemetry for the child build.
        - Added a contract regression for normalized sample enrichment payloads with `sample_detail.lanes`, then updated the parser to accept both raw MLWH `name` samples and normalized frontend/cache `sample_name` samples.
    - Verification:
        - Failed before fix: `timeout 15m make test`, `timeout 10m make lint`, and focused `timeout 2m pnpm exec vitest run tests/contracts.test.ts -t "accepts normalized sample enrichment payloads with sample detail lanes"`.
        - Passed after fix: focused seqmeta Go tests, `make lint-go`, the full Vitest suite, focused Playwright lane-filter e2e, and final `timeout 15m make test` plus `timeout 10m make lint`.

- [x] `export WA_ENV=development`, `make dev &` then `.tmp/wa results register --operator fs18 --user dvs --runid 48522 --additional-unique random_exon --run 48522 --study 7607 --sample 7607STDY14643771 --library Custom --nextflow-workflow /lustre/scratch126/gengen/projects_v2/lehner_splicing/nf_splicing/main.nf --input-file /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/input_files/random_exon/run_48522/sample_sheet.tsv --command "nextflow run -resume /lustre/scratch126/gengen/projects_v2/lehner_splicing/nf_splicing/main.nf -with-trace run_48522.nf_trace.txt --sanger_module true --sample_sheet /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/input_files/random_exon/run_48522/sample_sheet.tsv --outdir /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/output_files/random_exon/run_48522 --library random_exon --do_spliced_products true" /lustre/scratch126/gengen/projects_v2/lehner_splicing/3_splicing_analysis/output_files/random_exon/run_48522` takes a minute to run. Should be less than a second. What is the slow part? Make it fast. (The paths do not exist on this server, so just come up with your own paths in `.tmp` to test with.)
    - Root cause: `results register` opened the full MLWH client whenever `WA_MLWH_DSN` was set. That parsed and pinged the upstream MySQL source before using the already-synced cache, so development registration waited on source DB connectivity even though register only needs cache-backed resolver reads.
    - Timing evidence: with a `.tmp/agent` output tree and a fake local MySQL listener that accepts but never speaks, the pre-fix command spent `elapsed=2.01` in source ping and failed with `open mlwh resolver client: mlwh: ping source db: invalid connection`. After the fix, the same command returned in `elapsed=0.02` and failed immediately on the intentionally empty cache instead, proving the source ping was avoided.
    - Fix: changed the register resolver opener to use `mlwh.OpenCacheOnly` unconditionally, matching the cache-read behavior documented for `mlwh info` and preserving metadata resolution/validation through the same resolver methods.
    - Regression: added a CLI registration test with a warm MLWH cache row and a poisoned `WA_MLWH_DSN`; it fails before the fix and passes only when `register` resolves from cache without opening/parsing the upstream source DSN.

- [x] Following the above commands, the web frontend says “Upstream services were unavailable while resolving this sanger sample id value.” in seqmeta details for the sample. Likewise for study. Library type and runid get “No enrichment matched this runid value.”. Despite `.tmp/wa mlwh info 7607STDY14643771` working perfectly and in less than a second.
    - Root causes:
        1. Seqmeta enrichment did not try the broad `ResolveSample` resolver path used by `results register`/`mlwh info`, so a canonical sample name stored in `seqmeta_sampleid` could miss the direct `sanger_sample_id` classifier and be reported as unresolved.
        2. Run enrichment required sample expansion through `FindSamplesByRunID`; if the run itself existed in the cache but no expandable sample rows matched, `/enrich/{run}` returned no match instead of a `run_id` enrichment.
        3. Library enrichment pre-filtered candidate library types with a whitespace heuristic, so one-word `pipeline_id_lims` values such as `Custom` never queried the cache.
    - Fixes:
        - Added a fallback sample classifier that calls the provider's cache-backed `ResolveSample` after the direct sample classifiers, preserving hydrated direct lookups while accepting canonical registered sample names and other supported sample aliases.
        - Added a run-only fallback that calls `ResolveRun` when sample expansion is empty or not found, returning a partial `run_id` enrichment instead of a frontend not-found state.
        - Removed the one-word rejection from the library classifier so exact cache-backed library-type lookups are attempted for registered `seqmeta_librarytype` values.
    - Regression:
        - Added seqmeta enrichment tests for registered canonical sample names, run IDs that resolve without sample expansion, and one-word library types.
        - Added stale-negative cache regressions for canonical sample names, run IDs, and one-word library identifiers so old 404 rows cannot mask newly resolvable values.
        - Added frontend cache regression coverage proving cached `null` entries for `Custom`, `48522`, and canonical sample names are refreshed instead of reused.
    - Reviewer follow-up:
        - Server enrich now ignores all negative cache rows and no longer stores 404 no-match responses, so previous misses do not keep registered identifiers stuck.
        - Frontend enrich now treats cached `null` as unusable for every seqmeta value and overwrites stale null aliases when a fresh positive enrichment arrives.
    - Verification:
        - Failed before fix: `timeout 2m env CGO_ENABLED=1 go test -tags netgo --count 1 ./seqmeta -v -run TestEnrichUsesMLWHDetailGraphs`.
        - Reviewer follow-up failed before fix: `timeout 2m env CGO_ENABLED=1 go test -tags netgo --count 1 ./seqmeta -v -run TestServerEnrichEndpoint` and `timeout 2m pnpm --dir frontend exec vitest run tests/seqmeta-enrichment-parallel.test.ts`.
        - Passed after fix: focused seqmeta test, `timeout 3m env CGO_ENABLED=1 go test -tags netgo --count 1 ./seqmeta -v`, focused frontend enrichment tests, final `timeout 15m make test`, and final `timeout 10m make lint`.

- [x] Using `make dev-fixtures`, getting seqmeta details for the example results with the sampleid set gives “No enrichment matched this sanger sample id value.” despite `.tmp/wa mlwh info WTSI_wEMB10524782` working perfectly and in less than a second. “Upstream services were unavailable while resolving this study identifier value.” for the studid example. The library example just hangs the browser.
    - Root causes:
        1. Sample enrichment still reached broad sample classifiers before trying the exact canonical sample-name path that made `mlwh info WTSI_wEMB10524782` fast, so the fixture sample could stall on unindexed/expensive sample-cache scans.
        2. Library enrichment used a uniqueness-style `FindSamplesByLibraryType` lookup; real library types such as `Chromium single cell 3 prime v3` match many samples, so they failed or encouraged huge downstream payloads instead of returning a bounded library-type graph.
        3. The frontend enrichment contract rejected raw MLWH sample nodes from live study enrichment because they use `sanger_sample_id`, `name`, `studies`, and `libraries`, not the already-normalized `sanger_id`, `sample_name`, `id_study_lims`, and `library_type` shape.
    - Fixes:
        - Added a seqmeta exact sample-name classifier backed by `ResolveSampleName` and wired the seqmeta MLWH adapters to expose it, so canonical fixture sample names use the same cache-backed fast path as `mlwh info`.
        - Changed library-type enrichment to fetch a bounded page via `SamplesForLibraryType(limit=MaxLibrarySamples+1)` and kept the existing cross-study payload cap, returning 200 samples with `samples_truncated` for the fixture library instead of freezing the browser.
        - Extended the frontend Zod contract to normalize raw MLWH sample payloads into the UI sample shape.
    - Regression:
        - Added Go enrichment regressions proving `WTSI_wEMB10524782` uses `ResolveSampleName` before broad scans and proving multi-sample library types use the bounded paged lookup.
        - Added a frontend contract regression for the raw MLWH sample shape emitted by study enrichment.
    - Live verification:
        - Rebuilt `.tmp/wa` and started a bounded seqmeta server. `/enrich/WTSI_wEMB10524782` returned 200 in 0.16s with 40 lanes; `/enrich/6568` returned 200 in 0.08s with 196 samples and 3 library details; `/enrich/Chromium single cell 3 prime v3` returned 200 in 0.67s with 200 samples, 17 study details, `partial=true`, and `samples_truncated`.
    - Verification:
        - Failed before fix: focused seqmeta Go enrichment test and focused frontend contract test.
        - Passed after fix: focused seqmeta/cmd regressions, full frontend contract/enrichment Vitest bundle, final `timeout 15m make test`, and final `timeout 10m make lint`.

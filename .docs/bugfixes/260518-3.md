- [x] The seqmeta details related data entity display is inconsistent. Eg. looking at the details of a `run_id`, a related sample shows with its name / id as a title, then this info is duplicated undeneath as key: value pairs. Library and study don’t get a title. Make all entities show the id as the title, with other key value pairs beneath (no duplicates), similar to how related sample currently shows on `run_id`. Also make entities display consistently when viewed from any context (seqmeta details page for any `seqmeta_*` value); actually use the same code and get rid of code duplication as much as possible.
    - Related Study, Sample, Library, and Lane rows now share one row renderer that shows the entity identity as the title and compact key/value metadata beneath it.
    - The shared metadata builders remove any title identity duplicates, so rows no longer show values such as `id: 7607`, `id: 71046409`, or `name: 7607STDY14643771` underneath the matching title.
    - The same row shape is used for run, sample, study, library ID/type, and expanded JIT library-sample contexts while preserving copy, filter, and JIT sample expansion behavior.
    - Added frontend regressions covering the original run-id repro and cross-context sample, study, library, and lane related rows.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`, `frontend/tests/seqmeta-enrichment.test.ts`.
    - Verification: new focused regressions failed before the fix; focused seqmeta badge/enrichment tests and full frontend Vitest pass. Browser screenshot captured at `.tmp/agent/seqmeta-runid-related-entity-display-postfix.png` with DOM summary at `.tmp/agent/seqmeta-runid-related-entity-display-postfix.json`. Root `make test` was not run because it can exercise MLWH sync paths, which the caller explicitly disallowed.

- [x] `deleteSeqOpsTrackingPerSampleRows` executes one `tx.ExecContext` per deleted id, which means the DELETE statement is re-prepared for every row. When many rows are deleted (e.g., large snapshot shrink), this adds significant overhead. Prepare the DELETE once and reuse it (mirroring `updateSeqOpsTrackingPerSampleRows`).
    - PR #32 thread `PRRT_kwDORrF2pM6QHVnj`; comment `3564378443`; reviewed commit `8dc9da7fb8dcd5321460d3d4f51441bf2ac73949`.
    - Updated `mlwh/sync_platform_coverage.go` to prepare the indexed, binary-exact tracking DELETE once per transaction and reuse it across every deleted ID, while skipping preparation for an empty deletion set.
    - Added a regression in `mlwh/sync_a5_test.go` that requires two exact-match deletes to share one prepared statement.
    - Verification: reviewer PASS; focused regression passed with 5 assertions; 13 tracking tests passed with 98 assertions; `make lint`, all-package compile-only, `gofmt -d`, and `git diff --check` passed.

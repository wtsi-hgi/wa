- [x] `make test` should be runnable without worrying about syncing against real MLWH. If not already, make any such tests skip if a special documented env var isn’t set.
    - Live MLWH integration tests now require the explicit `WA_LIVE_MLWH_TESTS=1` opt-in before they read `WA_MLWH_DSN` or development dotenv MLWH credentials.
    - The real cold-sync performance test still additionally requires `MLWH_SYNC_PERF_TEST=1`, so `Client.Sync` against real MLWH cannot run during ordinary `make test`.
    - Documented the opt-ins in `README.md` and `DEVELOPING.md`.
    - Added regressions proving live credentials and cold-sync perf settings are ignored unless `WA_LIVE_MLWH_TESTS=1` is set.
    - While verifying root `make test`, fixed Playwright shared state where `seqmeta-result-detail-performance.spec.ts` registered a temporary result and left it visible to parallel dashboard/file-browser tests. The test now deletes its temporary result, and fixture-count assertions ignore that known transient row if they race with registration.
    - Updated the recent-registration sort-icon e2e expectation to include the default `Unique` column added in the previous bugfix.
    - Files touched: `mlwh/integration_test.go`, `README.md`, `DEVELOPING.md`, `frontend/e2e/*.spec.ts` fixture-count/performance cleanup files.
    - Verification: new MLWH gate regressions failed before the fix, then passed; `go test -tags netgo --count 1 ./mlwh`; focused Playwright dashboard/performance checks; `make test-e2e`; final `make test`; `golangci-lint run --fix ./...`; `make lint`; and `git diff --check` all passed.
- [x] The seqmeta details “direct metadata” section has a row that can duplicate the title of the seqmeta details popup. Eg. clicking on a seqmeta_libraryid value top open seqmeta details for the library shows a title of the library id, then under direct metadata there’s a row for seqmeta_libraryid. Don’t show the duplicate row (and don’t show the direct metadata section if this means there are now no rows), and put the copy and filter buttons next to the title value.
    - Direct metadata rows whose value matches the seqmeta details title are now suppressed uniformly, including primary identifier rows such as `seqmeta_libraryid` and `seqmeta_librarytype`.
    - The seqmeta details header now shows copy and filter actions next to the title value, preserving copy/filter behavior after duplicate primary rows are hidden. Title filters use the same logical search keys as the existing rows, such as `seqmeta_libraryid=71046409` for exact library IDs and `library=Custom` for library type values.
    - Direct Metadata is not rendered when the duplicate title row was the only direct row.
    - Added frontend regressions for the exact `seqmeta_libraryid=71046409` duplicate-row case and the empty-direct-section library-type case.
    - Post-fix browser evidence: `.tmp/agent/seqmeta-libraryid-direct-metadata-postfix.png` and `.tmp/agent/seqmeta-libraryid-direct-metadata-postfix.json`.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`, `frontend/tests/seqmeta-sample-details.test.ts`.
    - Verification: focused regression failed before the fix, then passed; focused seqmeta badge/enrichment/sample-details Vitest tests, the browser repro, full `make test`, full `make lint`, and `git diff --check` passed.
- [x] A result that was added with `wa results register --sample 7607STDY14643771` shows in the metadata section of “result detail” under the seqmeta_sampleid key, but when clicked on to open the seqmeta details popup, the title is subtitled “seqmeta_sampleid (sanger_sample_name)”. If sample id and sanger sample name are distinct columns in MLWH that can have different values, the title should just be for seqmeta_sampleid, and the direct metadata section should have a row for the sanger sample name, even if the name happens to equal the id. If “sample id” is not actually a thing in MLWH and it is our own alias for sanger_sample_name, don’t display our alias in the frontend, only use the MLWH official names for things. Likewise for seqmeta_studyid vs study_lims_id, and everything else. Be consistnet and singular. Take this opportunity to align ourselves correctly with MLWH if we’re not already; get rid of our aliases entirely if they don’t serve any purpose, not just in the frontend display but throughout the code and our own schemas. The aim is to avoid confusion and surprises. Someone who knows MLWH should be able to understand our code, our databases, and the the frontend without any confusion as to what relates to what.
    - Repo evidence showed the MLWH cache/schema and Go MLWH structs already use official names such as `name`, `sanger_sample_id`, `id_sample_lims`, `id_study_lims`, `pipeline_id_lims`, `library_id`, `id_library_lims`, and `id_run`; `seqmeta_sampleid`, `seqmeta_studyid`, `seqmeta_libraryid`, etc. are WA legacy result metadata keys.
    - New `wa results register` shorthand metadata now stores MLWH-shaped keys: `seqmeta_id_run`, `seqmeta_id_study_lims`, `seqmeta_name`, `seqmeta_pipeline_id_lims`, `seqmeta_library_id`, and `seqmeta_id_library_lims`.
    - Legacy result databases and URLs remain compatible: old keys still validate, search, enrich, and render, but the frontend displays/copies/filters them through the official MLWH-shaped keys. The reported subtitle is now just `seqmeta_name` for old `seqmeta_sampleid`, never `seqmeta_sampleid (sanger_sample_name)`.
    - Direct metadata rows now use MLWH-shaped keys, and distinct direct fields such as `seqmeta_sanger_sample_id` remain visible even when their value matches the title.
    - Shared frontend seqmeta key canonicalisation was added so result metadata display, badges, enrichment priority, dashboard suggestions, and filter links do not drift.
    - No MLWH cache schema change was needed, so no `wa mlwh sync` was run.
    - Files touched: `cmd/results.go`, `results/types.go`, `results/server.go`, frontend seqmeta metadata/filter components, seqmeta enrichment helpers, e2e selectors, and focused Go/frontend regressions.
    - Verification: focused Go and Vitest regressions failed before the fix, then passed; `go test -tags netgo --count 1 ./cmd -run 'TestResultsRegisterCommand|TestResultsSearchCommand'`; `go test -tags netgo --count 1 ./results`; focused Playwright repros; full `make test`; full `make lint`; `golangci-lint run --fix ./...`; and `git diff --check` all passed.
- [ ] Seqmeta details rows in the direct metadata section waste a line with a double title for things, such as “SANGER SAMPLE ID” being followed by `seqmeta_samplid`. Don’t display the following line. Instead have that info (which should be the real MLWH key prefixed by `seqmeta_`) show as explanatory pop-up text on hovering over the title.
- [ ] Seqmeta details for a sampleid is missing “sample_supplier_name” in the direct metadata section. If our cache doesn’t store this info, update our cache schema and re-run `wa mlwh sync`.

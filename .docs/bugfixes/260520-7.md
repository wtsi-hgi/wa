- [x] The preview height resizer icon/box on the bottom right of previews looks bad. I’d expect it to look more like angled lines on the corner of the preview, seamlessly integrated in to the rounded corner of the preview box, not a separate small rounded corner box with its own border. Is this a standard thing from shadcn/ui or tailwind? If not, try and use something standard. If so, look for a standard alternative or implement something new that looks better and more integrated. It’s possible the odd icon is in addition to something more correct underneath, but I can’t quite see it properly, so maybe something just needs to be removed to address this bug.
    - Updated `ResizablePreviewFrame` and preview surfaces so the resize grip is integrated into the visible preview corner, with angled lines instead of a separate bordered mini-box.
    - Added Playwright coverage proving the image preview is narrower than its frame and the grip aligns with the visible surface rather than the invisible wrapper.
    - Verified by implementor/reviewer with focused Playwright, frontend lint, frontend Vitest, and `make lint`.
- [x] Preview boxes in single preview mode have gained an odd looking vertical scroll bar (that doesn’t scroll because it’s more or less full height) with a height resizing box underneath. Remove this previews look the same in single as other view modes.
    - Made single-preview wrappers fill the resize frame so CSV/table previews do not create a detached inner vertical scroller inside the preview box.
    - Kept horizontal table overflow while suppressing vertical overflow in the shared CSV table wrapper, preserving truncation and enlarged-preview behavior.
    - Added Playwright coverage for the `nf-core/rnaseq` `reports/report.csv` single-preview case and verified the prior integrated resize-grip coverage still passes.

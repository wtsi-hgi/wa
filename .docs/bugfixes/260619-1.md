- [x] CI tests are failing for the PR. Use the bugfix skill to fix them. Make sure the full `make test` passes locally as well.
  - Fixed the SVG preview regression by probing SVG content type with `HEAD` before choosing whether to bypass inline body fetching.
  - Preserved URL-rendered SVG previews while allowing mislabelled `.svg` files to fetch inline content and use the returned MIME type.
  - Added regressions for result-detail preview selection, shared preview type helpers, and the file proxy `HEAD` response.

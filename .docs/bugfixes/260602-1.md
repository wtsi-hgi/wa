- [x] The “latest result sets” box has a title and “columns” drop-down. The “result sets” view of search results should also have this title and “columns” drop-down in exactly the same styling. The title can be “search results”.
    - Updated search result-set tables to render the same compact icon/title/Columns header as Latest result sets, titled `Search results`.
    - Kept Combined files as the default search mode with no result-set table/header underneath, while showing the header only after switching to Result sets.
    - Added unit and browser-rendered Playwright coverage for the matching title styling and Columns dropdown behavior.

- [x] The recent change to adding `*` buttons either side of the glob filter field seems to have broken using `*` in the field. With the left `*` button disabled, typing `*searchterm` should give the same result; currently it gives no results. Path `/lustre/scratch127/gengen/projects_v2/dual_tf/MORF_10/Multiome/sc_analysis_MORF_v2/sc_analysis_BTLNCK/qc_outputs/HAP1_MORF10_BTLNECK2_1_A1_BTLNCK/HAP1_MORF10_BTLNECK2_1_A1_BTLNCK_py_inputs/atac/barcodes.tsv.gz` is no longer found by filter `py_inputs*.tsv.gz`. Make it work again. This is not a regression that should have happened given prior tests that should have been there for this case. How did it break?
    - Fixed glob composition so typed wildcard syntax no longer bypasses the active left/right wildcard toggles; enabled toggles now add only the missing edge `*`s.
    - Added rendered file-browser regression coverage for mixed typed/toggle wildcards, including `py_inputs*.tsv.gz` matching the nested `_py_inputs/atac/barcodes.tsv.gz` path.
    - Updated Playwright persistence coverage so exact `**/*.tsv` behavior explicitly disables the trailing wildcard toggle and verifies the saved wildcard state.
    - Root cause: `applyFileBrowserGlobWildcards` returned the typed pattern unchanged whenever it contained any glob token, so the default leading `*` was skipped and the pattern stayed too anchored; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] The warning when skipping symlinks that point outside the directory (“warning: skipped 1 path(s) while scanning output files”) is not useful. It should list out the paths to the problematic symlinks and where they point.
    - Added structured scanner warnings for skipped directory symlinks that escape the output root, carrying both the symlink path and resolved target while keeping the existing count-based scanner wrapper compatible.
    - Updated `results register` and `results rescan` warning output to list each skipped symlink and where it points instead of only reporting a count.
    - Added scanner and CLI regressions for escaped symlink details; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] The recently implemented multichannel (ome) tiff file preview doesn’t seem to work. I just get “preview unavailable Unable to read Tiff metadata Unable to load Tiff metadata”. This is when using the exact same tiff files as are in `.tmp`. Make it actually work. Where are the webp single plane derivaitons stored? Are they produced on-the-fly by the server, or created and stored somewhere at register time? Is the former going to be fast enough in a scenario of showing 10s of tiffs at once?
    - Fixed OME-TIFF metadata and plane requests to authorize with backend `HEAD` plus `download=true`, bypassing normal preview byte caps without downloading large TIFF bodies through the Go backend.
    - Added proxy-route regressions covering metadata, plane rendering, stale-JWT public fallback, byte-limit bypass, and no backend `GET` body fallback when OME `HEAD` is unsupported.
    - Verified via scratch repro that `.tmp/ome.tiff` and `.tmp/ome_collapsed.tiff` now return metadata and render first-plane WebPs through the route; the large `.tmp` fixtures remain untracked.
    - WebP single-plane derivatives are generated on demand by the Next server via Sharp and returned from memory; they are not created at register time or stored in a persistent server-side cache. Cold renders around 400 ms on the provided fixtures are fine for one preview but likely too expensive for tens of TIFFs at once without persistent thumbnail caching or precomputed common planes; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.

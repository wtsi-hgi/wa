- [x] Filtering for real Saga-backed seqmeta integration was implemented incorrectly, so the live integration tests are bypassed or broken instead of verifying the real MLWH contract.
	- Fixed the MLWH samples filter wire contract in saga/mlwh.go to use the live Saga filter keys, list shape, and double URL encoding required by the backend.
	- Added regression coverage for the exact outgoing filter payloads and tightened the live Saga probe and seqmeta matrix tests to exercise the corrected contract.
- [ ] Coverage is missing for the main seqmeta enrichment purpose: associating arbitrary sequencing metadata identifiers with a result set and resolving the identifier plus related metadata, for example sample name to sample id, library, and study.

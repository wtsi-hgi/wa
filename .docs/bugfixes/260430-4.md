- [x] Preview graphics in file browser are in a rounded corner container with a border that has padding or margin, which is in another rounded corner container with a border that has larger padding or margin. Get rid of the unecessary containers so the previews display as large as possible without any wasted padding/margin. In “1 preview per row” mode there is also a redundant byte size in one of the parent containers, which should be removed.
    - Removed extra bordered/padded preview wrappers from the file browser grid and image preview thumbnail.
    - Removed the duplicate grid-mode byte-size label from the thumbnail wrapper.
    - Added Vitest and browser-rendered Playwright regression coverage for preview chrome and duplicate size text.
- [x] In the Result Metadata section, seqmenta\_\* values are clickable, but show no indication of this. Change the mouse pointer on hover.
    - Added pointer cursor affordance to clickable seqmeta metadata badge triggers.
    - Added Result Metadata regression coverage for the clickable seqmeta hover affordance.
- [x] In single preview mode of the file browser, when there are many rows, the preview scrolls out of view when you click on lower rows. The preview container should either gracefully be “stuck” to the top of the viewport so it is always visble while file rows are visible, if such a thing is possible. Otherwise the preview container should move down to be aligned with either the top or bottom of a file row when it is clicked on, depending on if we are at the top or bottom of the list of file rows.
    - Made the single-preview panel sticky to the viewport top while file rows scroll.
    - Added component and Playwright scroll regression coverage for lower-row selection.
- [x] In the file browser, when viewing a folder with more than 100 files, there is an indication at the top of the file browser this is “Page 1 of x”, but there is no way of changing the page. Move the toggle for for single preview vs “1 preview per row” and the page number indicator to something that appears on the folder row if a folder has files to preview and has been expanded, and add a mechanism to decrement, increment or directly choose the page to view, replicated at the bottom of the file listing.
    - Moved the preview-mode toggle and page status out of the browser header and onto the expanded selected folder row.
    - Added Previous/Next icon buttons and a direct page selector, with page controls mirrored below the file listing.
    - Added Vitest regression coverage for >100-file pagination, expanded-folder controls, direct page selection, Previous/Next, and mirrored bottom controls.
- [x] In the file browser, previews for image files can be clicked on to make them fill the screen, but not other previewable file formats. Make them all enlargable on click. The enlarged version of eg. csv file would show more rows from the file. Interactive formats such as csv where you can click to sort columns should only become interactable in that way once enlarged, allowing the click-anywhere-to-enlarge apprach of images to apply to all previewable formats.
    - Added a shared enlarged preview dialog for non-image previewable formats while preserving the existing image lightbox behavior.
    - CSV compact previews are display-only and limited to the first 100 rows; enlarged CSV previews show all rows and enable filtering/sorting.
    - Added Vitest coverage for CSV enlarge, expanded row count, compact non-interactivity, enlarged sorting, enlarged filtering, and image lightbox preservation.
    - Added parameterized Vitest coverage that markdown, HTML, SVG, PDF, text, and code previews all open the enlarged dialog.
    - Updated Playwright coverage so the browser flow verifies CSV controls appear only after enlargement.
- [x] The "wtsi/amplicon” example from `make run` fixtures has a seqmeta_library of “Chromium single cell”, but clicking on that shows seqmeta_library repeated 4 more times with different values. I don’t know what this means. Is it something wrong with the fixture, or the lookup? A library shouldn’t be related to other libraries.
    - Fixed frontend seqmeta enrichment caching so library types are looked up independently instead of being primed from unrelated study/sample graphs.
    - Suppressed sibling library values in direct `seqmeta_library` popup details.
    - Added regression coverage for the amplicon `Chromium single cell` path and library-specific popup display.
- [x] The different result sets from `make run` fixtures should be constructed to demonstrate what happens when you set a single different kind of seqmeta value on the result. One should have only a real saga library identifier, one should have a real sample, one should have a rela study. Clicking on the values will then let us see how metadata and relationship look ups work. In the pop-ups, it also needs to be clearers what is additonal metadata about the thing in question (eg. other identifiers for samples, descriptions for studies), and what is related data (eg. the study a sample came from).
    - Updated seed.json fixtures to demonstrate distinct single seqmeta kinds: study-only (6568), sample-only (WTSI_wEMB10524782), and library-only (Chromium single cell 3 prime v3).
    - Added seqmeta-badge field grouping with "Direct Metadata" and "Related Data" section headers in the popup dialog.
    - Direct metadata includes properties of the clicked entity (e.g., sample identifiers, study description), while related data shows linked entities (e.g., study a sample belongs to).
    - Added Go test coverage for fixture structure validation ensuring each fixture demonstrates a single seqmeta kind.
    - Added frontend test coverage for popup field grouping with appropriate section headers and field categorization.

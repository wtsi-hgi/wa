- [x] Viewing a results page gives node error “Hydration failed because the server rendered text didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used”
    - Fixed the initial seqmeta cache hydration path so client-only cookie state is merged after hydration instead of changing first-render markup.
    - Added a regression hydration test covering a stale cached unavailable marker and verified repo-wide `make lint` and `make test` pass.

        ```
        components/seqmeta-badge.tsx (875:21) @ SeqmetaBadge

          873 |
          874 |                 {error === "not_found" ? (
        > 875 |                     <span
              |                     ^
          876 |                         aria-label="enrichment unavailable"
          877 |                         className="inline-flex h-5 w-5 items-center justify-center rounde...
          878 |                     >
        ```

- [x] File preview truncation is broken following recent commits and attempts in prior 2 bugfixes md files to improve this. Now all 81979 rows of a large tsv are shown in the file browser, ignoring the preview height, limits on how much data to send to the browser, and pagination of large tables at 1000 lines when enlarged.
    - Restored line-limited readable preview requests from the frontend so large CSV/TSV payloads no longer stream the full table into the browser by default.
    - Limited inline CSV/TSV row rendering only when the backend marks the preview as truncated, which restores preview-height clipping without regressing complete small-file previews.
    - Added regressions for the restored `line_limit` contract and for truncated inline table previews that still paginate enlarged previews at 1000 rows per page.

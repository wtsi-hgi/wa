- [x] Switching between “combined files” and “Result sets” views shows the box underneath the buttons is a different spacing from the buttons. Make the spacing consistent for both views and equal to the spacing above the buttons.
    - Matched the Combined files and Result sets gaps below the display toggle to the 24px gap above the toggle.
    - Added browser-rendered Playwright coverage measuring the toggle-to-content gaps in both modes.
- [ ] Switching between “combined files” and “Result sets” views shows the title icon is a different horizontal spacing from the left edge of the box beneath the buttons, and possibly the box has different corner radius. Make these consistent. The search box at the top of the page should also have its search icon and title be consistent with the boxes that appear under the buttons. Currnetly it looks like there’s too much padding/margin above the search title.

- [x] I can do:
    ```
    make dev &
    export WA_ENV=development
    .tmp/wa results register [...]
    ```
    That works fine, and I can access the web UI as well.
    But given this dev server, how can another user (a beta tester) register results with it?
    I thought of:
    ```
    export PATH=/path/to/my/checkout/.tmp:$PATH
    export WA_ENV=development
    ```
    But they don’t have, and shouldn’t have, my `.env.development.local` file. Then I thought maybe I could tell them to export just the WA_DEV\* env vars to get the right ports. Is that all they need?
    However, they will be on a different machine in the cluster. Am I forced to tell them to specify the `--server` option when using register? If so, use the bugfix skill workflow to allow the server host to also default to an appropriate env var (for both dev and prod), and improve all documentation to more clearly explain what normal users need to be able to use wa servers.
    - Made `WA_RESULTS_SERVER_URL` the single client-facing results CLI default, equivalent to `--server`, with precedence before the existing `WA_RESULTS_BACKEND_URL` compatibility fallback and before loopback scenario-port defaults.
    - Changed `WA_DEV_RESULTS_HOST` and `WA_PROD_RESULTS_HOST` to mean server bind hosts only. `results serve` now combines the active scenario bind host and `WA_*_RESULTS_PORT` when `--url` and explicit `--port` are absent, so operators can bind `0.0.0.0:<port>` without duplicating the port in another env var.
    - Added regressions for client URL precedence, avoiding `0.0.0.0` as a client fallback, server bind host/port defaults, explicit `--url`/`--port` overrides, production env isolation, and run-dev guards.
    - Updated README, DEVELOPING, scenario env files, frontend env example, Makefile, run-dev, and root env validation docs/guards so normal users and operators have separate, clear setup paths.
    - Verification: focused `go test -tags netgo --count 1 ./cmd`, `make lint`, and full `make test` passed during implementation. Final review passed; its full `make test` run hit the known `logout-delay-repro.spec.ts` suite-context flake once, and the exact spec passed on rerun.

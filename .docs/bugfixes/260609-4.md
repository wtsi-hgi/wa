- [x] Now I get `Error: Post "https://farm22-wrstat01:3671/rest/v1/jwt": tls: failed to verify certificate: x509: certificate signed by unknown authority`. Fix that too using the bugfix skill workflow. The `--cert` option was already supposed to enable trust of the given or WA_RESULTS_SERVER_CERT wasn’t it?
    - Fixed authenticated results clients so an explicit `--cert` still wins, but a blank cert argument falls back to `WA_RESULTS_SERVER_CERT` before constructing the go-authserver client used for `/rest/v1/jwt`.
    - Added an HTTPS auth/register regression proving `wa results register` can log in and post when the server certificate is trusted only via `WA_RESULTS_SERVER_CERT`.
    - Preserved TLS verification, hostname verification, owner-login behavior, and auth token permission checks.
    - Verification: focused auth/results Go tests, full `timeout 20m make test`, `timeout 10m make lint`, and `git diff --check` all passed in review.

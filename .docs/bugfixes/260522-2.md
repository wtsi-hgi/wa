- [x] `make dev` fails because of the relative path defaults for the pem paths. Is it fixable without having to specify absolute paths in the .env.development file?

    ```
    Starting results server on https://127.0.0.1:3672 (mode=dev)
    Skipping fixture seed (mode=dev, --fixtures not requested)
    Starting seqmeta server on http://127.0.0.1:3673
    Waiting for seqmeta studies readiness at http://127.0.0.1:3673/studies (MLWH cache: hgi_wf_test_admin@tcp(hgi-workflow-automation-test-db.internal.sanger.ac.uk:3324)/workflow_automation_mlwh_sb10;?parseTime=true; a cold cache can take a while on first run)
    Starting frontend dev server on https://127.0.0.1:3671
    Timed out waiting for frontend health at https://127.0.0.1:3671/api/health
    frontend health process status:
    2351272       00:31 SN    0.0  0.0 /bin/sh /software/hgi/installs/pnpm/pnpm dev --port 3671 --experimental-https --experimental-https-key .tmp/wa-dev-key.pem --experim
    Last 40 lines from /nfs/hgi/wa/repo/logs/frontend.log:

    [dev]

    > wa-results-web@ dev /nfs/hgi/wa/repo/frontend
    > next dev --port 3671 --experimental-https --experimental-https-key .tmp/wa-dev-key.pem --experimental-https-cert .tmp/wa-dev-cert.pem

    ⚠ Self-signed certificates are currently an experimental feature, use with caution.
    Unhandled Rejection: Error: ENOENT: no such file or directory, open '/nfs/hgi/wa/repo/frontend/.tmp/wa-dev-key.pem'
        at ignore-listed frames {
      errno: -2,
      code: 'ENOENT',
      syscall: 'open',
      path: '/nfs/hgi/wa/repo/frontend/.tmp/wa-dev-key.pem'
    }
    make: *** [Makefile:88: dev] Error 1
    ```

    - Changed `run-dev.sh` to resolve `WA_RESULTS_SERVER_CERT` and `WA_RESULTS_SERVER_KEY` relative to the repo root before creating certificates, exporting child env, running health probes, or constructing the default `pnpm dev` command.
    - Added a regression to the default frontend dev command test that sets the same relative `.tmp/wa-dev-*.pem` env values from `.env.development` and asserts Next receives repo-absolute PEM paths.
    - Documented that `run-dev.sh` accepts relative TLS paths in the env files and anchors them at the repo root for child processes.

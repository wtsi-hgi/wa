- [x] `make dev` on real data on another machine has messed up web UI compared to `make dev-fixtures`. Try to replicate the issue where permanent search fields render half box wide with too-small “+” buttons (2 fields per row instead of all 5 fitting on 1 line), and “latest result sets” scrolls horizontally with long output directory paths instead of wrapping the paths with no horizontal scroll. Once replicated, fix.
    - Added a Playwright regression in `frontend/e2e/real-data-layout-repro.spec.ts` that registers a long real-data-like output directory and captures browser metrics/screenshots for the reported layout failure.
    - Changed the permanent search-field grid in `frontend/components/filter-builder.tsx` to fit all five fields at the reproduced 1000px viewport with compact 40px add buttons.
    - Changed `frontend/components/results-table.tsx` and `frontend/components/results-columns.tsx` so latest-result rows use a fixed-width table and long output-directory paths wrap in-cell without horizontal overflow.
    - Verified red/green with the source fix temporarily reversed, plus focused Playwright/Vitest checks, `pnpm lint`, `pnpm test`, `make lint`, and `make test`; no `wa mlwh sync` was run.

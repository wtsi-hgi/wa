- [ ] `make test` is not passing. Fix tests properly without skipping or removing unless they are definitely in error/outdated.
    - Could not reproduce in the current workspace: the implementor ran `timeout 20m make test` successfully end to end (Go packages, Vitest 38 files / 406 tests, Playwright 47 passed / 2 pre-existing skipped) and `timeout 20m make lint` successfully.
    - No tests were skipped, removed, weakened, or edited, and no `wa mlwh sync` was run.
- [x] The recent changes to the preview height changer did follow the instructions (see previous bugfixes md file), but actually it looks bad because the diagonal line area is too big and the rounded corner with the lines looks ugly. Instead of clipping to the rounded corner, make the bottom right corner of preview boxes right-angled and put a smaller borderless box of diagonal lines there. Size should be on par with the other icons (download and enlarge).
    - Squared the bottom-right corner of resizable preview surfaces and changed the visible resize grip to a compact, borderless diagonal-line patch inside an icon-sized drag target.
    - Added browser-rendered Playwright coverage that fails on the old rounded/clipped corner and oversized grip while preserving the global drag-to-resize behavior and single-preview scrollbar regression.
    - Post-fix evidence: `.tmp/agent/preview-resizer-square-corner-image-post-fix.png` and `.tmp/agent/preview-resizer-integrated-corner-csv-post-fix.png`.
    - Verification: reviewer confirmed `timeout 20m make lint` and `timeout 20m make test` passed. No `wa mlwh sync` was run.
- [x] In the file browser, the “preview modes” and “File types” tools on folders is not getting hidden on parents when their subfolders are opened. On nested sub fodlers these also appear undereath the folder name instead of to the right, even when there is plenty of horizontal space.
    - Hid ancestor folder controls while any descendant folder is expanded, while preserving the prior behavior where the parent controls reappear after the child closes.
    - Allowed deeply nested folder headings to use the same desktop name/control grid as shallower rows, so `Preview modes` and `File types` sit to the right when there is horizontal room.
    - Added Vitest and Playwright regressions for ancestor-control hiding/restoration and nested right-side layout; restored an unrelated dashboard exact-count assertion after review caught an accidental weakening.
    - Post-fix evidence: `.tmp/agent/bug3-nested-controls-hidden-parent-right-aligned-postfix.png`.
    - Verification: reviewer confirmed `timeout 20m make lint` and `timeout 20m make test` passed with Vitest 407 passed and Playwright 47 passed / 2 pre-existing skipped. No `wa mlwh sync` was run.

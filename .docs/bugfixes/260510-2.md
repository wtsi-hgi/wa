- [x] `make dev-fixtures` uses real mlwh study, library and study examples, but the
      library and study result metadata fails to resolve in the frontend (eg. "No
      enrichment matched this sanger sample id value."), and the seqmeta details for
      study 6568 only has 2 bits of direct metadata (name and accession) and no
      related metadata (library and samples in the study). Get the mlwh lookups to
      work properly and quickly (timed after doing a sync).
    - Fixed MLWH-backed seqmeta enrichment for library-type and study lookups by
      rebuilding related study, library, and sample detail graphs from matched MLWH
      data and by teaching the CLI/client adapters to resolve library-type samples
      through direct and cached fallback paths.
    - Added regression coverage across `cmd`, `mlwh`, `seqmeta`, and frontend
      hydration/enrichment tests so stale negative cache entries, study-detail
      hydration, and library-type lookup drift are exercised explicitly.
    - Updated `run-dev.sh` and `cmd/run_dev_test.go` so `make dev-fixtures` reuses
      already-healthy dev services on configured ports instead of failing on
      duplicate startup, and verified the full gate with `make lint`, `make test`,
      and `make dev-fixtures`.

- [x] In the file browser, it looks like there is a stray bottom left corner
                  border visible of an otherwise invisible box underneath the bottom left corner
                  of expanded folders. Stop displaying this.
            - Reproduced the artifact on the seeded `nf-core/rnaseq` result detail page
                  with the `qc/images` folders expanded and captured the pre-fix screenshot
                  before changing the file-browser shell styling.
            - Removed only the decorative grouped-shell bottom-left radius that was
                  leaking a visible corner under expanded folders, preserving the existing
                  grouped borders, spacing, and nested folder behavior.
            - Added a focused Playwright regression in
                  `frontend/e2e/file-browser-layout.spec.ts` that asserts the connector elbow
                  no longer renders a rounded bottom-left corner, and revalidated with `make
                  lint`, `make test`, and `make dev-fixtures`.

- [x] The File types popup has a feature where any click outside the popup will
      close the popup. Apply the same idea to the Preview modes popup.
            - Mirrored the File types outside-click behavior in the Preview modes popup
                  by tracking its open state explicitly and only attaching the dismiss handler
                  while the popup is open.
            - Consumed dismiss clicks that land on the underlying folder toggle so the
                  popup closes without collapsing the folder, while unrelated outside clicks
                  still propagate normally.
            - Added unit and Playwright regressions covering both behaviors and reran
                  `make lint`, `make test`, and `make dev-fixtures`.

- [ ] Selecting both subfolder previews and 1 per row options on a folder, then
      opening a subfolder seems to mess up the options in the preview modes
      selectors. The newley opened subfolder says "1 per row" in the selector, even
      though it is not in that mode, and the initial folder loses the ability to
      turn off 1 per row until you close the subfolder. Make sure that the selector
      shows the correct current state and allows selecting or deseleting the correct
      options at all times under all possible scenarios.

- [x] In seqmeta details for a seqmeta_library, clicking a Copy button changes all Copy buttons in a section to Copied, not just clicked. Also clicking Study row copies filter study id rather than visible study name. Clipboard value must be one of visible names shown on row. Filter button behavior unchanged.
	- Updated copy-state tracking in `frontend/components/seqmeta-badge.tsx` to key by row value so only the clicked row shows `Copied`.
	- Changed study-row copy action to copy the visible study name while keeping existing `study_id` filter link behavior.
	- Added regression coverage in `frontend/tests/seqmeta-badge.test.ts` for both row-scoped copy state and study-name clipboard behavior.
- [x] For seqmeta details of study 6568 from make run fixture (nf-core/rnaseq result set), related data section sometimes initially shows bad 4-row sample metadata + library type; after refresh it correctly shows Libraries section. Remove code generating initial bad display.
	- Removed legacy flat sample/library field generation for study metadata in `frontend/components/seqmeta-badge.tsx` so stale responses no longer render misleading 4-row related data.
	- Added regression test in `frontend/tests/seqmeta-badge.test.ts` to ensure study metadata without `study_detail` does not emit those legacy rows.
- [x] For make run fixtures, search builder Study field with value EGAS00001005445 returns no results; previous bugfix also failed to combine Library with Seqmeta Library; previous bugfix broke sample seqmeta detail filter links where related study emits non-working Study ID field. Filters should use combined new field names (Study, etc.).
	- Added combined `library` handling in results search and frontend search builder so `library` and `seqmeta_library` are treated as one logical field.
	- Updated seqmeta detail filter links to emit combined query keys (`study`, `sample`, `library`) while keeping lane filtering on `seqmeta_lane`.
	- Added regression coverage in `results/server_test.go`, `frontend/tests/filter-builder.test.ts`, and `frontend/tests/seqmeta-badge.test.ts` for combined key behavior and fixed filter links.
- [x] Search builder must do hierarchical searching: Study search includes result sets with library/sample/lane metadata from that study; Library search includes samples/lanes from that library; Sample search includes lanes for that sample. Example search Study=6568 should include nf-core/sarek via sample relation as well as nf-core/rnaseq. Ensure backend seqmeta/saga API queries are efficient and cached so searches are under 1 second.
	- Extended `results/server.go` resolver/search flow to union direct metadata hits with resolver-derived sample and lane IDs for study, library, and sample searches.
	- Added short-lived in-memory resolver caching in `results/server.go` to avoid duplicate upstream seqmeta calls on repeated queries.
	- Added regressions in `results/server_test.go` for library hierarchical expansion, sample-to-lane expansion, and resolver cache reuse.

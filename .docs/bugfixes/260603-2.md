- [x] That made things worse. Now the search fields display 2 per row in `make dev-fixtures` as well. The "+" symbols moved a bit, but isn't correctly centered in either now.
    - Added a Playwright regression in `frontend/e2e/search-layout-repro.spec.ts` for the seeded dashboard at 760px, 767px, and 1000px. It failed before the fix with `2/2/1` search-field rows plus 0.5px icon and 1px button center offsets.
    - Changed `frontend/components/filter-builder.tsx` so the permanent search controls use compact auto-fit columns when the grid is wide enough, and adjusted the plus button divider/vertical span so the icon and button are browser-measured centered.
    - Extended `frontend/e2e/real-data-layout-repro.spec.ts` to assert plus centering while preserving the 1000px long-output-directory checks for wrapped paths and no Latest result sets horizontal scroll.
    - Evidence: `.tmp/agent/search-layout-dev-fixtures-postfix-760.png`, `.tmp/agent/search-layout-dev-fixtures-postfix-767.png`, `.tmp/agent/search-layout-dev-fixtures-postfix-1000.png`, `.tmp/agent/search-layout-dev-fixtures-postfix.json`, and `.tmp/agent/real-data-layout-repro-current.png` / `.json`.
    - Verification passed: focused seeded-dashboard Playwright before/after, focused real-data long-path Playwright, focused existing results UI Playwright checks, focused filter-builder/dashboard Vitest, full frontend Vitest, `pnpm lint`, `make lint`, and `make test`. No `wa mlwh sync` was run.

- [x] The login pop up shouldn’t shift content down, it should overlay like the logout popup.
    - Changed `frontend/components/auth-menu.tsx` so the logged-out auth menu remains in the normal-flow auth row, while the open login form is absolutely positioned below the login button as an overlay.
    - Added `frontend/e2e/login-popup-overlay-repro.spec.ts` Playwright coverage that opens a logged-out protected result page and verifies the login form does not change auth bar height, main content position, locked section position, or scroll.
    - Captured before/after evidence under `.tmp/agent/`; post-fix geometry reported zero auth bar, main, and locked-section shift.
    - Reopened because clicking `Log in` can still move elements on the page; the regression must cover the visible page more broadly, not only the locked result detail section.
    - Resolved the reopened issue by keeping username focus but calling `focus({ preventScroll: true })`, so short viewports no longer scroll the page when the overlay opens. Added short-viewport Playwright coverage for the login button, auth bar, back/header action, and main content, with verified zero geometry shift in `.tmp/agent/login-popup-overlay-short-viewport-postfix.json`.
    - Added the clarified logged-out dashboard regression for `/` with `[data-search-builder="true"]` visible. It passes against the current uncommitted state with zero shift for the auth bar, header actions, login button, main content, search builder, result heading, and scroll position; evidence is in `.tmp/agent/login-popup-overlay-dashboard-current.json` and `.tmp/agent/login-popup-overlay-dashboard-current.png`.

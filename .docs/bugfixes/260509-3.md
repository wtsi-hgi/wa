- [x] As the preview height widget was recently combined to work in all 3 file browser modes (single preview, 1 preview per row, subfolder previews), make the file types widget apply to all modes as well, and change its default to all previewable file types. Combine the “subfolder previews” and “1 preview per row” selectors in to a single space-saving widget that lets you select none, one or both modes.
    - Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts, frontend/e2e/file-browser-layout.spec.ts, frontend/e2e/results.spec.ts
    - Approach: routed direct single-preview and grid-preview rendering through the same per-folder file-type selection used by subfolder galleries, defaulted that selection to all previewable kinds, kept full file lists visible while filtering only preview surfaces, and replaced the separate grid/subfolder checkboxes with one compact Preview modes disclosure that can enable neither, either, or both modes.
- [ ] Selecting “subfolder previews” mode creates new rows beneath for each subdir with previews in it, but does not remove the original display of the subfolder rows, which remain beneath and expandable. Instead of duplicating the rows, the original subfolder rows, with their expandability functionality intact, should simply gain the previews within themselves. The previews would disappear if the subfolder was clicked on to expand it.
- [ ] When the preview height is large enough that all the lines of a text-based file format (log, md, txt) preview are visible, the last line of the preview is still shown greyed out, as if to indicate truncation. Meanwhile for csv and maybe tsv, the table can get truncated at low preview height but not show a greyed out last line. Fix the greying out truncation indicator to only and always appear when a preview isn’t showing all the content, and be consistent for all file formats.
- [ ] The root folder in the file browser does not start with `/`. Make it so.
- [ ] To save horizontal space and have consistent display of files and previews, file listings under an expanded folder are left aligned, unlike their parent folder name, which might be indented because it was itself a nested folder under root. The seperate rows for the file listing are then followed by sub folder listings which are indented, and then by sibling folders and then siblings of parent folders. While indentation of folders may vary, the styling of the rows is all the same and they all seem independent. Try and do something, without affecting the use of full horizontal space for file listings, to better visually indicate folder groupings and heirachy. It should be clear that a block of file and subfolder rows belong to a particular expanded folder. Eg. at the very least expanding a folder should show its contents within its own expanded box. Sacrificing a small amount of horizontal space to include file rows within the borders of its parent folder box might be acceptable, but I suspect that with deep enough nesting all the padding and borders would add up and give too little space for the previews. So file rows could still look like they belong to their parent folder by being on top of their parent folder’s coloured background box, but just break the side walls in a clear and stylisticly deliberate way to use full horizonal space.

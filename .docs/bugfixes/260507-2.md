- [x] Opening a results page gives node error "Hydration failed because the server rendered text didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used: […] +aria-label="enrichment unavailable" [..] - aria-label="loading enrichment""
    - Root cause: `ResultMetadataEnrichment` read `SeqmetaCacheContext` directly during render; `not_found` cookie entries hydrated on client made the first client render diverge from SSR.
    - Fix: gate cache reads behind a hydration-safe mounted flag via `useSyncExternalStore` (server snapshot returns `false`), using a module-scoped `EMPTY_CACHE` until mounted; effect also gated.
    - Files: `frontend/components/result-metadata-enrichment.tsx`, regression tests added in `frontend/tests/seqmeta-badge.test.ts`.
- [x] File preview truncation is broken following recent commits and attempts in prior 4 bugfixes md files to improve this. All 81979 rows of a large tsv are still shown in the file browser, ignoring the preview height and limits on how much data to send to the browser. Clicking to enlarge the preview correctly shows pagination, but the file browser preview should not have been given all the rows to render. Only enough rows to fill the preview height (or the maximum possible preview height, with height slider adjustements then not having to trigger fresh data requests). Learn from the mistakes of prior attempts at fixing this. How can this be so difficult? The backend just shouldn't send more than approx 20 lines of data when sending file browser previews, obey its max size limits and truncate when sending the data for enlarged previews, and send whole files for the download file button.
    - Root cause: a single backend preview path served all surfaces; prior fixes patched only the frontend and could be overridden by a client-supplied `line_limit`.
    - Fix: introduce `mode` query param with three server-enforced modes — `inline` (hard cap `MaxInlinePreviewLines=20`, ignores larger client `line_limit`), `enlarged` (byte-cap only, paginated client-side), `download` (full file, attachment).
    - Frontend inline preview requests `mode=inline`; height-slider changes no longer trigger refetches (excluded from effect deps); enlarge fetches `mode=enlarged` once on open; download untouched.
    - Files: `results/server_file.go`, `results/server_file_test.go`, `frontend/app/api/file/route.ts`, `frontend/components/file-preview.tsx`, `frontend/components/result-detail-files.tsx`, `frontend/tests/result-detail-files.test.ts`, `frontend/e2e/results.spec.ts`, `frontend/e2e/file-preview-truncation.spec.ts`.
- [x] `wa mlwh sync --env development` fails with “Error: mlwh: query sample sync source: Error 1054 (42S22): Unknown column 'id_study_lims' in 'field list’”. This seems very concerning. How did any of the implementation or tests or reviews of the .docs/mlwh/spec.md pass if the first basic and most fundamental thing I tried fails? Do a complete audit and ensure we have proper integration tests using real mlwh, and ensure this command and all other fundamentals actually work.
  - Root cause: upstream queries selected `sample.id_study_lims`, but real MLWH `sample` has no such column; the link lives on `iseq_flowcell`. Tests passed because go-sqlmock doesn't validate SQL and the cache `sample_mirror` table deliberately has the column.
  - Fix: `mlwh/resolver.go` introduces `sampleStudyLimsSubquery` (correlated `COALESCE((SELECT iseq_flowcell.id_study_lims ... LIMIT 1), '') AS id_study_lims`), used by `sampleSelectColumns` and `mlwh/sync.go` syncSampleTable.
  - Added `mlwh/sync_real_schema_test.go` with two regression tests that run sync/resolve against a sqlite schema where `sample` has no `id_study_lims`; they would fail without the fix.
  - Audited all upstream MLWH SQL in mlwh/: hierarchy.go, all_studies.go, resolver.go run/iseq paths — no other real-vs-mock schema mismatches.
  - Files: mlwh/resolver.go, mlwh/sync.go, mlwh/resolver_sample_test.go, mlwh/sync_test.go, mlwh/sync_real_schema_test.go.
- [x] `wa mlwh sync -h` doesn’t explain anything about the database we’re syncing from or to or the various env vars it can use. Improve the help text of this command and all other subcommands so the long help text actually explains the command, why and when it would be used, how to configure it, examples of usage etc.
  - Added detailed `Long` help to `wa mlwh` and `wa mlwh sync` covering purpose, when to use, all configuration env vars (`WA_MLWH_DSN`, `WA_MLWH_PASSWORD`, `WA_MLWH_CACHE_PATH`, `WA_MLWH_CACHE_PASSWORD`, `WA_ENV`), the `--env` flag, and example invocations.
  - Added `TestMLWHCommandsHaveDescriptiveLongHelp` and `TestMLWHSyncHelpRendersConfigurationDetails` in cmd/mlwh_test.go that recursively assert every mlwh subcommand has substantive Long help and that rendered `--help` mentions each env var.
  - Files: cmd/mlwh.go, cmd/mlwh_test.go.
- [ ] I think the removed (since branching from the results-rest branch)`wa saga` command had a simple thing of supplying an identifier and it would search and give you the info it had on it. Add one or more `wa mlwh` subcommands to expose some straight-forward high-impact most-useful features of the mlwh pkg on the CLI, such as the removed saga command functionality of just being able to see all the info we have for a given identifier.

- [x] `wa mlwh sync` fails against the real MLWH source (mlwh/sync.go): (1) seq_ops_tracking_per_sample lives in schema mlwh_reporting, not mlwarehouse (Table doesn't exist); (2) useq_run_metrics has no id_useq_run_metrics_tmp (real PK id_run); (3) eseq_run_lane_metrics has no id_eseq_rlm_tmp (real PK id_run,lane); (4) eseq_run has no run_status column (has outcome/date_started/date_completed); (5) seq_product_irods_locations.irods_data_relative_path is nullable -> NULL scan error. Fix the source queries/scans, align the hermetic test fixtures' source schema with reality, and add an integration test that skips when the real MLWH source DB is unavailable and validates that the source schema the other tests assume is correct. Wire `make test` to pick up the MLWH source DB details from .env.development.local so that integration test runs locally (and skips where those details are absent/unreachable).
    - mlwh/sync.go, mlwh/sync_platform_coverage.go: corrected all 5 source queries/scans against the real schema, plus a 6th issue (nullable context columns on the reporting table -> COALESCE): mlwh_reporting-qualified tracking table; useq_run_metrics keyed on id_run; eseq_run_lane_metrics keyed on (id_run, lane); eseq_run uses outcome/date_started/date_completed (no run_status); COALESCE for nullable irods_data_relative_path.
    - mlwh/cache_schema/{sqlite,mysql}/{eseq_run_lane_metrics,useq_run_metrics}\_mirror.sql: mirror PKs/columns corrected to match the real source (mirror-shape drift auto-migrates at the same CacheSchemaVersion).
    - Integration test mlwh/sync_source_integration_test.go (TestSyncSourceSchemaMatchesRealMLWH): skips when WA_MLWH_DSN is unset/unreachable; otherwise prepares EVERY sync source query (all supported tables, all watermark variants) against the real DB, so a missing column/table/wrong schema fails. Proven to fail on HEAD's buggy queries.
    - mlwh/sync_real_schema_test.go: aligned the hermetic fixture source schema with reality + behavioural NULL-irods / NULL-tracking regression tests. cache_schema_test.go / progress_test.go / sync_test.go: expectation alignments only (no test deleted or weakened).
    - Makefile: `make test`/test-go surfaces only WA_MLWH_DSN/WA_MLWH_PASSWORD from .env.development.local for the ./mlwh run (hermetic env undisturbed; integration test runs locally, skips in CI).
    - Verified end-to-end: real `wa mlwh sync` exits 0; all five tables load (seq_ops_tracking_per_sample 1,457,062; useq_run_metrics 299; eseq_run_lane_metrics 282; eseq_run 289; seq_product_irods_locations synced). Reviewer PASS; make lint-go 0 issues; full mlwh suite green (only pre-existing env-only run-dev cmd tests fail).

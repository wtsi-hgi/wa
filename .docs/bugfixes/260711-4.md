- [x] recordingSQLiteStmt.ExecContext returns driver.ErrSkip before checking RejectWriteDuringTrackingRead() when the wrapped stmt does not implement driver.StmtExecContext. In that case, database/sql may fall back to Exec(), which would bypass the write-during-tracking-read guard and undermine the invariant these tests rely on.
    - PR #32 thread `PRRT_kwDORrF2pM6QI4W4`; comment `3564927968`; reviewed commit `76402d5ea587d97ac472dcbadfeaeeee2072b403`.
    - Updated `recordingSQLiteStmt` so direct legacy `Exec`, contextual execution and the legacy fallback all pass through the same write guard and observer recording; fallback performs database/sql-style named-value conversion and cancellation handling instead of returning `driver.ErrSkip` into an unguarded embedded call.
    - Added a focused legacy-statement regression covering guarded direct execution, guarded contextual fallback and successful one-time execution/recording. Independent reviewer PASS: focused regression 18 assertions, tracking tests 150 assertions, all-package compile-only, `make lint`, `gofmt` and `git diff --check` passed.
- [ ] The CI Test check fails `TestClientSyncRepairsSparseProductReadIndexesBeforeIRODSEnrichment` because its SQL mock does not expect the new cold `iseq_product_metrics_irods_repair` marker transaction before the second `iseq_product_metrics` state read.
    - PR #32 Actions run `29164396153`; Test job `86574900030`; target commit `76402d5ea587d97ac472dcbadfeaeeee2072b403`.

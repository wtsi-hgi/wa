- [x] CI Test check is red: the run-dev tests (TestRunDevScript, TestRunDevScriptF1DevCertificates, TestRunDevScriptUsesEphemeralMLWHCacheInTestMode, TestRunDevSnapshotWaitAllowsSlowFrontendStartup, TestStartRunDevForTestAutoCleanup) fail with "run-dev.sh exited before writing frontend snapshot: exit status 1". The run-dev tests must go green WITHOUT skipping or weakening them.
    - Root cause (real, evidenced): in TEST mode run-dev.sh seeds a throwaway ephemeral SQLite cache via `seed_test_mlwh_cache`, but that function hardcoded `schema_version = 7` and only a 10-file subset of the schema. The current `mlwh.CacheSchemaVersion` is 10 with ~20 schema tables, so when `wa results serve --mlwh-cache` opened the seeded cache its drift/version check ran the drop+recreate migration ("mlwh cache: schema v7->v10, recreated tables: [...]"), WIPING the seeded studies + sync_state. The now-empty cache reported "never synced", so `seed_results` (line 1565, `>>RESULTS_LOG`) got a 502 ("cache has never been synced; run \"wa mlwh sync\" first") and the script died under `set -euo pipefail` BEFORE "Starting MLWH server" -- a silent exit (error went to the log, not stderr).
    - Masking: this developer shell exports WA_MLWH_SERVER_URL (a running server); the test harness inherited it, flipping run-dev.sh onto the remote-MLWH branch (REMOTE_MLWH_SERVER_CONFIGURED=1, never opens a local cache) so tests passed locally. CI sets no WA_MLWH_* and took the failing auto-managed ephemeral-cache branch. An earlier fix attempt was wrongly declared green because it was verified with WA_MLWH_SERVER_URL still set.
    - Fix (run-dev.sh): `seed_test_mlwh_cache` now applies EVERY mlwh/cache_schema/sqlite/*.sql (read from the dir, not a hardcoded subset) and records the version parsed from `CacheSchemaVersion` in mlwh/cache.go (new `cacheSchemaVersion()` helper) instead of literal 7. The seeded cache matches the running binary's schema shape+version, so `wa` opens it without migrating/wiping. Drift-proof: adding schema tables or bumping the version no longer breaks the seed. Kept the test-mode early-return in `ensure_mlwh_synced_after_serve` (test mode serves an always-cold ephemeral cache and forbids WA_MLWH_DSN, so once /freshness confirms the server is up it is ready -- no sync, no /studies gate).
    - Fix (cmd/run_dev_test.go): `runDevEnvForTest` now also strips WA_MLWH_SERVER_URL and WA_MLWH_BACKEND_URL from the inherited env so every run-dev test exercises the same MLWH branch CI does (local == CI); tests that want a remote server set it explicitly in their env map. Strengthened TestRunDevScriptUsesEphemeralMLWHCacheInTestMode to assert the run never prints "mlwh sync" or "never been synced".
    - Verified in the CI-equivalent clean env (all WA_MLWH_* unset), real exit codes: `go test ./cmd/ -run 'TestRunDev|TestStartRunDev'` GoTestExit=0, 0 FAIL (118.9s); whole `./cmd` package GoTestExit=0; `make lint-go` 0 issues; `go test ./mlwh/...` pass. No test skipped or weakened.

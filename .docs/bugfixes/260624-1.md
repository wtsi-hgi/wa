- [x] Improve the docs so that admin knows how to (and that they must) set WA_MLWH_SERVER_URL for end-user CLI users. Also, it doesn't seem to work. Given `.env.development.local` with WA_DEV_SEQMETA_PORT=3673, and WA_MLWH_SERVER_URL=https://farm22-wrstat01:3673 set for end-users, I get `Error: resolve "JAGUAR_Genomes15249455": mlwh: upstream database impaired: ResolveStudy request failed: Get "https://farm22-wrstat01:3673/resolve/study/JAGUAR_Genomes15249455": dial tcp 172.27.71.182:3673: connect: connection refused`. By comparison, the WA_RESULTS_SERVER_URL=https://farm22-wrstat01:3672 setting lets them use results register.
    - Split MLWH server bind configuration from client discovery: `WA_DEV_SEQMETA_HOST` / `WA_PROD_SEQMETA_HOST` control `mlwh serve`, while `WA_MLWH_SERVER_URL` remains the public URL used by end-user CLI clients.
    - Updated `make dev` MLWH handling so a configured local MLWH backing source still starts the local server even when the public client URL is set.
    - Documented the admin setup in `README.md`, `DEVELOPING.md`, and env templates, including port choice, the results-server analogy, and the HTTP-vs-HTTPS/TLS requirement.
    - Added regression coverage for scenario bind defaults, remote-bind development setup, and production environment guardrails; `make lint` and `make test` passed in implementation and review.

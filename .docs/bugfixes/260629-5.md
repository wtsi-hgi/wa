- [x] Show the new mlwh feature endpoint outputs in 'wa mlwh info' for study/sample/run, with an optional --since (default now-7d) for study recency
  - cmd/mlwh_info.go: extended the mlwhInfoClient interface with the real StudyOverview/StatusBreakdown/CountSamplesWithData(+Since)/SamplesWithoutData/SampleProgress/RunOverview/RunStatus methods (present on both *mlwh.Client and *mlwh.RemoteClient); added the new study/sample/run sections to infoReport and both the text and --json writers.
  - Added a --since flag (default now-7d, accepts RFC3339 or a Go duration) driving the study recency count; resolved at run time. Per-section graceful degradation (ErrNotFound -> none; other errors -> warnings; exit 0). Both local-cache and remote (--server) modes via the shared buildInfoReport path.
  - cmd/mlwh_info_test.go: behavioural tests (text + --json for study/sample/run, --since default/override, graceful degradation). Reviewer PASS; make lint-go clean; cmd/mlwh packages green (only pre-existing env-only run-dev tests fail).

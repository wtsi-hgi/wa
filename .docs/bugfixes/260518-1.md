- [x] Currently, when you expand a directory in the file browser, it looks like a seperate box opens up underneath, that has a vertical line to the left of it. Instead it should look like the box simple enlarges with its contents inside its own box. Maintain the indenting of its contents.
    - Updated the file browser so expanded directory contents render inside the directory row's own bordered box instead of a separate grouped shell with a decorative left rail.
    - Added Vitest and Playwright regressions proving the expanded content belongs to the row, no connector rail is rendered, and child indentation remains deeper than the parent.
    - Verified with a post-fix screenshot at `.tmp/agent/file-browser-expanded-directory-enlarged-box-postfix.png`; reviewer confirmed the broad `make test` failure was unrelated shared e2e state after focused and serial suites passed.
- [x] The “recent registrations” table has inconsistent sorting icon sizes. Fix there and anywhere else these icons get used.
    - Added `shrink-0` to the shared results-table sort icon so all `ArrowUpDown` affordances keep the same rendered size at narrow widths.
    - Added a Playwright regression at a 390px viewport that measures recent-registration sort SVG dimensions and computed `flex-shrink`.
    - Verified post-fix metrics at `.tmp/agent/recent-registrations-sort-icons-postfix-metrics.json`; `make lint` and `make test` passed in review.
- [ ] The “registration” box takes too much space and duplicates ID and pipeline name from the top “result detail” section. Put the registration details inside the top “result detail” section, get rid of the duplication, and find an attractive and much more compact way of showing all the info. Also compact and integrate the bottom “result metadata” key values in to the top “result detail” section.

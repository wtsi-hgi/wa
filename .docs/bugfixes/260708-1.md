- [x] `wa mlwh export -h` help text is inadequate. It must list and properly explain all possible arguments and make sense. Currently it says `The grammar is 'wa mlwh export <children> <parent-kind> <parent-id>'. Relationships include [...]`, but "relationships" isn't part of the grammar. It should instead list possible children, parent-kind and parent-id. It should also list possible columns. If possible, these sections of the help text should be generated from the code so they don't drift. "Deliverables" needs to be explained in mlwh terms. Why is there a sort option with only 1 supported value? Explain what RFC3339 is by example. In general, someone who knows a little but about MLWH but noting about this codebase must be able to use the export command just be reading this help text. Formulate chunks of the help text as triple backtick multiline strings, and if different wa subcmds share similar bits of helptext, have them all use the same string const for those bits.
  - Added behavioural CLI help coverage for `wa mlwh export -h`.
  - Reworked export help into raw multiline chunks covering grammar, children, parent-kind/parent-id values, selectable columns, deliverables, sorting, RFC3339 examples, paging, server/cache configuration and examples.
  - Moved export relationship and column descriptions into shared MLWH helpers so the rendered child/parent-kind matrix and column lists are derived from export validation/vocabularies.
  - Added a shared MLWH query-command configuration help constant reused by related MLWH subcommands.
- [ ] `WA_LIVE_MLWH_TESTS=1 MLWH_SYNC_PERF_TEST=1 make test` is failing. Fix without skipping tests.

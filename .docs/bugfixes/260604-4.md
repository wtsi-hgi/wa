- [x] Add a `--match` to `wa results register` to limit the files found in the output-dir to those that match the given glob expression. Can be repeated multiple times to OR the matches.
  - Added repeatable `--match` wiring in `cmd/results.go` and output-relative OR glob filtering in `results/scanner.go`.
  - Added CLI and scanner regression tests proving repeated matches include only matching output files while preserving the default scan behavior.
- [ ] Rename `--nextflow-workflow` option of `wa results register` to `--workflow` and make this continue to support nextflow nuances but also let it just be a generic thing that could be for a different workflow system (eg. snakemake), or even just an arbitrary string if the results weren’t produced by a workflow system, but we still need this as its part of the “primary key” we store results under. Do not do a lazy minimal change here; rename and update variables, functions, docs, comments and tests everywhere to move away from the nextflow-specific naming. Make nextflow handling a clean seperate flow and organsise the code so that we could easily add in code to do anything special needed for other workflow managers, with all of them triggered with silent-fail-fast to figure out which one we’re dealing with until we find one.
- [ ] Add the ability to specify the MLWH-related flags of `wa results register` and same keys of `--meta` multiple times. This should, for example, enable searching on a sample in the frontend and it finding result sets where the sample was one of multiple `--sample` values set, for a workflow run that worked on multiple samples in one go.
- [ ] `wa results register -h` help text is clumsily written with repetition and poor ordering. Make it better ordered (most important and required concepts first, clearly stating what is actually required) and improve the concisenes and clarity of everything else.

- [x] remove the forced `mlwh sync` during `make dev` and `make dev-fixtures` and anywhere else.
    - Removed the auto-managed `WA_MLWH_DSN` warm-up sync from `run-dev.sh`
      so dev startup launches `mlwh serve` directly; `make dev-fixtures`
      uses the same path via `make dev FIXTURES=1`.
    - Updated the `cmd/run_dev_test.go` regression to prove `run-dev.sh`
      invokes `results serve` then `mlwh serve` and never calls `mlwh sync`.
    - Verification: implementor and reviewer confirmed the focused regression
      fails against the old startup path and passes after the fix. `bash -n run-dev.sh`,
      `git diff --check`, focused `./cmd` RunDev tests, and `make test-frontend`
      passed. Broad `make lint`, `make test-go`, `make test`, and
      `make test-e2e` still fail on unrelated existing Prettier, results
      serve, and e2e MLWH validation issues.

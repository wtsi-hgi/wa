- [x] Why does sample-crams have such limited column choices, and why is it the only one that gets "ega_id"? I suspect this is bad literal conformance to examples in my input prompt to spec writing. Use the bugfix skill workflow to make column choices sane across options in the CLI and the mlwh endpoints. We want consistency and no abitrary limitations.
- [x] Promote sample-crams by placing it first in the Children list and giving an example of it in the export help text.
- [x] Polish the export help text overall and remove agent literalisms; this is end-user help, not implementation guidance. For example, the Completeness paragraph should read like user help.
- [x] Remove the big repeated blocks on many `wa` subcommands about "Normal CLI user", config, and env vars. Keep non-specific config help on a high-level command and have lower-level commands point to it, e.g. "For config issues, see `wa xxx -h`."

Implementation notes:
- `sample-crams` now shares the file/iRODS export column vocabulary, advertises canonical `accession_number` and `irods_path`, and keeps `ega_id` / `irods_cram_path` as hidden compatibility aliases.
- iRODS/file exports gained sample `accession_number` support, and generated CLI/REST docs derive export column choices from the same vocabulary.
- Export help now promotes `sample-crams`, includes a `sample-crams` example, keeps complete-by-default/no-status behavior, and uses end-user phrasing.
- Lower-level MLWH/results help now points to the high-level command help instead of repeating the long config/environment block.

Verification:
- Focused `./mlwh` sample-crams/export/remote/docs tests passed.
- Focused `./cmd` export/help/results-register tests passed.
- `make lint-go` passed.
- `make test-go` was attempted twice; both runs reached unrelated `cmd/run_dev_test.go` harness failures around dev-server startup/port ownership.

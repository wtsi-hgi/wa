- [x] `.tmp/wa results register --user sb10 --workflow one-off --unique test --match '*.bar' --sample Hek_R1 --meta 'foo=bar' --meta 'foo=baz' .tmp/outputtest` causes the metadata section of the results page for that result set to display incorrectly. It has both “sample Hek_R1” and “Sample 7607STDY14643771”, where only 7607STDY14643771 is clickable to bring up seqmeta details. “Sample 7607STDY14643771” should not show, “sample Hek_R1” should become “Sample Hek_R1”, and “Hek_R1” should be clickable to bring up seqmeta details. The same idea should apply to all other MLWH types (study, library, run). When `.tmp/wa results register --user sb10 --workflow one-off --unique test --match '*.bar' --sample Hek_R1 --sample Hek_R2 --meta 'foo=bar' --meta 'foo=baz' .tmp/outputtest` is run, the comma seperated values become exclusively plain text, when they should remain clickable.
    - Reproduced the single- and repeated-sample metadata states with repo-local browser evidence saved at `.tmp/agent/260605-3-*.png` and `.tmp/agent/260605-3-ui-evidence.json`.
    - Added paired MLWH metadata rendering so user-facing `sample`, `study`, `library`, and `run` rows use title-case labels, hide duplicate canonical `seqmeta_*` rows, and render each repeated value as an individual seqmeta details trigger when canonical/enrichment data is available.
    - Extended seqmeta enrichment lookup to include paired user-facing repeated values from `metadata_values`, while preserving plain comma-separated display for repeated non-MLWH metadata such as `foo: bar, baz`.
    - Added focused Vitest coverage for paired MLWH dedup/clickability and enrichment collection, plus updated browser expectations/stubs for the Hek_R1/Hek_R2 flows.
    - Verification: implementor and reviewer confirmed the focused regressions fail without the fix; post-fix screenshots show `Sample Hek_R1` and individual `Hek_R1`/`Hek_R2` triggers with no canonical duplicate row; `timeout 10m make lint` and reviewer rerun `timeout 20m make test` passed. No `wa mlwh sync` was run.

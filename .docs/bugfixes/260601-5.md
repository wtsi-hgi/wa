- [x] The result rows are still shown under the file browser. Switching between "Combined files" and "result rows" views should actually change the views to show one or the other, not show both in "combined files" view.
  - Added a client wrapper so combined search pages render the combined file browser by default and only render the result table after selecting "Result rows".
  - Verified with the focused repro and full combined-browser Playwright spec; evidence now shows `resultRowCount: 0` in `.tmp/agent/search-combined-file-browser-result-rows-under-combined-repro.json`.

- [x] File preview truncation is broken following recent commits and attempts in prior 3 bugfixes md files to improve this. All 81979 rows of a large tsv are still shown in the file browser, ignoring the preview height and limits on how much data to send to the browser. Clicking to enlarge the preview correctly shows pagination, but the file browser preview should not have been given all the rows to render. Only enough rows to fill the preview height (or the maximum possible preview height, with height slider adjustements then not having to trigger fresh data requests).
	- Fixed the results file preview handler to enforce `line_limit` alongside the existing byte cap for readable previews.
	- Added a regression test proving short-row TSV previews still truncate and set `X-Preview-Truncated` when `line_limit` is below the total row count.
	- Verified `make lint` and `make test` pass after the change.

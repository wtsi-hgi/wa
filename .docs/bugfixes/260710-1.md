- [x] I meant for the `wa mlwh sync` help text btw, where the incorrect short help text is shown by `wa mlwh -h`, and the incorrect long help text is shown by `wa mlwh sync -h`. For the latter, either don't mention the tables, or list them all.
      Updated `cmd/mlwh.go` so the sync short and long help describe supported MLWH metadata/data without a stale table count or incomplete table list. Added `cmd/mlwh_test.go` regression coverage for the `wa mlwh -h` and `wa mlwh sync -h` help boundaries. Reviewer confirmed direct help output plus focused tests, `make test`, and `make lint`.
- [x] `timeout 10m make lint` fails during the help-text fix because root Prettier reports formatting issues in `.docs/nomanifest/phase1.md`, `.docs/nomanifest/prompt.md`, `.docs/nomanifest/spec.md`, and `README.md`.
      Reproduced with `timeout 10m make lint-prettier-root`, formatted only the four reported markdown files, and confirmed `timeout 10m make lint-prettier-root` and `timeout 10m make lint` pass.

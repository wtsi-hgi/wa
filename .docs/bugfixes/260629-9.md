- [x] `wa mlwh info <study>` is still ~5s, and ~4.75s of that is ONE query: the per-platform partition of StatusBreakdown (`statusBreakdownPerPlatformSQL()` in mlwh/progress.go). Every other study/sample/run call is ~0.05s. Two compounding causes, measured against the real MySQL cache for study 7607 (1242 iseq product rows, 469 distinct samples): (1) each platform arm computes delivered-ness with a correlated `CASE WHEN EXISTS (...)` in the SELECT list, so the subquery runs per product-metrics row (1242x) BEFORE `DISTINCT` collapses to samples; (2) the delivered linkage joins on `id_iseq_product`, which has NO index on either `iseq_product_metrics_mirror` or `seq_product_irods_locations_mirror` (and the per-platform arms join `pi.id_pac_bio_product`/`id_eseq_product`/`id_useq_product` = `spi.id_iseq_product`). Rewriting one arm to `LEFT JOIN ... GROUP BY id_sample_tmp` already cut it 4.74s->1.85s with no index; adding the missing product-id index(es) on top should make it index-served (~tens of ms). Fix: rewrite the per-platform partition to avoid the per-row correlated subquery (LEFT JOIN + GROUP BY per sample, MAX(delivered)), AND add the missing index(es) to the mirror schema (both sqlite + mysql) and to the cold-load sparse read-index set so a freshly built/synced cache actually has them (mlwh/sync.go iseqProductMetricsMirrorReadIndexes / mlwh/cache.go iseqProductMetricsSparseReadIndexColumns + the sparse-shape acceptance, and/or ensureAdditiveCurrentCacheIndexes; bump CacheSchemaVersion if the declared schema shape changes). The StatusBreakdown result MUST be byte-for-byte identical (same partition semantics incl. multi-platform double-counting across platforms and ONT always `registered`); preserve all existing StatusBreakdown/parity regression tests. Add a MySQL integration test (real cache creds from .env.development.local, unique throwaway db dropped on cleanup incl. failure, skipped when absent -- same pattern as cache_mysql_integration_test.go) asserting the per-platform query's EXPLAIN is index-served (no full scan of the big mirror) and that `wa mlwh info 7607`-class study breakdown is web-responsive. Target: `wa mlwh info 7607` ~sub-second.
    - Result verified against the real MySQL cache: `wa mlwh info 7607` 4.85s -> 0.15s (study 7147 0.26s); StatusBreakdown output byte-for-byte unchanged (distinct with_data 428; Illumina 98/371/0). One-time ~25s first run = the additive CREATE INDEX on the 7.3M-row iRODS mirror; sub-second thereafter.
    - SQL (mlwh/progress.go statusBreakdownPerPlatformSQL): each product arm rewritten from per-row `CASE WHEN EXISTS(<correlated subquery>)` (evaluated per product-metrics row before DISTINCT) to `LEFT JOIN seq_product_irods_locations_mirror spi ON spi.id_iseq_product = pm.<productID> AND spi.id_study_lims = pm.id_study_lims ... CASE WHEN MAX(spi.id_iseq_product IS NOT NULL)=1 THEN 'with_data' ELSE 'sequenced_no_data' END ... GROUP BY pm.id_sample_tmp`. ONT arm and outer per-platform GROUP BY/SUM unchanged; placeholder count/order per arm unchanged. Semantics proven equivalent (old EXISTS correlated only on the sample, so it was already ANY-delivered semantics; MAX+GROUP BY matches).
    - Index `spi_mirror_study_lims_iseq_product_idx` on seq_product_irods_locations_mirror(id_study_lims, id_iseq_product) added to: declared schema (sqlite + mysql), the sync secondary set AND the cold-load SPARSE read-index set (mlwh/sync.go -- the 260629-7 trap), the sparse-shape acceptance + new seqProductIRODSLocationsSparseReadIndexColumns() (mlwh/cache.go), and the additive path (ensureAdditiveCurrentCacheIndexes via new idempotent ensureAdditiveMySQLIndex helper, which installs it on existing v10 caches with no re-sync). No CacheSchemaVersion bump (consistent with the prior study-index handling). Product-metrics mirrors needed no new index (index-served build side).
    - Tests: all existing StatusBreakdown/parity/schema regression tests pass unchanged (correctness guard for the rewrite); sync_test.go index-name list updated one line (cold load now genuinely installs the index); new MySQL integration test TestRealMySQLPerPlatformBreakdownIsIndexServed (same throwaway-db pattern) asserts pinned ladders + EXPLAIN index-served, no full scan, no dependent subquery -- ran and passed (not skipped). Reviewer PASS; make lint-go 0 issues; ./mlwh and ./cmd green; integration tests run (not skip) with .env.development.local creds.

- [x] File Browser previews now correctly display to the right of file listing in single preview mode, but are incorrectly displaying underneath in “1 preview per row” mode.
    - Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts
    - Approach: removed the `xl:` breakpoint prefix from the grid-mode per-row container (`grid-cols-[minmax(18rem,0.88fr)_minmax(0,1.12fr)]`, `items-start`, border/padding utilities) so the file row and its preview lay out side-by-side at all widths, mirroring the existing single-preview fix.
- [x] File Browser previews redundantly repeat the file name (it is part of the file metadata displayed in the same row). Remove, and also remove the word “preview”. Save additional space by making the download button just the little download icon overlaid on the preview, top right.
    - Files touched: frontend/components/file-preview.tsx, frontend/tests/file-preview.test.ts
    - Approach: removed the `Preview` eyebrow + filename `<h3>` from the single `FilePreview` and the `Image preview` eyebrow + filename `<h3>` from `FileImageThumbnail`; replaced the textual `Download file` button with an icon-only `DownloadIconLink` (rounded-full bordered anchor wrapping `ArrowDownToLine`) absolutely positioned at the top-right of the preview/error containers (which now use `relative`), preserving the `aria-label="Download file"` affordance; updated the binary preview test to drop the filename assertion and added regression tests asserting no filename heading, no `Preview`/`Image preview` eyebrow, and an icon-only download anchor in success and both error branches.
- [x] Changing the preview height takes 10s of seconds “Rendering…” on first usage. It should be ~instant. It also doesn’t seem to actually do anything.
    - Files touched: frontend/components/file-preview.tsx, frontend/components/result-detail-files.tsx, frontend/components/file-browser.tsx, frontend/tests/file-preview.test.ts
    - Approach: threaded a new `maxHeight` prop through `FilePreview` so the slider actually resizes single-mode image/svg/pdf/html/markdown/csv/code previews; fixed `FileImageThumbnail` to pass STABLE intrinsic `<Image>` width/height (672×420) so Next/Image's srcSet is no longer recomputed for every grid thumbnail when only CSS `maxHeight` should change; removed the `key={previewHeight}` on `<PreviewHeightControl>` that was forcing remounts. Added regression tests asserting `maxHeight` is applied to single-mode image previews and that `FileImageThumbnail` intrinsic dimensions stay stable across height-prop changes.
- [x] “Seqmeta details” has a “Resolution” that used to take minutes “Rendering…” and “Looking up x”, sometimes timing out. Why did it take so long? We should be able to get ~instant lookups of related metadata. The last fix I think just caused it to time out faster and give up. Is the issue that the `make run` fixtures are using non-real identifiers? Fix and test this properly by using real identifiers discovered using real Saga in the fixtures, so that the lookups will actually work. As it stands the resolution error message claims “Upstream services were unavailable while resolving this” when this probably was not true; upstream Saga is available, but it is probably being used incorrectly with bad identifiers. But even using an invalid identifier should just get a “not found” type error, not “service unavailable”.
    - Files touched: frontend/lib/seqmeta-enrichment.ts, frontend/tests/seqmeta-enrichment-timeout.test.ts, frontend/e2e/results.spec.ts, .docs/results-web/fixtures/seed.json, cmd/run_dev_seed_metadata_test.go
    - Approach: removed the artificial 5s `withTimeout` wrapper around `enrichIdentifier` so a slow but valid 404 reaches the classifier as `not_found` instead of being mislabelled `upstream_impaired`; rewrote the timeout regression to use vitest fake timers with a simulated 10s upstream delay (so it actually fails on the old code); replaced the three fake seed metadata blocks (`SANG5993`/`SMP5994`/`RNA`) with real Saga-resolvable identifiers discovered via `wa saga inspect` and `wa seqmeta serve` (`3361/3361STDY5994718/2153063`, `7796/Embryo-Imp15289123/9941914`, `4861/4861STDY7771117/3990641` + library `Chromium single cell`); updated the matching playwright e2e expectation; added a Go fixture-sanity test that fails if the banned fake IDs reappear and enforces digit-only LIMS keys.

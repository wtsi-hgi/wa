- [x] Dates throughout the web UI (eg. “last updated” on results page) should display in local time zone. For example I’m in England, the time was 10:03 when I did a register. `date` on the registering machine says `Fri 19 Jun 10:03:29 BST 2026`. But the web UI says “Last updated 19 Jun 2026, 09:03”. Fix it so all dates will display per user expectation.
  - Added hydration-safe browser-local timestamp rendering and reused it on result details, result tables, registration summaries, and file browser mtimes while keeping stable UTC fallbacks for server render.
  - Added a Playwright regression that pins a UTC timestamp in a Europe/London browser and asserts both result and file dates show the local 10:03 time.
  - Verified by implementor and reviewer with focused Playwright coverage plus `make lint` and `make test`; no `wa mlwh sync` was run.
- [ ] The file browser glob filter doesn’t seem to work to filter to files nested within a particular kind of subdirectory, eg. path `/lustre/scratch127/gengen/projects_v2/dual_tf/MORF_10/Multiome/sc_analysis_MORF_v2/sc_analysis_BTLNCK/qc_outputs/HAP1_MORF10_BTLNECK2_1_A1_BTLNCK/HAP1_MORF10_BTLNECK2_1_A1_BTLNCK_py_inputs/atac/barcodes.tsv.gz` is not found by filter `*py_inputs*.tsv.gz`. Glob should work across directories or there should be some way of acheiving the desired filter.

- [x] The following hangs until I kill it. There should be an ~instant resolution that the samples are not valid and instant error.

    ```
    $ make dev FIXTURES=1
    make[1]: Entering directory '/home/ubuntu/wa'
    Building Go binary at /home/ubuntu/wa/.tmp/wa
    Starting results server on https://127.0.0.1:3675 (mode=dev)
    Seeding fixtures from /home/ubuntu/wa/.docs/results-web/fixtures/seed.json
    Starting seqmeta server on http://127.0.0.1:3676
    Waiting for seqmeta studies readiness at http://127.0.0.1:3676/studies (MLWH cache: hgi_wf_test_admin@tcp(hgi-workflow-automation-test-db.internal.sanger.ac.uk:3324)/workflow_automation_mlwh_sb10;?parseTime=true; a cold cache can take a while on first run)
    Starting frontend dev server on https://127.0.0.1:3674
    Development environment is ready.
    Results: https://127.0.0.1:3675
    Seqmeta: http://127.0.0.1:3676
    Frontend: https://127.0.0.1:3674
    Logs: /home/ubuntu/wa/logs

    $ WA_ENV=development time .tmp/wa results register --user sb10 --workflow one-off --unique test --match '*.foo' --sample gallery-beta --sample gallery-alpha --meta 'foo:bar' --meta 'foo:baz'
    .tmp/outputtest
    ^CError: resolve --sample "gallery-beta": mlwh: upstream database impaired: query sample cache: context canceled
    [...]
    Command exited with non-zero status 1
    0.00user 0.01system 0:32.31elapsed 0%CPU (0avgtext+0avgdata 19584maxresident)k
    0inputs+0outputs (0major+1148minor)pagefaults 0swaps
    ```

    - Fixed the root cause by making dropped-index sample cache repair rebuild the full `sample_mirror` lookup index set instead of leaving only `sample_mirror(name)` on large warm caches.
    - Follow-up live cache check found the development MySQL cache had only `sample_mirror_name_idx` even though `sync_state.indexes_dropped` was already `0`; updated repair to detect and create missing sample lookup indexes for that completed-cache state too.
    - Removed the rejected hardcoded `gallery-*` / `sample-*` shortcut and added register regressions proving invalid `gallery-beta` fails quickly without posting while a fixture-shaped value can still resolve through broad sample lookup.
    - Files: `cmd/results.go`, `cmd/results_register_test.go`, `mlwh/cache.go`, `mlwh/cache_test.go`, `mlwh/resolver_sample_test.go`, `mlwh/sync.go`, `mlwh/sync_test.go`.

- [x] `.tmp/wa mlwh info 7607STDY14643771` works instantly, listing `supplier_name: Hek_R1`. But `.tmp/wa mlwh info Hek_R1` takes 1m11.816s to return the same result. Find the cause of the slowness and fix it. Our MLWH cache ought to have appropriate indexes on everything needed by they queries we do.
    - Repaired legacy sparse sample caches on open so supplier-name and other alternate sample lookups use indexed `sample_mirror` columns instead of scanning.
    - Added `Hek_R1`-style supplier-name coverage on a sparse reopened cache and updated sync/cache repair expectations to create all sample lookup indexes and clear `indexes_dropped`.
    - Live verification rebuilt `.tmp/wa`; the first `mlwh info Hek_R1` run repaired the missing MySQL sample indexes without sync and returned in 1:25.82, then a second run returned in 0.07s. No `wa mlwh sync` was run.

- [x] When "search builder" was renamed "search" it lost its styling. It should have styling to match the "file browser" title, and also have a little search icon next to it like the file browser has an icon. The permanent search filters shouldn't say "value" after them.
    - Restored the Search title's icon and uppercase/tracked title treatment to match File Browser.
    - Removed "value" from permanent filter labels while keeping added-filter labels unchanged.
    - Added a browser-rendered Playwright regression comparing the rendered Search and File Browser title metrics with the real app stylesheet, plus focused Vitest coverage for labels and behavior.
- [x] Rename the "Result rows" button to "Result sets”.
    - Renamed the combined search toggle label to "Result sets" while keeping the internal `rows` mode and behavior unchanged.
    - Updated dashboard and Playwright coverage to assert the new label and preserve mode switching.
- [x] Get rid of the double titleing of “Recent registrations / Latest results sets” and title that box just “latest result sets”, using the same title styling as for “search” and “file browser”, ideally with an appropriate icon next to it, if there is an icon that could represent “latest”.
    - Replaced the dashboard table's double `Recent registrations` / `Latest result sets` heading with one `Latest result sets` title using the shared compact title treatment and a `History` icon.
    - Preserved the distinct search-results title text and updated affected e2e waits.
    - Added unit and browser-rendered Playwright coverage comparing the latest, search, and file-browser title treatment.
- [x] Make the “Add filter” pill better match in styling to to the “Columns” pill, ie. reduce the margin/padding.
    - Tightened the Add filter button to the same compact rounded-pill density as Columns while preserving the popup behavior.
    - Added a browser-rendered Playwright regression comparing Add filter and Columns metrics and verifying the dialog still opens.
- [x] Move the “x rows” indicator on latest result sets to the bottom of the box next to the “rows per page” selector.
    - Moved the row-count pill from the latest-result-set header actions into the footer controls beside Rows per page.
    - Added unit and browser-rendered Playwright coverage proving the count sits near the page-size selector and away from the title/header.
- [x] Don’t show an initial (suggested/placeholder) value in permanent or being-added filters.
    - Removed example placeholders from empty permanent fields and the selected Add filter value input while keeping datalist suggestions wired.
    - Added unit and browser-rendered coverage for empty inputs, active URL values, and autocomplete wiring.
- [ ] Change the login system top right to longer show the avatar anywhere, only the username.

- [x] Web interface seems broken. Button clicks on the landing page aren’t working. Visiting a result set page from `make run` can make the browser non-responsive. If you get as far as clicking on files in the file browser it can get stuck on “Rendering…”.
    - Removed the landing page's blocking live-study suggestion fetch so SSR no longer waits on SAGA before hydration.
    - Added dashboard regression tests for non-blocking render behavior and continued manual study filtering.
- [x] File Browser previews appear in nested bordered boxes. Eliminate the wasted container boxes so the preview only has 1 visible border and takes up all available space in the box.
    - Removed the extra bordered preview wrapper so FilePreview is the only visible bordered surface in single-preview mode.
    - Made FilePreview fill its available panel space and added jsdom plus Playwright layout regression coverage.
    - Removed an unused seqmeta helper so repository lint passes.

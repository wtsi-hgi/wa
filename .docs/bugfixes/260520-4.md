- [x] The file browser box has a horizontal rule under the “file browser” title. Get rid of it.
    - Removed the visible bottom divider from the File Browser title row while preserving the compact header spacing and surrounding browser shell.
    - Added a Playwright regression asserting the rendered File Browser header has no effective bottom border, with before/after screenshots captured from the existing `nf-core/rnaseq` fixture.
    - Validation: focused file-browser tests and `make lint` passed. One full `make test` rerun passed for the implementor; later independent full-suite reruns hit an unrelated `seqmeta-lane-filter` row-count flake while all file-browser tests passed.
- [x] The main content boxes on the results page (the info box and the file browser) have quite a lot of margin to the page edges. Reduce this a little bit so it still looks attractive but wastes a little less horizontal space. Make the internal padding/margin of the content boxes consistent between the boxes, erring on the side of minimising the value.
    - Widened the result detail rail from `max-w-7xl` to a restrained `84rem` and reduced responsive page padding, cutting desktop edge margins while keeping the boxes framed.
    - Matched the result info box and file browser internal padding at a compact `12px`, and reduced the file browser tree shell padding to avoid stacked inset waste.
    - Added a geometry-based Playwright regression that measures edge margins, box widths, internal padding, first content insets, and mobile overflow across desktop/tablet/mobile viewports.
    - Validation: focused result-detail spacing/header/file-browser Playwright checks and `make lint` passed. Full `make test` continues to hit an unrelated `seqmeta-lane-filter` row-count flake in full-suite order while the new spacing spec and isolated lane-filter spec pass.
- [x] Get rid of the “Preview height” control in the file browser. Instead, add a standard resize affordance in the bottom right of previews that lets users drag the preview to the desired size. This should set a hidden global preview height value that immediately applies to all previews shown on the page and for all folders going forward.
    - Removed the visible `Preview height` range control from file browser controls and added bottom-right resize handles on single, grid, and subfolder preview frames.
    - Replaced per-directory preview height storage with one hidden clamped result-page height value, so resizing one preview immediately applies to other visible previews and persists when moving between folders and preview modes.
    - Added regression coverage for absent visible height controls, draggable resize handles, global height propagation, folder navigation persistence, and no inline-preview refetch on height changes.
    - Validation: focused file-browser/result-detail Vitest, focused file-browser Playwright, and `make lint` passed. Full `make test` remains blocked by the unrelated `seqmeta-lane-filter` full-suite row-count flake, while that spec passes in isolation.

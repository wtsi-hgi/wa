- [x] In the file browser, for a folder eligible for both the “subfolder previews” and the “1 preview per row” selectors, there are 2 preview height widgets, only 1 of which has an affect when “1 preview per row” is selected. There should be just 1 height widget that affects all 3 modes (single preview, 1 per row, subfolder).
    - Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts
    - Approach: merged the mixed folder-row controls into one shared preview-height widget and routed single-preview, 1-preview-per-row, and subfolder-preview rendering through the same height state with regression coverage for the mixed-eligibility case.
- [x] Preview height affects does not affect tsv files. Make sure preview height correctly adjusts the size of all file formats in all modes and situations. This was probably a regression from the last bugfixes md file where the bug report asked to get rid of the different containing border, but it seems the containing box was needed for correct sizing and layout. Revert and do this correctly: keep the layout and functionality, but just make sure the containing box is not VISIBLE.
    - Files touched: frontend/components/file-preview.tsx, frontend/tests/file-preview.test.ts
    - Approach: restored the invisible max-height/overflow layout wrapper around inline delimited previews so TSV and CSV previews obey the shared preview-height control without reintroducing a visible nested border or rounded box, and added a regression that proves complete inline TSV previews keep that invisible wrapper.
- [ ] When the file types drop down is open, capture all clicks outside it to close it. In particular, I always click above the file types widget to close it, but this usually ends up closing the folder. Prevent this folder close from happening and only close the drop down.
- [ ] The previous bugfixes md file attempted to make subfolder previews ui persistent, and now you can open multiple nested directories each with their own “subfolder previews” related UI. However, selecting a child one actually activates the highest parent, so functionally the fix was useless. Fix this properly so it is not just a visual change, but a change that lets someone enable subfolder previews, with different file type and preview height settings, on subfolders of subfolder-preview-eligible folders. Only the most recently selected folder will display its previews, but unselecting will then let you select a different elgible folder (ancestor or decendent) and the new one will have kept its own settings.

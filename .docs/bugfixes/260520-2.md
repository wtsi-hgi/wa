- [x] The “metadata” section next to “run details” at the top of results pages has strange display behaviour. On a fresh load of a page with 5 bits of metadata/ page width small enough that the metdata displays across 2 or 3 lines, they show across 3 lines with a vertical scrollbar since the 3rd line doesn’t fit. But then closing the root file browser folder makes the metadata items display across 2 lines and the scroll bar goes away. Solve this odditiy so that page interactions can’t change layout of the metadata section, and that in this example the 5 items display across 2 lines with no scroll bar. My example was a result with the following seqmeta set on it: library id:71046409, library type: custom, run: 48522, sample name: 7607STDY14643771, study id: 7607.
    - Changed the compact result metadata strip so seqmeta loading/error status is rendered as an overlay marker instead of inline text that changes badge width and wrapping.
    - Added a Playwright regression for the five-item seqmeta case at the reproduced viewport; it verifies two stable metadata lines with no vertical overflow before and after collapsing the root folder.
- [x] The “Preview height” control in the file browser is inconsistent with the controls to the left of it: it is taller, and not wide enough to fit the “Preview height” text. Put the selected px value text next to the slider so it fits on 1 line, solving the height issue. Make the control wide enough for its contents. The 3 controls should still typically fit on 1 line; give less space to the directory name if that might not be the case.
    - Reworked the file-browser preview height control into one horizontal row with the label, slider, and selected pixel value beside each other, widening it enough to avoid label overflow and matching the height of the neighboring controls.
    - Let the directory button shrink before the controls wrap, preserving the compact inline control row from the previous bugfix.
    - Extended the file-browser Playwright layout regression to assert height parity, no control overflow, and the pixel value positioned beside the slider.

- [x] In the search box of the landing page, move the 5 called out fields back in to the “Add filter” dropdown (pinned to the top, and rename it “Add specific field to filter”), and have a generic search box with the + instead. This generic search will quickly (parallel searches with early returns on fail? A quick all-columns single search?) search all fields to find matches, so you can search on a string without knowing what type of thing that was. Indicate in the auto-complete which types of things are matching, so if multiple types of things match, the user can pick the desired type and add that as a search filter. Also, make the search work with substrings of actual values in the database. For now limited to values in our results registration database, not our mlwh cache database, unless it seems like substring search in mlwh will be trivial and not a performance concern.
    - Added registration-DB substring search suggestions/search matching across core result fields and metadata values, exposed through the results API and a frontend suggestion route.
    - Reworked the landing filter builder so the five common fields are pinned in the renamed “Add specific field to filter” dropdown while the compact generic search input adds typed matching filters.
    - Added Go, Vitest, and Playwright regression coverage, including the repro screenshot path under `.tmp/agent/`; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] The file browser box (regardless of it is formed for search results, or when viewing a results page), should have a filter box at the top (in line with the “file browser” title) where a user can type a glob pattern, which should result in the file browser only showing files that match the glob pattern, and the directories that contain those files. Next to the box should be a little “save” icon, which should store the value in kept-for-a-long-time-cross-session-user-local-browser-storage of some kind against a key of the pipeline (for searches, a dedup’d combination of matching pipelines), so that the next time the user sees a file browser for similar data, their filter auto-populates and applies. Otherwise the filter auto-clears when they view a new file browser.
    - Added compact File Browser header glob controls with an icon-only save button, filtering files before the directory tree is built so only matching files and containing directories remain visible.
    - Stored saved filters in localStorage under single-pipeline detail keys and sorted deduped multi-pipeline search keys, while unsaved filters remain scoped to the current browser key.
    - Added Vitest and Playwright regression coverage, including post-fix evidence screenshots under `.tmp/agent/`; `wa mlwh sync` was not run.
- [x] The “file types” dropdown which determins what file types to preview should switch from the vague categories it currently has to specific file extensions we actually support, with the execption of keeping “Images” for all bitmap formats. svgs should be their own separate type. Because there will be many filetypes, make the display and selection of these much more compact, ie. not just one per line of the dropdown.
    - Added a shared preview file-type helper so the file browser selector, preview gating, bitmap image detection, gzip extension handling, and inline-fetch bypass rules use one supported-extension source.
    - Replaced vague categories with compact multi-column choices: `Images` for bitmap formats plus individual supported extensions such as `.svg`, `.csv`, `.tsv`, `.md`, `.html`, `.log`, and `.pdf`; SVG remains visually sized like image previews but is selected independently.
    - Added Vitest and Playwright regressions with post-fix evidence at `.tmp/agent/file-types-dropdown-compact-extensions-postfix.png`; `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] Add a `--project` option to `wa results register`, but implement it as if `--meta "project=xxx"` was used.
    - Added repeatable `--project` as a `project=value` metadata shortcut that appends into the same metadata stream at flag parse time, preserving cross-flag occurrence order, repeated-value deduplication, and first-value `metadata` semantics.
    - Added register command regressions for project metadata, `--meta project` coexistence, `--project` before `--meta project=...`, and help ordering; focused register tests, `make lint`, and `make test` passed. No cache schema change was needed, and `wa mlwh sync` was not run.
- [x] ”Latest Result Sets” box should give access to all result sets since the beginning of time via pagination. Currently, despite the pagination controls, it only seems to offer 1 page of results, even when there have been more than 10 registrations. Changing “rows per page” should also work.
    - Dashboard stats loading now follows the backend `total` count with a second stats request sized to all latest result sets when the initial 10-row response is incomplete, so the existing table pagination and rows-per-page controls receive every historical latest row.
    - Added a dashboard unit regression and a Playwright regression covering page 2 plus the 25-row selector, and kept the expanded 12-registration fixture ordered so existing first-page fixture flows remain stable.
    - Updated run-dev fixture-count tests to read the seed count instead of assuming six registrations; focused tests, `make lint`, and `make test` passed. `wa mlwh sync` was not run.
- [ ] ”Latest Result Sets” first column should be “Project”, with “Pipeline Name” relegated to optional via the “Columns” selector. “Project” value should be poplated from project metadata. If the result set has no project metadata set, display pipeline name instead.
- [ ] It should be possible to easily copy the full path of any file or directory displayed in the file browser. Perhaps add a little copy icon next to the path/names, that doesn’t trigger normal behaviour of clicking a directory or file box.

- [x] Clicking on a result set spends an innordinate amount of time (10s of seconds) “Rendering…” when on the `make run` demo result sets there is only a tiny of amount of content to display. It should be instantaneous. Going back to the default page with search builder also takes ~2seconds of rendering, which should also be instantaneous.
    - Frontend detail rendering no longer blocks on server-side seqmeta enrichment; the existing client enrichment path loads after hydration instead.
    - Dashboard fetches for stats, meta keys, and studies now start concurrently so returning to the search builder is no longer serialized on backend calls.
    - Regression coverage added for both the non-blocking detail render path and concurrent dashboard fetch scheduling.
- [x] The “Result Detail” header box has an identifer that is truncated and a copy-to-clipboard icon, but clicking the icon or the id has no effect. Enable to the copy-to-clipboard feature and change the mouse pointer on hover to indicate the clickability.
    - Result ID copying now uses the Clipboard API when available and falls back to `document.execCommand("copy")` when it is not.
    - The chip now shows clear click affordance and success/error feedback so clicks are no longer silent.
    - Focused regression coverage was split into a dedicated unit test plus a Playwright browser check for the pointer cursor.
- [x] The “Result Detail” page currently breaks the view of files in to seperate “Directories”, “Directory Contents” and “Preview” sections. Instead it should be a single “File Browser” pane with listing of folders that you can click on to list their directory contents at the top and file contents beneath, showing up to 100 files, with pagination. The file listing should be split in to 2 columns, like Windows file explorer with image previews turned on to the right. By default the right column will span all rows and show the single preview. The toggle for 100 previews will change it to 1 preview per row, with a preview height slider, with pagination ability to see any more. Improve the fixtures used by `make run` to have multiple directories with some nesting per result set, to better demo this interface.
    - Image previews now show a visible "Click to enlarge" affordance and open an in-page lightbox overlay that uses the original full-size asset.
    - Grid mode continues to request thumbnail previews with `thumb=true` for the first 100 items, while the overlay requests the original image URL.
    - Regression coverage includes a focused file-preview unit test and a Playwright browser check for grid-thumbnail versus full-size lightbox behavior.
- [x] Previews should be clickable to make them pop up and fill most of the screen (as an overlay, not a new page), using the original full-size image. Use appropriate HTML technology to make the request and send of 100 thumbnails efficient.
- [x] The click on a seqmeta\_\* value in the “Result Metadata” value opens an overlap pop-up, but it needs a vertical scroll bar if the contents don’t fit in the window.
    - The seqmeta details dialog now caps its viewport height and makes the body scroll independently so long metadata stays reachable.
    - Regression coverage includes both a focused unit assertion on the scroll container classes and a Playwright viewport test that verifies actual scrolling.
- [x] The seqmeta\_\* row and contents once clicked don’t readily make any sense. For example, the `make run` fixture for the wtsi/amplicon result set shows the key as seqmeta_library, but the value as “sanger_sample_id: RNA”. “RNA” may or may not be a library type, but it’s not a sample id and it doesn’t make any sense for a library value to be mentioning a different key in the value. Once clicked on, the “Summary” box has a “status” section that lists 6 apparently random and unexplained identifiers.
    - Seqmeta badges now keep the selected metadata value as the primary label unless the enrichment resolves to a study name, so `seqmeta_library=RNA` renders as `RNA` instead of borrowing a different identifier type.
    - The dialog sidebar now explains the resolution path with explicit sentences instead of an opaque `Status` dump of mixed identifiers.
    - Regression coverage was updated for the new library-focused label, resolution copy, partial-state messaging, and non-blocking client enrichment flow.

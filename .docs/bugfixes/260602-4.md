- [x] In the file browser, when a directory path is too long to display and it is truncated with `...`, make hovering over the path show the full path.
    - Reproduced with a focused Playwright regression in `frontend/e2e/file-browser-layout.spec.ts` using the seeded `nf-core/rnaseq` root directory at a narrow viewport. Evidence written to `.tmp/agent/file-browser-truncated-directory-hover-repro.png` and `.tmp/agent/file-browser-truncated-directory-hover-repro.json`; the test is red because the label is truncated but no title/custom-tooltip hover signal exposes the full path.
    - Added the full directory path as the native hover title on the truncated directory label without changing row click/expand behavior.
    - Verified post-fix evidence in `.tmp/agent/file-browser-truncated-directory-hover-postfix.json`: the label remains truncated with ellipsis and `labelTitle` equals the complete path.

- [x] The tools panel (containing "preview modes" and "file types" buttons) should have even padding/margin above and below as it had 2 commits ago. It also doesn't need quite as much internal padding/margin.
    - Reproduced current excessive density in browser at 1440x900 with red Playwright spec `frontend/e2e/file-browser-tools-panel-density-repro.spec.ts`. Evidence: `.tmp/agent/file-browser-tools-panel-density-repro-current.json`, `.tmp/agent/file-browser-tools-panel-density-repro-detail.png`, and `.tmp/agent/file-browser-tools-panel-density-repro-panel.png`.
    - Current geometry: panel padding is 8px on all sides, trigger top/bottom clearance is 9.375px/9.375px, trigger height is 41.25px, and panel height is 60px. Heading/name-area top and bottom clearance are symmetric at 0px, and the selected row-to-panel top clearance is 9px.
    - `git show HEAD~2:frontend/components/file-browser.tsx` shows the same `folderControlsClass` (`p-2`) and `controlTriggerClass` (`px-2 py-1`) as current HEAD, so the two-commits-ago reference confirms vertical symmetry rather than a smaller class value. The new red assertions target the user's additional request to reduce internal padding/margin while preserving that symmetry.
    - Tightened the tools panel surface from `p-2` to `p-1.5`, keeping the existing trigger sizing and row placement.
    - Verified post-fix browser evidence: panel padding is 6px on all sides, trigger top/bottom clearance is 7.375px/7.375px, and panel height is 56px.
    - Updated the existing equal-padding regression to require the compact 6px panel while preserving title spacing, logout revocation, and directory hover-title coverage.

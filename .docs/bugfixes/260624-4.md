- [x] Multichannel tiff file preview still doesn’t work, same “preview unavailable Unable to read Tiff metadata Unable to load Tiff metadata” as before with the same tiff files in `.tmp`. Make it actually work.
    - Implemented backend-resolved local file path handling: after authorizing the exact registered file through the results backend, successful file responses expose `X-WA-Resolved-File-Path`, and the OME route uses that absolute path for Sharp/open without guessing from the repo root.
    - Confirmed the normal contract: CLI registration, fixture seeding, backend validation, and frontend combined-file logic use absolute `FileEntry.path` values under an absolute registered `output_directory`; the backend header also supports legacy relative result-file rows by resolving them under the registered output directory after exact registered-path authorization.
    - Added backend and frontend regressions for OME metadata and first-plane WebP rendering from a TIFF nested under a registered-output subdirectory, preserving `HEAD` authorization with `download=true`, no backend body `GET` fallback, and stale-JWT public fallback behavior.
    - Verified exact fixtures through the route via absolute paths: `.tmp/ome.tiff` metadata 200 (`channels=4`, `sizeZ=16`, `2160x2160`) and WebP plane 200 (`25578` bytes); `.tmp/ome_collapsed.tiff` metadata 200 (`channels=11`, `sizeZ=1`, `2160x2160`) and WebP plane 200 (`4848` bytes).
    - Full-gate stabilization also required making the run-dev tests wait for the later MLWH/front-end summary lines after `Development environment is ready.` before inspecting stdout.
    - Verification passed: focused run-dev test, focused backend file-serving test, focused registered-output-subdirectory Vitest, full `frontend/tests/file-proxy.test.ts`, `make lint`, and `make test`; no `wa mlwh sync` was run.

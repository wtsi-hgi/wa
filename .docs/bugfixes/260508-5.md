- [x] Recent commits for the previous bugfixes md file tried to make subfolder previews ui persistent, but it seems to have broken the appearance of this ui on (nested) subfolders of a subfolder-eligible dir that are themselves eligable. Preview-related UI widgets must always display on all eligble folders when they are in expanded state. Some folders could show both “1 preview per row” and “subfolder previews” selectors, if they’re eligble for both.
    - Improved the `make dev-fixtures` `galleries-demo` seed data with nested eligible subfolders under `sample-a`, including nested graphical outputs and TSVs so the broken parent-plus-child expanded case is reproducible in the live app.
    - Updated `frontend/components/file-browser.tsx` so subfolder preview controls are computed per expanded eligible row instead of being routed through a single owner directory, allowing expanded eligible parents and children to show their own preview widgets simultaneously while preserving existing persistence behavior.
    - Added focused regressions in `frontend/tests/file-browser.test.ts` and `frontend/e2e/file-browser-layout.spec.ts`, strengthened `cmd/run_dev_seed_metadata_test.go` to verify the new fixture topology, captured before/after screenshots for the live repro, and the reviewer confirmed `make lint` and `make test` pass.
- [x] Previews for tsvs in “subfolder previews” mode display in a containing box with a different background colour and corner radius. One of the recent bugfixs md files already had fixes that were supposed to get rid of all these containing boxes and make all previews for all file types in all file browser modes consistent, not have multiple borders, and not waste space. The wasted space issue is fixed, but extra borders are still here. Do an exhausive check with screenshots against every possible combination to make sure this is properly fixed. Improve the fixtures used by `make dev-fixtures` to demo all these combinations so you can do the screenshots.
    - Expanded the `galleries-demo` dev fixture with direct markdown, HTML, log, and nested markdown/log files so live `make dev-fixtures` coverage now includes representative single, grid, and subfolder preview combinations alongside the TSV/table repro.
    - Updated `frontend/components/file-browser.tsx` so subfolder preview frames are layout-only and no longer add an extra visible shell around image, table, markdown, or code previews, while `frontend/components/result-detail-files.tsx` keeps non-image grid previews stretched to the shared preview height and the image path retains its width/overlay behavior without extra chrome.
    - Added focused regressions in `frontend/tests/file-browser.test.ts` and `frontend/e2e/file-browser-layout.spec.ts`, strengthened `cmd/run_dev_seed_metadata_test.go`, captured before/after screenshots under `.tmp/agent/bug2-screenshots/`, and confirmed `make -C /home/ubuntu/wa test` plus `make -C /home/ubuntu/wa lint` pass on the finished bugfix.

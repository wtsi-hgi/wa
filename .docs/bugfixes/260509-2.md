- [x] Fix the issue with the bad column name. The database referenced in
      .env.development.local is the only and canonical mlwh, so take its
      current schema as gospel. Try to make us less likely to break on future
      upstream schema changes if possible, or at least make it easier to fix
      these issues if it isn't already easy. - Files touched: mlwh/all_studies.go, mlwh/sync.go,
      mlwh/sync_real_schema_test.go - Approach: added a shared study-source query fallback that probes the live
      MLWH study schema after unknown-column errors and aliases known upstream
      column variants into the cache's canonical names, then made study scans
      normalize nullable upstream strings to empty strings so cold-cache
      read-through, sync, `wa mlwh info`, and seqmeta `/studies` tolerate the
      current canonical MLWH schema and data.

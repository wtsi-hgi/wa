- [x] The “All details” button to the right of “run details” can look too far from run details, being close to the “Metadata” title. Move it to right next to “run details” text instead, and just lable it “all”. Make the same change for all metadata button (when it is displayed) as well for consistency.
    - Integrated Run details and Metadata headers now keep the trigger immediately after the heading text instead of pushing it to the far edge.
    - The visible trigger label is `all` for both details popovers; the accessible names remain `All details` and `All metadata`.
    - Added component regressions for the visible label/accessibility pairing and browser geometry checks that require each trigger to sit within 16px of its heading.
    - Post-fix evidence: `.tmp/agent/result-detail-header-all-buttons-postfix.png` and `.tmp/agent/result-detail-header-all-buttons-postfix-dom.json`.

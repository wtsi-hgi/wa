- [x] In the Seqmeta Details for a studyid, the related data is library types, when it probably should be the more specific library ids which would then heirachically link to the samples under this study. In any case, the samples that are revealed on clicking the “Samples” button on a row in the related libraries for a study should be limited to samples that in both the library and the study, if that is not already the case.
    - Study enrichment now preserves specific library identifiers (`library_id` / `id_library_lims`) so same-type libraries are listed separately instead of being collapsed by type.
    - Study sample expansion now sends the specific library identity to the backend and filters samples by both the selected library and requested study.
    - Files touched: `seqmeta/enrich.go`, `seqmeta/server.go`, `seqmeta/enrich_hierarchy_test.go`, `seqmeta/server_test.go`, `frontend/app/(results)/actions.ts`, `frontend/components/seqmeta-badge.tsx`, `frontend/lib/contracts.ts`, `frontend/lib/seqmeta-enrichment.ts`, `frontend/tests/contracts.test.ts`, `frontend/tests/seqmeta-badge.test.ts`, `frontend/tests/seqmeta-enrichment.test.ts`.
    - Verification: implementor and reviewer confirmed the new Go/Vitest regressions fail before the fix; focused seqmeta and frontend tests passed; reviewer reran `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
    - Reopened on 2026-05-15: user reports Seqmeta Details for study `7607` still list only library types, not specific library IDs. The retry must determine the actual MLWH hierarchy and make study details prefer that hierarchy rather than treating library type as the primary related object.
    - Retry root cause: old positive server-side enrichment cache rows could still contain the pre-hierarchy type-only graph, so the UI reused stale `Custom` related libraries even after fresh enrichment learned library IDs.
    - Retry fix: added a versioned `enrich_cache.cache_version` migration and made the seqmeta server ignore older hierarchy-cache entries, forcing a fresh enrichment that includes specific library identifiers while preserving current-version cache reuse.
    - Retry files touched: `seqmeta/store.go`, `seqmeta/server.go`, `seqmeta/types.go`, `seqmeta/server_enrich_test.go`.
    - Retry verification: reviewer confirmed the stale study-cache regression fails without the cache-version fix, frontend already prefers `id_library_lims`/`library_id` when present, and focused seqmeta/frontend tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check` pass.
- [x] `wa results register --library 71046409` fails with `Error: resolve --library "71046409": mlwh: identifier not found` even though it is a legitimate library id. Make the library option work for both library ids and types.
    - `mlwh.ResolveLibrary` now resolves exact `pipeline_id_lims`, `library_id`, and `id_library_lims` values from the cache without running sync.
    - `results register --library <library_id>` now stores the exact ID as `seqmeta_library` and the resolved type as `seqmeta_librarytype`, while existing library-type input remains supported.
    - Seqmeta validation and enrichment accept exact library identifiers and filter enrichment to the matching library.
    - Files touched: `cmd/results.go`, `cmd/results_register_test.go`, `mlwh/resolver.go`, `mlwh/resolver_test.go`, `results/types.go`, `results/validate_test.go`, `seqmeta/enrich.go`, `seqmeta/enrich_test.go`.
    - Verification: implementor and reviewer confirmed the numeric library-ID regressions fail before the fix; focused Go packages passed; reviewer reran `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
    - Reopened on 2026-05-15: user reports `wa results register --library 71046409 [...]` is slow again, stores `seqmeta_library` instead of expected `seqmeta_libraryid`, and the resulting `seqmeta_library` value is treated as a library type and shows “No enrichment matched this library type value.” A retry must preserve library type support but make library IDs first-class as `seqmeta_libraryid` and keep registration under one second.
    - Retry fix: `--library 71046409` now records `seqmeta_libraryid=71046409` plus `seqmeta_librarytype=Custom` when the cache has type context; library type inputs still record/use `seqmeta_librarytype`.
    - Retry fix: MLWH, results search, seqmeta validation/enrichment, and frontend filters now keep library type, library ID, and library LIMS ID paths separate so `seqmeta_libraryid` is not expanded or labelled as a type.
    - Retry performance: added indexed cache lookup/repair for `library_samples.library_id` and `id_library_lims`, and a registration regression proving the broad library-type path is not called and the realistic library-ID path stays under one second.
    - Retry files touched: `cmd/results.go`, `cmd/results_register_test.go`, `mlwh/cache.go`, `mlwh/cache_schema/mysql/library_samples.sql`, `mlwh/cache_schema/sqlite/library_samples.sql`, `mlwh/cache_schema_test.go`, `mlwh/hierarchy.go`, `mlwh/hierarchy_test.go`, `mlwh/mlwh.go`, `mlwh/resolver.go`, `mlwh/resolver_test.go`, `results/server.go`, `results/server_test.go`, `results/types.go`, `results/validate_test.go`, `seqmeta/enrich.go`, `seqmeta/enrich_test.go`, `seqmeta/types.go`, `seqmeta/types_test.go`, `seqmeta/validate_test.go`, `frontend/app/(results)/page.tsx`, `frontend/components/filter-builder.tsx`, `frontend/components/seqmeta-badge.tsx`, `frontend/lib/seqmeta-enrichment.ts`, `frontend/tests/filter-builder.test.ts`, `frontend/tests/seqmeta-enrichment.test.ts`.
    - Retry verification: review initially failed because `seqmeta_libraryid` search still expanded as a library type and the diff was too noisy; the follow-up separated exact ID search, reduced unrelated churn, and the final reviewer added frontend filter-key coverage before passing focused Go/Vitest tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
- [x] The Seqmeta Details for a runid only lists library type under related data. It should give the library id and samples as well.
    - Run-id enrichment now carries `library_id` and `id_library_lims` through the seqmeta graph and keeps grouped run samples under the run study details.
    - The Seqmeta Details UI now renders run-related libraries with specific library identity and expands the samples already present in the run payload instead of doing a broad library-sample fetch.
    - Files touched: `seqmeta/types.go`, `seqmeta/enrich.go`, `seqmeta/enrich_hierarchy_test.go`, `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`.
    - Verification: implementor confirmed the Go and frontend regressions fail before the fix; a first review found ambient Playwright gate failures, a follow-up clean rerun passed them without code changes, and the final reviewer reran focused tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
- [x] Resolving the result metadata for an example result with librarytype, runid, sampleid and studyid set takes a very long time. It should take less than a second. Find the bottlenecks and fix them.
    - Bottleneck evidence: backend enrichment was scanning all studies via `AllStudies` before resolving non-study values such as run IDs, canonical sample names, and one-word library types; frontend enrichment also started only the first uncached seqmeta lookup before waiting, creating a request waterfall.
    - Backend study-accession classification now uses indexed `ResolveStudy` and only accepts `KindStudyAccession`, avoiding the full study-cache scan on run/sample/library paths.
    - Frontend result metadata enrichment now starts all uncached seqmeta lookups together and caches each direct lookup before alias priming, avoiding both the waterfall and a parallel alias race.
    - Files touched: `seqmeta/enrich.go`, `seqmeta/enrich_test.go`, `frontend/lib/seqmeta-enrichment.ts`, `frontend/tests/seqmeta-enrichment-parallel.test.ts`.
    - Verification: implementor confirmed the new backend scan and frontend waterfall regressions fail before the fix; reviewer added an alias-race regression, then reran focused seqmeta/frontend tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
    - Reopened on 2026-05-15: user reports rendering result metadata for a result with multiple `seqmeta_*` values still takes tens of seconds. The retry must profile the current path, including live-cache-style calls where feasible without `wa mlwh sync`, and verify the combined rendering/enrichment path is below one second.
    - Retry bottlenecks: one-word library metadata could hit broad sample fallback classifiers before library resolution, and exact library IDs could resolve to a library type before fetching the broader library-type sample page and filtering afterward.
    - Retry fix: classifier order now tries resolved sample/library paths before broad sample scans; exact library IDs and LIMS IDs now use bounded provider/cache-backed exact sample lookups instead of type-wide pages.
    - Retry files touched: `cmd/seqmeta.go`, `mlwh/hierarchy.go`, `seqmeta/client_adapter.go`, `seqmeta/enrich.go`, `seqmeta/enrich_test.go`, `seqmeta/provider_mock_test.go`.
    - Retry verification: deterministic regressions cover combined `seqmeta_librarytype`, `seqmeta_libraryid`, `seqmeta_runid`, `seqmeta_sampleid`, and `seqmeta_studyid` enrichment under one second; reviewer reran focused `seqmeta`, `mlwh`, and `cmd` tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.
- [x] Seqmeta details for sample `7607STDY14643771` still don't list library id `71046409` as the related library, only the `Custom` library type.
    - Root cause matched the study case: stale server-side enrichment rows could mask fresh sample enrichment that now carries `SampleDetail.Libraries` and the `71046409` library instance.
    - Added cache-versioned stale-positive regressions for sample enrichment and forced old entries to refresh on next request.
    - Files touched: `seqmeta/store.go`, `seqmeta/server.go`, `seqmeta/types.go`, `seqmeta/server_enrich_test.go`.
    - Verification: reviewer confirmed the sample regression fails without the fix; focused seqmeta/frontend tests, `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check` pass.
- [x] Determine the typical MLWH hierarchy and display that hierarchy quickly in related data: identify the usual leaf element (lane? sample?) and the usual one-parent chain all the way up to study, making library type a second-class attribute rather than the primary hierarchy node when more specific identifiers exist.
    - The practical display hierarchy is sequencing product/lane (`id_iseq_product` or `id_run` + lane/tag) -> sample -> specific library instance (`library_id` / `id_library_lims`) -> study, with `pipeline_id_lims` kept as library type metadata rather than the hierarchy identity.
    - `LibrariesForStudy` now groups by specific library identifiers as well as type, and sample enrichment copies `SampleDetail.Libraries` into the sample graph when that is where the library identity lives.
    - The reviewer judged the current cache schema sufficient: `library_samples` already stores `library_id` and `id_library_lims` and has study-bounded query support, so no new cache table or sync was needed for this item.
    - Files touched: `mlwh/hierarchy.go`, `mlwh/hierarchy_test.go`, `seqmeta/enrich.go`, `seqmeta/enrich_hierarchy_test.go`.
    - Verification: implementor confirmed the new regressions failed before the fix; reviewer reran focused `mlwh`/`seqmeta` tests plus `timeout 15m make test`, `timeout 10m make lint`, and `git diff --check`.

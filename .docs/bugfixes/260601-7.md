- [x] Make pipeline name, unique, study, sample and requestor permanent options in the search builder box so they’re immediately available without having to add them as new filters. Leave everything else as additional filters you can add via the existing “Add filter” popup. Make sure searching for fields in the popup can’t find the permanent ones. The permanent options should retain the same auto-fill functionality for values as found when adding a filter. Rename the search builder box to simply “search”.
    - Updated `frontend/components/filter-builder.tsx` so the search panel always shows pipeline name, unique, study, sample, and requester controls with the existing suggestion/autofill datalists and compatible query keys.
    - Kept remaining fields behind the existing Add filter popup and filtered the permanent fields out of popup search results.
    - Added regression coverage in `frontend/tests/filter-builder.test.ts`, refreshed dashboard expectations, and updated the requester e2e flow in `frontend/e2e/results.spec.ts`.
    - Verified by implementor and reviewer with focused Vitest/Playwright checks plus `make lint` and `make test`; no `wa mlwh sync` was run.

- [x] Saga interactions have had issues (see previous issues in prior .docs/bugfixes/\*.md files), and now Saga has been improved, per the following comments from its author. Update our go libs and usage to remove any workarounds or unneeded oddities or now inapporpriate fall backs and have us work cleanly with latest saga.

Saga author comments:

> I’ve adjusted the way filters work: you should no longer need to redundantly percent-encode them, and there are some (fairly minimal but hopefully still useful) docs at /api/docs about the available filters for each endpoint as well.
> I also noticed you were getting server errors when querying details about MLWH studies that don’t exist; those should give you a proper 404 now.
	- Removed redundant Saga filter percent-encoding and updated Saga/seqmeta tests to assert the current single-encoded filter contract.
	- Removed obsolete MLWH full-scan and unsupported-filter fallbacks from validation, enrichment, and sample metadata lookup paths.
	- Updated live Saga/seqmeta integration expectations for current advertised filters, run ID precedence, and fast unsupported identifier handling.

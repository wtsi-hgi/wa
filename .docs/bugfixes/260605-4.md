- [x] `.tmp/wa results register --user sb10 --workflow one-off --unique test --match '*.bar' --sample Hek_R1 --sample Hek_R2 --meta 'foo=bar' --meta 'foo=baz' .tmp/outputtest` gives a results page where Hek_R1 and Hek_R2 can be clicked on to bring up the seqmeta details page, but these are mislabelled “seqmeta_sample_name” in the title area, and then a duplicated “sample supplier name” row showing the Hek\* value is shown, when title values are not supposed to be duplicated in the direct metadata section. The 2 clickable values are also displayed with a comma in between, which combined with the padding/margin in the clickable pills, leaves a lots of wasted space and it doesn’t look right. At least just get rid of the comma when the values are clickable in pills, or add some subtle separator (or just a few pixels space) if needed to keep the pills from touching.
    - Reproduced the repeated Hek_R1/Hek_R2 flow with repo-local browser evidence at `.tmp/agent/260605-4-repro-*.png` and `.tmp/agent/260605-4-repro-evidence.json`.
    - Removed literal comma separators between clickable seqmeta pills while preserving comma-separated text for plain repeated metadata.
    - Supplier-backed sample seqmeta dialogs now show the user-facing `Sample` title/aria label and hide the duplicate `Sample supplier name` direct row for the selected Hek value.
    - Kept the other direct sample metadata visible, including canonical sample name, Sanger sample ID, sample LIMS ID, and accession.
    - Added focused Vitest regressions for repeated supplier-backed pills and supplier-backed dialog title/direct metadata behaviour.
    - Verification: focused regressions failed before the fix; post-fix screenshots/evidence show zero separators, `Open Sample details`, title key `Sample`, and no `seqmeta_supplier_name` direct rows. After one transient unrelated Playwright full-gate failure, a follow-up `timeout 20m make test` and final reviewer rerun passed; `timeout 10m make lint` also passed. No `wa mlwh sync` was run.

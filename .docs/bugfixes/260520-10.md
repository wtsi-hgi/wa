- [x] The preview height changer fix did not work. The rounded corner of the preview box is still visible, and the diagonal lines look like a seperate inset floating icon. Make it look like it is the corner of the preview box. It's also not visible in subfolder preview mode, so make sure it is consistent on all preview boxes. The drag marker should be a triangular corner grip, not a square patch.
    - Reworked the resize marker into a flush triangular bottom-right corner grip with no inset padding, visible border, or rounded preview corner showing underneath.
    - Fixed subfolder table/text previews by forcing the shared resize handle size so local preview button sizing rules cannot collapse the grip.
    - Added Playwright geometry/style regressions covering single CSV preview, grid/1-preview-per-row image preview, subfolder image preview, and subfolder table preview.
    - Post-fix evidence: `.tmp/agent/preview-resizer-triangular-single-csv-post-fix.png`, `.tmp/agent/preview-resizer-triangular-grid-image-post-fix.png`, `.tmp/agent/preview-resizer-triangular-subfolder-image-post-fix.png`, and `.tmp/agent/preview-resizer-triangular-subfolder-table-post-fix.png`.
    - Verification: reviewer confirmed `timeout 20m make lint` and `timeout 20m make test` passed with Vitest 407 passed and Playwright 48 passed / 2 pre-existing skipped. No `wa mlwh sync` was run.

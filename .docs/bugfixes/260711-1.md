- [x] Do a quick confirmation with a throw-away database named workflow_automation_mlwh_agent to prove your idea will help (or figure out what does help), then use bugfix to make the change
    - Reproduced on MySQL 8.4.7 with 250,002 indexed rows: the binary-only predicate scanned the whole index and took about 409 ms, while an indexable equality plus binary exact filter read one row in about 0.021 ms and distinguished `CaseProbe` from `caseprobe`.
    - The throwaway `workflow_automation_mlwh_agent` database was dropped after the benchmark.
    - Updated `mlwh/sync_platform_coverage.go` so tracking UPDATE and DELETE operations use the existing index to narrow candidates before applying the binary-exact comparison, binding each ID twice and preserving prepared UPDATE reuse.
    - Added a case-variant behavioral regression in `mlwh/sync_a5_test.go` covering one exact update and one exact deletion without touching the other case variant.
    - Verification: reviewer PASS; `make lint` passed; 15 tracking tests passed with 127 assertions; all-package compile-only and `git diff --check` passed.

- [x] When a folder contains files, but none of them are previewable
                  (unsupported binary file type), do not display any preview-related
                  widgets.
      - Files: `frontend/components/file-browser.tsx`,
            `frontend/tests/file-browser.test.ts`,
            `frontend/e2e/file-browser-layout.spec.ts`
      - Approach: gate file-preview widgets on folder-level previewability instead
            of merely having displayed files, while preserving pagination controls for
            mixed folders that still contain previewable files on other pages.
- [x] If a folder shows “subfolder previews” on it in the file browser when
                  clicked on, and you then click on a subdir, the intial folder becomes
                  deselected and stops displaying “subfolder previews” and other UI
                  elements as expected. However, when you close the subfolder the parent
                  folder does not regain focus, and so the UI does not re-appear. Make
                  parent folder reselection happen and UI re-appearance happen when
                  children are closed. A fix for this bug was attempted in a recent commit
                  for the previous bugfixes md file, but actually made things worse, where
                  not only does the UI not appear when you close the subdir, it doesn’t
                  appear on closing and reopening the initial folder either. It only comes
                  back on closing and repoening the parent of the desired folder, then
                  opening the desired folder again. Make sure you really fix it this time
                  with screenshot taken after clicking closed a subdir.
      - Files: `frontend/components/file-browser.tsx`,
            `frontend/components/result-detail-files.tsx`,
            `frontend/tests/result-detail-files.test.ts`,
            `frontend/e2e/file-browser-layout.spec.ts`
      - Approach: pass the visible parent row through the directory click event so
            collapsing a selected child restores the parent row that was actually
            visible in the compressed tree, while reopening the currently selected
            parent no longer climbs to its own parent.
      - Evidence: focused Vitest and Playwright regressions now cover both the
            collapse-only fallback and the browser close-subdir flow; screenshot
            captured at `frontend/test-results/close-subdir-parent-reselected.png`.
- [ ] Remove the “Showing x preview rows” text on previews.
- [ ] Remove the “click to enlarge” text, but keep the little icon to the left
      of that text, and also indicate clickability of all file format previews
      by changing the mouse pointer on hover.

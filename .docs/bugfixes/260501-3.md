- [x] The `make run` fixture for nf-core/rnaseq result set has 2 csv files in reports subdir. report.csv preview shows all 20 rows, disobeying the preview height setting. It (and all previews for all file types) should truncate at preview height, needing the click to enlarge to show all rows.
	- Threaded preview height through gallery preview rows so CSV previews in the rnaseq reports path truncate consistently.
	- Added a 20-row CSV regression for constrained preview height and preserved enlarged preview behavior.
	- Reviewer confirmed `make test` passes.
- [x] Previews of json files say “syntax-highlighted preview” at the top. There’s no need for such delcarations. Remove.
	- Removed the unnecessary code-preview banner while preserving syntax highlighting, truncation, and enlarge behavior.
	- Added JSON and code-preview regressions proving the declaration is absent.
	- Reviewer confirmed `make test` passes.
- [x] The "Chromium single cell 3 prime v3" library from the `make run` fixture of wtsi/amplicon result set claims to be related to 4 different studies according to seqmeta details view. Is this really what Saga says? I think a library only belongs to a single study.
	- Confirmed Saga returns 100 samples from 4 studies (5631, 5819, 4861, 4931) for library type "Chromium single cell 3 prime v3" - this is correct global behavior for a library TYPE.
	- Fixed frontend to filter studies by the samples returned in the enrichment, not all studies globally.
	- When library type enrichment returns samples belonging to study 5631 only, UI now shows only that study instead of all 4 studies.
	- Added regression test "shows only the single study when all samples belong to one study, even when enrichment returns multiple studies globally".
	- Added test "shows all studies when samples belong to multiple studies" to verify correct behavior when samples span multiple studies.
	- Modified `buildHierarchicalGroups` in seqmeta-badge.tsx to collect unique study IDs from samples and filter the studies list accordingly.
	- Reviewer confirmed `make test` passes.
- [ ] The previous commit brok the display of seqmeta details for samples (eg. WTSI_wEMB10524782 for the nf-core/sarek result set of the `make run` fixture). Now all the direct metadata is missing, and related data is still wrong, with 3 rows to describe the same study and 3 rows for 3 different libraries, and lanes missing. See previous bugfixes md file for related bug and don’t make the same mistakes again. There should be direct metadata about the sample, the name of the single library the sample came from, the name of the single study the library came from, and the identifiers of lane(s) the sample was sequenced on.
- [ ] The previous commit was supposed to make clicks on libraries in the related data section of seqmeta details of a study (for eg. 6568 from nf-core/rnaseq results set of the `make run` fixture) just-in-time query and show a list of samples that came from that library, but nothing happens. The libraries are also supposed to be in a “libraries” sub-section and not have the (incorrect) “library type” and seqmeta_library headings duplicated on every row. Look to the preevious bugfixes md file for related bugs and don’t make the same mistakes again.

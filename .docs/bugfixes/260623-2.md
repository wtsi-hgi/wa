- [x] For the file browser glob filter, it isn’t obvious or natural that for most usages you need to add a `*` at the start and end of what you type. Add highly compact `*` toggle buttons to the left and right of the glob filter field that default to activated so users don’t have to type the stars but can easily disable them if needed.
    - Added compact active-by-default `*` toggles around the file-browser glob input, applying natural text as a contains-style glob while allowing either side to be disabled independently.
    - Persisted wildcard toggle state with saved file-browser filters while preserving legacy saved filters and explicit glob patterns.
    - Added Vitest and Playwright coverage for natural text matching, independent toggles, saved state, result-detail filtering, and compact rendered controls; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] When an output directory contains a symlink to outside the directory, warn and skip instead of error and fail.
    - Updated output-directory scanning and CLI validation so directory symlinks that resolve outside the registered root are skipped with scan warnings instead of aborting registration or rescan.
    - Preserved in-root directory symlink traversal, file symlink behavior, and explicit registered directory-symlink escape rejection.
    - Added scanner, `results register`, and `results rescan` regressions; reviewer verified focused Go tests, `make lint`, and `make test` passed, and no `wa mlwh sync` was run.
- [x] When logged in, have a toggle (default enabled) next to the logged-in user’s username to only show result sets they have access to in latest and search results, instead of showing them with locked status.
    - Added a compact accessible-only toggle beside the signed-in username that defaults enabled for logged-in users and filters inaccessible latest/search rows from the rendered results.
    - Added `show_locked=1` as the opt-out UI state so users can restore locked rows while preserving existing search filters and keeping the flag out of backend search requests; logged-out behavior remains unchanged.
    - Added dashboard, auth-menu, filter-builder, and Playwright regressions; reviewer verified `make lint` and `make test` passed, and no `wa mlwh sync` was run.
- [x] Add support for previewing multichannel (ome) tiff files. Something like [https://github.com/davidvi/tissueviewer](https://github.com/davidvi/tissueviewer), https://academic.oup.com/bioinformatics/article/41/5/btaf246/8120081
    - Added OME-TIFF/TIFF preview support that uses authenticated `HEAD` access checks and server-side Sharp conversion of selected planes to WebP, avoiding full-stack TIFF downloads in the browser.
    - Parsed OME XML metadata to expose channel, Z-slice, and timepoint controls, with first-plane derived thumbnails in grid views and honest plane-selection fallback for non-OME TIFFs.
    - Added helper, proxy-route, preview UI, and result-detail regressions; reviewer verified focused preview tests, `make lint`, and `make test` passed, and no large `.tmp` fixtures or `wa mlwh sync` were used. Known limit: this is single-plane preview, not full deep-zoom/composite tissue viewing.
- [x] `wa mlwh info` should not require WA_MLWH_DSN etc. It should be like `wa results register` where MLWH interactions go via a server process so that database credentials are not given to users. If there isn’t already, `make dev` should bring up a mlwh server that info requires.
    - Updated `wa mlwh info` to prefer `WA_MLWH_SERVER_URL` / `--server` and the active scenario MLWH API port, so normal users can query `wa mlwh serve` without local `WA_MLWH_DSN`, `WA_MLWH_PASSWORD`, cache path, or cache password.
    - Preserved local operator fallback through `WA_MLWH_CACHE_PATH` or direct `WA_MLWH_DSN`, while refreshing help/docs to make server-backed usage the normal path.
    - Updated `run-dev.sh` so development mode treats `WA_MLWH_CACHE_PATH` as a valid local managed MLWH source and starts `wa mlwh serve` from it without running `wa mlwh sync`.

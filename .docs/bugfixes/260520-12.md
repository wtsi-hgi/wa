- [x] It's almost there. I can only clearly see 2 lines now. It's the right size, so get a 3rd line in there by reducing the spacing between lines. The bottom and right edges of the triangle also need to have the same border colour and thickness as the preview box .
    - Kept the triangular marker at the same `24px` size, tightened the diagonal stripe rhythm from `8px` to `6px`, and preserved the token-based grip background.
    - Added matching `border-border/70` right and bottom edges to the triangular marker so the corner joins the preview box edge instead of floating visually.
    - Extended Playwright geometry/style coverage to assert the triangular marker has only right/bottom `1px` border edges while keeping its smaller visible size.
    - Verification: focused Playwright grip checks passed, `timeout 20m make lint` passed, `timeout 20m make test-e2e` passed after one transient navigation failure in a prior full run, and a final `timeout 20m make test` passed with Vitest 407 passed and Playwright 48 passed / 2 pre-existing skipped. No `wa mlwh sync` was run.

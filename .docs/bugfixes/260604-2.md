- [x] Add github action CI to seperately do `make lint` and `make test` for PRs against develop.
    - Added `.github/workflows/ci.yml` for pull requests targeting `develop`, with separate `lint` and `test` jobs that run `make lint` and `make test`.
    - Added `ci_workflow_test.go` to parse the workflow and assert the PR trigger, separate jobs, and exact make commands.
    - Moved `github.com/goccy/go-yaml` into the direct `go.mod` requirements because the new regression test imports it.
    - Verification passed in subagent review: focused workflow test with a temporary red check, `go mod tidy -diff`, `make lint`, and `make test`. No `wa mlwh sync` was run.

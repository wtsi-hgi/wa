- [x] The special channel-selecting UI doesn't appear when enlarging from "subfolder previews" mode. Make sure it always appears regardless of view mode.
    - Confirmed the focused Playwright subfolder-preview repro failed before the fix because the enlarged dialog lacked the Channel control.
    - Updated reusable image thumbnails so OME-TIFF thumbnails keep the plain first-plane thumbnail but mount the existing OME expanded preview controls in the lightbox using the original file proxy URL; non-TIFF images still use the generic image lightbox.
    - Added component coverage for enlarged OME-TIFF thumbnail controls and preserved grid/subfolder first-plane thumbnail URL behavior.
    - Post-fix screenshot: `/home/ubuntu/wa/.tmp/agent/ome-tiff-file-browser-subfolder-controls.png`.
    - Verification passed: focused Vitest, focused/full OME-TIFF Playwright, `make lint`, and `make test`; no `wa mlwh sync` was run.

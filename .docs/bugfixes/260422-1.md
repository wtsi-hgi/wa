- [x] The "browserType.launch: Executable doesn't exist" error remains. Fix it properly and ensure the test actually passes.
    - Approach: added a regression test for environments where `PLAYWRIGHT_BROWSERS_PATH` contains only a `chromium_headless_shell-*` bundle, then updated the Playwright executable resolver to scan both `chromium-*` and `chromium_headless_shell-*` cache directories and their platform-specific executable paths.
    - Files touched: `frontend/tests/playwright-browser.test.ts`, `frontend/lib/playwright-browser.ts`.
    - Verification: `cd frontend && pnpm test -- --run tests/playwright-browser.test.ts` passed; `make test-e2e` from the repo root passed with 6 Playwright tests.

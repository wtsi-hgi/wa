- [x] On a results page where “run details” is showing its “all” button but “metadata” is not becauses there’s only 1 or no metdata keys, the “run details” and metadata” titles are not in alignemnt, and the row of values for each are not in alignement. Make they they keep in alignment with and without “all” buttons and with 1 or 2 lines of metadata.
    - Reserved the same header height in the integrated Run details and Metadata sections so titles and first value rows align whether the `All`/`All metadata` button is present or absent.
    - Added a Playwright regression covering the one-metadata-row and two-line-metadata cases with Run details still showing `All` and Metadata showing no `All metadata` button.
    - Verification passed for focused Playwright regressions, frontend lint, and frontend Vitest. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer. No `wa mlwh sync` was run.
- [x] Once logged in, there is both a circle with the first letter of the logged in user, and the user’s username in the top right. Make this just the circle, with the username only becoming visible in the on-click pop-up.
    - Removed the visible username badge from the closed authenticated auth-menu trigger, leaving a fixed-size circular initial/avatar button with an accessible account label.
    - Kept the username visible inside the click-open account popup and added Vitest plus Playwright regressions proving the closed trigger is avatar-only.
    - Verification passed for focused auth-menu tests, the new Playwright regression, frontend lint, and frontend Vitest. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer. No `wa mlwh sync` was run.
- [x] Currently, search results bring up rows per result set as they were added to the system via `wa results register`, and clicking on a result gives a file browser that only shows the files registered for that result set. In addition to this behaviour, there should be a selectable behaviour (the new default) that immediately shows a dynamically created file browser beneath the search builder box. That file browser provides access to all files in all matching result sets. Actual registered output directories can be annoted in some way (perhaps an “i” info icon that when clicked on pops up similar info as found in the top box of a results page, ie. pipeline name, unique, run details, metadata). If not already suited, improve the `make dev-fixtures` fixtures to demonstrate this new mode by eg. having at least 2 different registered result sets be for different deep sub directories of the same parent pipeline working directory, be registered for the same pipeline name but different other details (eg. different sample), and thus allow searching on pipeline name to give access to output files from both in 1 file browser, or searching on one of the samples to only show output files from one of them in the file browser. The ability to expand and see previews of output directories and other files is still gated by the existing auth mechanism.
    - Added a default combined search file browser under the search builder, with a `Combined files` / `Result rows` selector so the existing row-based view remains available.
    - Reused the existing auth-aware result file browser and preview/proxy paths with per-file result IDs, skipping locked/403 file lists so logged-out users do not gain file access.
    - Added output-directory info popovers with registration details, plus dev fixtures for two `wtsi/galleries-demo` result sets in deep sibling output directories under a shared parent with distinct sample metadata.
    - Added Playwright and Vitest regressions for the combined browser and sample narrowing, and updated fixture count coverage. Verification passed for focused Playwright, frontend lint, frontend Vitest, and focused Go fixture tests. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer. No `wa mlwh sync` was run.

- [x] `make dev` on my real data on a different machine renders the “latest result sets” table differently to `make dev-fixtures` on that same machine. The long paths of dev-fixtures such as `/nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/.docs/results-web/fixtures/files/sibling-gallery-runs/sample-b` display wrapped and there is no horizontal scroll bar on the table. The long paths of my real data in `make dev`, eg. `/lustre/scratch127/gengen/projects_v2/dual_tf/MORF_10/Multiome/sc_analysis_MORF/sc_analysis_30`, render truncated when logged out, with a horizontal scroll bar on the whole table that doesn’t reveal more of the truncated text on scroll. When logged in it displays wrapped. When logged out, `make dev-fixtures` corrupts the display when adding a column, overlapping the directory path text with the value of the new column. This doesn’t happen when logged in. Try to replicate the issues and stop without making code changes if you can’t. If replicated, fix so that adding a column when logged out works correctly, and long paths always wrap.
    - Preserved the logged-out Playwright coverage in `frontend/e2e/real-data-layout-repro.spec.ts` that registers a long real-data-like output directory, confirms the row is locked, toggles the `Operator` column, and asserts no table/cell horizontal overflow, no text overlap, and wrapped output-directory text before and after the column change.
    - Root cause: locked result cells always used an `inline-flex` wrapper. For cells without the lock icon, that wrapper let the output-directory text keep its huge max-content width, bypassing the existing fixed-table and path-wrapping classes that authenticated links used directly.
    - Fixed `frontend/components/results-columns.tsx` so locked cells without the lock icon render the constrained text element directly, while the lock-icon identity cell uses a bounded flex wrapper. Long output-directory paths now wrap in logged-out rows, and adding columns no longer makes the path overlap neighboring cells.
    - Verification passed: `timeout 420 pnpm exec playwright test real-data-layout-repro.spec.ts --reporter=line`, `timeout 300 pnpm exec vitest run tests/results-table.test.ts`, `timeout 420 pnpm exec playwright test search-layout-repro.spec.ts --reporter=line`, `timeout 300 pnpm lint`, `timeout 420 pnpm test`, `timeout 600 make lint`, and `timeout 1200 make test`. No `wa mlwh sync` was run.

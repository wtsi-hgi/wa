- [x] File Browser previews are not displaying in a right hand column of the file box they are for, but undeneath as a seperate box. No seperate box/row for image previews! It must be part of the same row box that shows the file name. In the case of single image preview, one preview should row span all the rows and be displayed in the right hand column.
    - Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts
    - Approach: moved single preview into the selected directory grid with an explicit row span and wrapped grid previews inside the same file row container as their file entry.
- [x] In the Search Builder, selecting “Study” shows a list of study names, instead of those showing up as autocomplete values like for all other fields.
    - Files touched: frontend/components/filter-builder.tsx, frontend/app/(results)/page.tsx, frontend/tests/filter-builder.test.ts, frontend/tests/dashboard.test.ts
    - Approach: removed the study-specific list UI and seeded `study_id` into the shared autocomplete suggestion map so Study uses the same datalist flow as other fields.
- [x] Changing the preview height takes 10s of seconds “Rendering…”. It should be ~instant.
    - Files touched: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts
    - Approach: kept slider drag state local inside the preview height control and only committed the final value at the end of the interaction so the heavy preview tree does not rerender on every intermediate drag step.

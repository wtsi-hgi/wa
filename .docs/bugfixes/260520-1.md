- [x] The last 2 commits were related to working on this item. Use the bugfix skill workflow to continue on this task. Get rid of the design selector and the other designs, keeping the "inline" one, but refining it further: the inline controls take up too much vertical space because a) there's too much wasted empty horizontal space (the file count and subfolder count on the folder are not next to each other, so the subfolder count wastes a whole "column" of space by itself) and the controls themselves are too wide since they show current state to the right instead of underneath. If horizontal usage is minimsed the controls can go next to each other and fit on 1 line. Make it so.
    - Removed the temporary file-browser design selector and the unused classic, sidecar, ribbon, matrix, and deck layout branches, leaving the inline design as the production layout in `frontend/components/file-browser.tsx`.
    - Compacted inline folder metadata so file and subfolder counts share the same wrapped line, and reworked preview mode, file type, height, and pagination controls so their current state sits beneath each label and the controls fit side by side at desktop widths.
    - Updated focused Vitest and Playwright coverage in `frontend/tests/file-browser.test.ts` and `frontend/e2e/file-browser-layout.spec.ts` for selector removal, compact inline controls, and metadata placement.
    - Post-fix evidence: `.tmp/agent/file-browser-inline-compact-postfix.png`. Focused Vitest, focused Playwright, `make lint`, and `git diff --check` passed; broad `make test` only failed in unrelated parallel `seqmeta-lane-filter` shared-state coverage, which passed when rerun alone.

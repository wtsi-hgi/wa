- [x] The "Back to dashboard" button displays correctly on a results page when logged out, but it still hasn't moved when logged in. Use the bugfix worfklow to fix this fully.
    - Moved the authenticated result detail return link onto the same header-action portal path as the logged-out locked state, so it renders in the results auth/account row instead of the summary card.
    - Changed the header action portal to render only once it has found the shared header slot, preventing a hydrated duplicate/fallback link from remaining in the page content.
    - Added Vitest coverage for authenticated detail markup staying free of summary-card return links and for the return-link header portal target, while keeping locked-state regressions intact.
    - Added/preserved Playwright coverage proving authenticated and logged-out result detail states place the link inside `data-results-auth-bar`, outside content boxes, and on the same row as the account/login control.
    - Focused Vitest, authenticated and logged-out Playwright regressions, `make test-frontend`, and `make test-e2e` passed. Broad `make lint` still hit unrelated existing Prettier formatting in `.docs/bugfixes/260522-1.md`; broad `make test` still hit unrelated unchanged Go TLS-default tests.

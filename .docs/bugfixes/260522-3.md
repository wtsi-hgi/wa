- [x] Move the “Back to dashboard” button out of the box it is currently in and have it instead display on the same line as the login button (aligned left).
  - Added a normal-flow left action slot to the results auth header in `frontend/app/(results)/layout.tsx`.
  - Moved the locked result detail “Back to dashboard” link out of the locked content box via `frontend/app/(results)/results/[id]/return-link-header-action.tsx`, preserving the row shared with the login/auth control.
  - Added Vitest and Playwright regressions proving the link is not inside the locked section and is on the same header row as the login button.
  - Focused frontend unit, focused Playwright, `make test-frontend`, and `make test-e2e` passed. Broad `make lint` and `make test` still hit unrelated pre-existing failures in `.docs/bugfixes/260522-1.md` Prettier formatting and unchanged Go TLS-default tests.
- [ ] The logout button still doesn’t work immediately, despite attempt in previous bugfixes md file. There is a delayed effect of perhaps 10s of seconds before the logout works. Refreshing the page manually doesn’t speed this delay up. Make it instant.

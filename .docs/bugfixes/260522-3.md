- [x] Move the “Back to dashboard” button out of the box it is currently in
      and have it instead display on the same line as the login button
      (aligned left).
    - Added a normal-flow left action slot to the results auth header in `frontend/app/(results)/layout.tsx`.
    - Moved the locked result detail “Back to dashboard” link out of the locked
      content box via
      `frontend/app/(results)/results/[id]/return-link-header-action.tsx`,
      preserving the row shared with the login/auth control.
    - Added Vitest and Playwright regressions proving the link is not inside the
      locked section and is on the same header row as the login button.
    - Focused frontend unit, focused Playwright, `make test-frontend`, and
      `make test-e2e` passed. Broad `make lint` and `make test` still hit
      unrelated pre-existing failures in `.docs/bugfixes/260522-1.md` Prettier
      formatting and unchanged Go TLS-default tests.
- [x] The logout button still doesn’t work immediately, despite attempt in
      previous bugfixes md file. There is a delayed effect of perhaps 10s of
      seconds before the logout works. Refreshing the page manually doesn’t
      speed this delay up. Make it instant.
    - Changed `frontend/app/api/auth/logout/route.ts` so the browser-visible
      logout route expires `wa_results_jwt` and returns the anonymous session
      immediately, while the backend logout call runs best-effort in the
      background.
    - Added integration coverage in `frontend/tests/integration/auth-api.test.ts`
      for a backend logout call that never responds, proving the browser still
      receives cookie expiry.
    - Added Playwright coverage in `frontend/e2e/logout-delay-repro.spec.ts`
      proving logout removes the auth cookie and locks a protected result detail
      page immediately.
    - Focused auth tests, focused Playwright, `make test-frontend`, and
      `make test-e2e` passed. Broad `make lint` still hit unrelated existing
      Prettier formatting in `.docs/bugfixes/260522-1.md`; broad `make test`
      still hit unrelated unchanged Go TLS-default tests.

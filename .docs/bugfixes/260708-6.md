- [x] get rid of the hidden cli aliases, but make sure the OpenAPI docs always mention that ega id is an alias of accession _number (or which ever one it is an alias of), not just for this endpoint, but everywhere.
  - Removed the hidden `ega_id` and `irods_cram_path` export-column aliases; selecting them now fails with the normal unknown-column error and the canonical `accession_number` / `irods_path` choices.
  - Centralised OpenAPI field-description augmentation so every `accession_number` and `study_accession_number` schema property explains that EGA ID / EGA accession terminology maps to the canonical field.
- [x] You also need to get the tests to work. They were working earlier in this thread. Fix underlying issues or improve the tests if they're just flakey, but don't bypass. We must still have a test that follows the spirit of what the flakey test was trying to prove, even if you have to reimplement it.
  - Changed `run-dev.sh` to build into a per-process `.tmp/wa-run-dev-$$` binary and remove it during cleanup, avoiding cross-test deletion/contention on the shared `.tmp/wa` path while preserving build-from-root and cleanup coverage.
  - Verified focused export/OpenAPI/run-dev tests, `make lint-go`, and `make test-go`; reviewer reran the same gates and passed.

- [x] Recent commits for the previous bugfixes md file have tests use
      `.env.test`, but that says not to put `SAGA_API_TOKEN` in there. How,
      then, are automated integration tests using real SAGA supposed to work?
      There ought to be some optional ones that skip without the env var, but
      work with it. Add such tests if they don't exist and improve the env system
      to enable them if token supplied.
    - Updated `scripts/wa-env.sh` so test mode can optionally import
      `SAGA_API_TOKEN` from gitignored `.env.dev` while keeping `.env.test`
      token-free.
    - Added regression coverage for token import and clean no-token skips in
      `cmd/run_dev_modes_test.go`, `saga/integration_test.go`, and
      `seqmeta/integration_test.go`, and documented the flow in
      `DEVELOPING.md` and `.env.test`.
- [x] Running `make dev` or prod or `wa results server --db ...` directly with
      the suggested DSN in WA_RESULTS_DB_PATH env file or directly supplied puts
      the database password on the command line for anyone to see in the list of
      processes. Come up with a solution that allows setting the password or whole
      DSN as an env var that does not appear on the command line, and actually
      reject password being supplied on the command line.
    - `wa results serve` now resolves `WA_RESULTS_DB_PATH` and optional
      `WA_RESULTS_DB_PASSWORD` at runtime, so `run-dev.sh` no longer passes DB
      config on the child process command line.
    - Added regressions covering command-line password rejection, env-based
      password injection, env DSN fallback, and the absence of `--db` on the
      spawned results-server command line.
- [x] Having run `make dev` or prod, client CLI command like `wa results
register` should automatically be able to connect to the server that was
      brought up. Currently the `--server` arg defaults to
      `WA_RESULTS_BACKEND_URL`, but this is not something we set in `.env*`
      files. Come up with a better solution so that this will "just work"
      neatly, ideally without duplicating the port number in the env file. If
      you end up getting rid of `WA_RESULTS_BACKEND_URL` because you calculate
      it from the relevant `WA_*_PORT` env var instead, update docs as
      appropriate. Do not do any backwards compatability or mention the old way
      of doing things. - `wa results ...` now derives its default `--server` URL from `WA_ENV`
      plus the active `WA_*_RESULTS_PORT`, so dev/prod CLI commands work under
      `scripts/wa-env.sh` without duplicating backend URLs in the env files. - Added regression coverage in `cmd/results_get_test.go` for both the URL
      derivation helper and an actual `results get` call with no `--server`. - Updated `README.md`, `DEVELOPING.md`, `.env.dev.example`, and
      `.env.prod.example` to document the port-derived CLI behaviour.
- [x] `wa results register` should should have dedicated args for the main
      seqmeta keys we are able to validate and search for via saga. If saga isn't
      actually available, these should still get stored as seqmeta metadata; the
      dedicated args are just convieiences for setting the correct `--meta`
      values, and so need to generate error if user supplies the same info both
      ways. - Added `--seqmeta-runid`, `--seqmeta-studyid`, `--seqmeta-sampleid`, and
      `--seqmeta-librarytype` to `wa results register` as convenience shorthands
      for the equivalent `--meta seqmeta_*=...` entries. - Added regressions for successful metadata population and duplicate-key
      rejection when a dedicated flag and `--meta` both provide the same
      seqmeta field.
- [x] File formats that are readable and displayable line-by-line, such as tsv
      files, should be previewable regardless of how large they are, since we can
      just read only as many lines as will approx fit in to the configured (or
      hardcoded?) preview size limit. Just give an indicator in the preview that
                  it is truncated at the end.
            - Added line-limited backend previews plus truncation metadata so readable
                  text formats can be previewed even when the full file is too large.
            - Updated the frontend proxy and preview UI to request line-limited previews
                  for readable formats, including `.gz` variants, and show a visible
                  truncation indicator.
- [x] Enlarging a tsv.gz of ~609kb and 81979 rows made the browser very slow
      and sluggish, with delay opening the full-size preview (with no indication
      anything was happening), and then subsequent interactions being too slow. I
      think this amount of data isn't really too much for a browser to handle, but
      the table in react is too slow to render? See if there's a way to speed up
      the render dramatically. If not, paginate such tables at 1000 rows per page
                  so react doesn't fall over.
            - Enlarged CSV and TSV previews now show an explicit loading state before
                  the full table mounts, and large expanded tables are paginated at 1000
                  rows per page so they stay responsive.
            - Result-table links now use full document navigation, which removed the
                  flaky blank-shell transition that started showing up in the affected
                  Playwright coverage after the preview work.
- [ ] In seqmeta details for a seqmeta_library, clicking a "Copy" button in
      the Samples section changes all Copy buttons in the section to "Copied",
      instead of just the one you clicked on.

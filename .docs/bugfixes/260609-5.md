- [x] Now the user gets "Error: authentication failed" when entering their correct ldap password that works when logging in to the web UI with the same username. Fix with the bugfix skill workflow.
    - Fixed `wa results register --user <ldap-user>` so password/JWT authentication uses the registration requester instead of defaulting to the local OS username when no readable server token is available.
    - Preserved readable owner-token login precedence, blank-requester behavior, TLS verification, certificate env fallback, and server-side MLWH lookup behavior.
    - Added an HTTPS register auth regression proving the JWT login posts the CLI requester username and would fail with `authentication failed` under the old behavior.
    - Verification: reviewer passed focused auth/register/dev-cert regressions, full `timeout 20m make test`, and full `timeout 10m make lint`.

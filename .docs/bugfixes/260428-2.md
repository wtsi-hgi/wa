- [x] “Seqmeta details” has a “Resolution” that takes 10s of seconds Rendering and “Looking up x”. Why does it take so long? We should be able to get ~instant lookups of related metadata.
	- Primed related seqmeta identifiers from the first successful enrichment so related rows resolve from cache instead of repeating slow lookups.
	- Reused the shared enrichment helper in the client details component and added regression coverage for cache reuse and stale lookup handling.
- [x] Improve the fixtures used by `make run` so that the example result sets are more interesting. Multiple of the same file type files per directory, an example of files in a series of nested directories, an example of files in sibling directories, examples of input, output and pipeline files. Provide enough that all aspects of the system can be demonstrated. For example, at least one of the result sets should have a folder with more than 100 image files, so that file preview pagination can be tested.
	- Expanded the seeded rnaseq, sarek, and amplicon result sets with nested and sibling directories, repeated file types, and explicit input, output, and pipeline files.
	- Added regression coverage proving `run-dev.sh` seeds a >100 image gallery for preview pagination and exercises the richer fixtures through the Playwright harness.
- [ ] The “File Browser” page currently breaks the view of files in to seperate “Folders”, “Files” and “Preview” sections. Instead it should be a single “File Browser” pane with listing by rows of folders that you click on to expand or collapse their contents beneath; it should be a tree view. Folders that only have a single directory child should be combined in to one entry. Up to 100 files (paginated) in the directory you expand should be displayed under the directory. The file listing should be split in to 2 columns, like Windows file explorer with image previews turned on to the right. By default the right column will span all rows and show the single preview. There should be a single toggle button to switch between that view and 1 preview per row, affected by the preview height slider.

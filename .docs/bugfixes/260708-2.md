- [x] Ok, I've done that and it works for me now. Run the full integration test and fix any issues with the bugfix skill.
  - Fixed the run-dev MLWH stdout race, live MLWH schema drift, MySQL query-plan regressions, run-scoped iRODS lookup semantics, merged-row export rendering, and live D1a flagship test brittleness discovered by the integration suite.
  - Kept the live cold-sync perf test changes: when `WA_MLWH_CACHE_PATH` is a MySQL DSN, the perf test now uses a unique throwaway MySQL cache database instead of a large temporary SQLite cache, and never syncs into the configured cache DB itself.
  - Deferred the explicitly gated perf run `WA_LIVE_MLWH_TESTS=1 MLWH_SYNC_PERF_TEST=1 make test`: the focused perf test used throwaway MySQL but timed out after 90 minutes while inserting `seq_product_irods_locations`; the test changes are retained while the remaining cold-sync performance investigation is deferred.
  - Verified non-perf gates: `make lint-go`, `make test-go`, and `WA_LIVE_MLWH_TESTS=1 make test-go` passed. Reviewer also passed focused merged/run/sample-crams/sync, live D1a flagship, MySQL query-path, and run-aggregation plan checks.

- [x] Using `wa mlwh` commands can show eg. "Cache synced Jul 14" at the bottom. Now that we sync every 30mins, it should show the sync to minute accuracy in the user's timezone with any summer adjustment.
    - Human-readable `wa mlwh info` output now parses the UTC sync instant, converts it through the user's local timezone rules, and renders the date, minute, and active zone abbreviation (for example, `Jul 14 03:10 BST`); JSON/API timestamps remain unchanged.
    - Added a Europe/London summer-time rendering regression and preserved the single-footer behaviour from 260629-10. Implementor and reviewer both verified focused tests, `timeout 30m make lint`, `timeout 30m make test`, and `git diff --check`; reviewer PASS.
- [x] The mlwh endpoints seem to expose fine-grained date information, such that the MCP server at ~/llm-knowledge-base that uses it answers questions like:
      [...answer...]
      Cache caveat: PacBio product metrics were synced through 2026-07-13T02:10:21Z; study metadata through 2026-07-10T09:30:57Z.
      However, this is misleading, since it makes it look like the study info is out of date, when in reality all tables of info will have been synced within about a minute of each other. I suppose in this case the source study metadata table just hadn't had any changes in a while, but we are still current.
      Use the /bugfix skill workflow to improve our CLI output. Then consider if the MCP server is just making poor use of our outputs, or if we're not outputting sync time appropriately / clearly enough. Also /bugfix any such issues on our end, and write up a bugfix checklist item in the ~/llm-knowledge-base, written from their perspective, if they're doing something wrong.
    - Clarified the maintained OpenAPI schema without changing JSON names, types, values, or UTC formatting: `high_water` is a sync-mode-specific source-progress watermark (source change, full-refresh snapshot, or empty depending on mode) and can remain old when source data is unchanged; `last_run` is the per-table cache sync/refresh timestamp for cache-currency and “as of” caveats.
    - Added a public OpenAPI regression covering those distinctions. After one reviewer rejected an over-broad source-mutation definition, a fresh implementor corrected the mode-specific semantics and a fresh reviewer passed the result; focused tests, `timeout 30m make lint`, `timeout 30m make test` (including 559 Vitest and 107 Playwright passes), and `git diff --check` passed.
    - Confirmed the downstream MCP guidance also needs correction: it exposes both fields but does not tell the model to use `last_run`, which explains the misleading “synced through” wording. Added the requested downstream-perspective unchecked item at `~/llm-knowledge-base/.docs/bugfixes/260714-1.md`.
- [x] If there's code that produces an endpoint-reference file, perhaps that should be produced in repo root?
      .docs/mcp/api-reference.md and other docs can be altered as part of that next item to say they are historical, to avoid this confusion in future.
      Just make sure that once it root it is clearly mlwh-specific. Ie. don't name it generically api-reference.md
    - Added the current generated/no-drift `MLWH_API_REFERENCE.md` at repository root, linked it from the root README, and retargeted the refresh helper and golden test to that explicitly MLWH-specific artifact. The current reference now carries the corrected freshness semantics from the Registry.
    - Retained `.docs/mcp/api-reference.md` only as a labelled historical snapshot and added `.docs/mcp/README.md` to mark the directory's planning/implementation records as historical while pointing readers to the root reference and live `GET /openapi.json`; no historical spec, phase, prompt, or glossary content was rewritten.
    - Implementor and reviewer verified byte-identical regeneration, focused navigation/no-drift/OpenAPI tests, `timeout 30m make lint`, `timeout 30m make test` (559 Vitest and 107 Playwright passes), and `git diff --check`; reviewer PASS. The API patch-version correction is intentionally queued as the next item.
- [ ] Did one of your previous commits bump the API version? Maybe by a minor or patch revision since only a doc change? Or were no API changes made at all?
      Did I miss it, or does MLWH_API_REFERENCE.md not mention the API version? Perhaps it should? This can be fixed as part of the next bugfix item.

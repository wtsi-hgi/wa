- [x] The “search builder” box is visually inside an outer box. This needlessly wastes padding/magin space. Get rid of (visually at least) the outer box.
    - Removed the dashboard section wrapper so the `FilterBuilder` renders directly in the page `main`, eliminating the extra visible outer panel while preserving the search builder itself.
    - Added Vitest and Playwright regressions proving the search builder has no extra dashboard panel ancestor, including before/after screenshot evidence in `.tmp/agent/`.
    - Verification passed for the focused dashboard tests, focused browser regression, `make test-frontend`, and focused login/search e2e checks. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer.
- [x] Tools in the file browser (preview mode and file types) are in a box panel, but it doesn’t read enough like a panel. Make the panel padding equal all around and have its background be greater contrast to make the panel more obvious.
    - Updated the file-browser folder controls surface to use equal 8px padding, an opaque higher-contrast mixed background, a normal border, and a lighter shadow so preview mode and file type tools read as a distinct panel.
    - Added a Playwright regression that opens the seeded `nf-core/rnaseq` gallery, captures panel evidence, and asserts equal padding, opaque background, and stronger contrast against the selected directory row.
    - Verification passed for focused file-browser Playwright/Vitest checks, `pnpm lint`, `make test-frontend`, and the new e2e test. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer.
- [x] In the file browser there is too much indenting for sub directory names, given that subdirectory boxes are already naturally indented simply by being inside their parent. Try having no additional indent.
    - Removed the depth-derived inline left padding from directory buttons so nested rows keep their natural parent-contained offset without adding a second name/button indent.
    - Updated unit and Playwright regressions to require structural depth and natural nested row offset while asserting parent/child button padding and name inset remain equal.
    - Verification passed for focused file-browser and result-detail unit tests, the full file-browser layout Playwright spec, `pnpm lint`, `make test-frontend`, and `make test-e2e`. Root `make lint` and `make test` still fail on existing unrelated issues noted by the reviewer.
- [ ] When there are only a few seqmeta and non-seqmeta metadata key pairs on a result, show them all in the metadata section at the top of the results page. Only hide and provide the “all” button if it would be more than 2 lines worth of metadata (would trigger the vertical scroll bar).

- [x] seqmeta_sample_name in the title should not have been renamed to Sample. It should have become something like seqmeta_sample_supplier_name, ie. to explain what kind of value Hek_R1 is (which mlwh column it comes from). It ought to be the case that even though at register time we supply the value via generic `--sample`, at search time we search via generic "Sample" field, and on the metadata value we click on is under "Sample", we have actually stored the value under a metadata key like seqmeta_sample_supplier_name that tells us what mlwh column the value was actually in. And make similar changes to other types like study, library, run etc. if they can also be stored by providing different column values.
    - Diagnosed the existing contract with repo-local evidence at `.tmp/agent/260605-5-repro/`: Hek_R1/Hek_R2 supplier-name inputs were stored only under generic `sample` plus canonical `seqmeta_name`, losing the MLWH source column.
    - Registration now preserves sample source kinds under source-specific seqmeta keys such as `seqmeta_supplier_name`, `seqmeta_sanger_sample_id`, `seqmeta_id_sample_lims`, `seqmeta_accession_number`, `seqmeta_uuid_sample_lims`, and `seqmeta_donor_id`, while retaining canonical `seqmeta_name`.
    - Study accession/UUID/name inputs now preserve source-specific keys such as `seqmeta_study_accession`, `seqmeta_uuid_study_lims`, and `seqmeta_study_name`, while retaining canonical `seqmeta_id_study_lims`; library and run source-specific behaviours were confirmed and guarded.
    - Generic `Sample`/`Study` display and search still work over the source-specific keys, while seqmeta dialog source labels now show the actual source key such as `seqmeta_supplier_name`.
    - Added backend registration/search/validation regressions plus frontend metadata/dialog regressions; post-fix evidence at `.tmp/agent/260605-5-postfix-evidence.json` records stored keys, generic search guards, and representative study accession behaviour.
    - Verification: implementor and reviewer confirmed focused regressions fail without the fix; `timeout 10m make lint` and a clean rerun of `timeout 20m make test` passed. No `wa mlwh sync` was run.

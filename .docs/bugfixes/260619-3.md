- [x] Use the same system to make the “result sets” view table show project column by default instead of pipeline name, with the value defaulting to pipeline name. Try to refactor the code so that the result sets table doesn’t become different to the “latest result sets” table and vice versa if code changes are made to one of them, ie they should share as much of the code as possible.
    - Moved default result-table column visibility next to the shared result column definitions so Latest result sets and Search results use the same Project-first defaults.
    - Search result-set tables now show Project first and Unique second by default, with Pipeline Name still available through Columns and Project values using the shared `metadata.project` trim/fallback behavior.
    - Updated results-table regression coverage to prove both table modes share Project-first defaults, trim project metadata, fall back to pipeline name, and keep Pipeline Name optional.
    - Reviewer confirmed the regression fails under a temporary reverse patch and passes after restore; `make lint` and `make test` passed. No `wa mlwh sync` was run.

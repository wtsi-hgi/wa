- [x] The search field “output directory prefix” no longer makes sense since values can be any substring. Rename the field to “output directory” and remove any special-case code that previously enabled prefix matching, in favour of the general code that enables substring matching for all fields.
    - Renamed the canonical search/filter field to `output_directory` and updated UI, autocomplete, applied filters, tests, and CLI help to say “Output directory”.
    - Removed the prefix-specific store matching helper/path so output-directory matching uses the same substring helper as other scalar fields.
    - Preserved legacy inbound `output_dir_prefix` compatibility for old URLs/API callers and the hidden deprecated CLI flag, while new output uses `output_directory`. `wa mlwh sync` was not run.

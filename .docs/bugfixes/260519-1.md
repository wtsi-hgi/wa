- [x] The top “result detail” section of results page is still too large. Search for available shadcn/ui components to use that can greatly compact all the information in to an attractive consistently sized header block, hiding or minimising most of the info so we can focus on the file browser below. Let less needed info be accessible on scroll/click/hover as appropriate. For example, the metadata box being one key val per row wastes a lot of both vertical and horizonal space.
    - Compacted the result detail header so identity, key run fields, registered file counts, and metadata fit in a consistent shallow summary block above the file browser.
    - Moved full registration and full metadata details behind shadcn-style popovers, keeping important fields inline and preserving seqmeta badge/dialog/copy/filter behavior.
    - Reworked metadata from tall one-key-per-row cards into dense chips, with the full popover wrapping long keys and values so less-needed data stays accessible without pushing down the file browser.
    - Added focused Vitest and Playwright regressions for the compact metadata strip and header geometry; browser evidence shows the summary at 239px and File Browser y=291, down from 532px and y=596 in the repro.
    - Files touched: `frontend/app/(results)/results/[id]/page-content.tsx`, `frontend/components/result-registration-summary.tsx`, `frontend/components/result-metadata.tsx`, `frontend/e2e/results.spec.ts`, and focused frontend tests.
    - Verification: focused Vitest and Playwright checks passed; `pnpm lint`, `make test`, `make lint`, and `git diff --check` passed. No MLWH sync was run.
- [x] seqmeta details for a sample shows direct metadata of sample supplier name, but this has no filter button. Sample supplier name should be filterable just like any other property of a sample. Sample lims id has a filter button, but the filter doesn’t work. All direct metdata of all entities, when filtered on, should be able to find at least the result set of the thing we clicked on, since there is a proven relationship.
    - Added supplier-name and sample-LIMS direct metadata filter links in seqmeta details, including title-filter coverage for suppressed duplicate rows.
    - Added direct sample metadata search kinds for supplier name, sample LIMS ID, and Sanger sample ID so result filters expand through MLWH using the correct proven relationship.
    - Kept raw direct metadata values exact in results search, while only MLWH-expanded canonical sample names are added to broad sample alias matching; added lookalike/no-relation regressions so filters cannot fake success by matching an unrelated field with the same value.
    - Preserved legacy `sample=` behavior for true sample-name filters while making `seqmeta_id_sample_lims`, `seqmeta_sanger_sample_id`, and `seqmeta_supplier_name` first-class filter keys.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, focused seqmeta frontend tests, `results/server.go`, `results/types.go`, `results/server_test.go`, `mlwh/hierarchy.go`, and `mlwh/hierarchy_test.go`.
    - Verification: focused frontend and Go regressions passed; `make lint` and `git diff --check` passed. `make test` passed earlier in the cycle, then later reproduced an unrelated cold-start Playwright performance-budget flake in `seqmeta-result-detail-performance.spec.ts` where the first repeat exceeded 1s and warmed repeats passed. No MLWH sync was run.

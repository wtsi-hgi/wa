- [x] I don't know if it's still an issue on this branch, or even a code problem at all, but somehow my cache (see .env.development.local) has gotten broken since I did a sync using develop branch code. Now every time I do `make dev` it thinks `MLWH cache at hgi_wf_test_admin@tcp(hgi-workflow-automation-test-db.internal.sanger.ac.uk:3324)/workflow_automation_mlwh_sb10;?parseTime=true is empty; running "wa mlwh sync" before studies readiness (a cold load can take a while).`, but letting the sync run and it seeming to start fine leaves clients with "the MLWH cache is not available yet". I've just killed a sync part way through, so ignoring what additional "damage" that may have done, investigate the code/ my database and fix. You can drop my db and cold sync if that's the only solution. Use the bugfix skill workflow to fix if it's a code issue.
  - Diagnosed the dev MySQL cache as a schema-v12/partial-cold-sync state after current code migrated it to schema v13 and deferred sparse indexes.
  - Added the sparse product-metrics product-id index and repair step before iRODS sync enrichment, then completed the cold sync on the configured dev cache.
  - Refresh MySQL optimizer stats after sparse index recovery, warm the read pool, batch freshness reads, and overlap independent StudyOverview/StatusBreakdown reads so big-study user endpoints stay web-responsive.
  - Deferred the older 90-minute sync perf tieout as documented earlier; the real MySQL user-query performance gate now passes.

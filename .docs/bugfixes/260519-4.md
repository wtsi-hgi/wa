- [x] The file browser, viewing expanded folders, can have various ui elements along with the file/sub folder listings. It is functional, but I think it could look better somehow. Search through shadcn/ui components and think outside the box and be more creative design-wise to find a way to make the file browser more beautiful, compact and easier to visually parse what’s going on (eg. the preview modes etc. ui buttons being visually so similar to file/dir rows isn’t ideal). Come up with 5 distinct designs to improve the file browser, along with a temporary design selector to on-the-fly switch between the current design and the 5 new designs. Once a design has been selected we will want to remove all trace of the design selection code and the other designs, so don’t put too much effort in to the selection code or have the designs directly rely on that code.
    - Added a temporary file-browser design selector with the current design plus five layout-level alternatives: inline nameplate, utility sidecar, breadcrumb ribbon, file matrix, and preview deck.
    - Replaced the initial subtle restyling variants so preview modes, file types, preview height, and pagination move into distinct layout regions instead of staying as a row among file/subfolder listings.
    - Kept the selector isolated in `frontend/components/file-browser.tsx` so a chosen design can be kept later without depending on the temporary selector.
    - Added focused Vitest and Playwright coverage for the selector, live switching, distinct control-surface attributes, and preservation of file visibility/controls.
    - Post-fix evidence: `.tmp/agent/file-browser-design-selector-classic.png`, `.tmp/agent/file-browser-design-inline.png`, `.tmp/agent/file-browser-design-sidecar.png`, and `.tmp/agent/file-browser-design-deck.png`.
    - Verification: focused file-browser Vitest and Playwright checks, `make lint`, `make test`, and `git diff --check` passed after the replacement. No `wa mlwh sync` was run.

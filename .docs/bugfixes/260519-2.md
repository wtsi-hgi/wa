- [x] The top “result detail” section of results page should be improved: remove the “RESULT DETAIL” text. Remove the isolated and redundant display of total file size. Put the Unique value next to the pipeline name as part of the title, have the visible run details be just the update date, requester and operator, and put everything else hidden under the all details popup. Make the Metadata section more compact by only displaying the main seqmeta keys but with the short entitiy names used when filtering (eg. “Library” instead of “seqmeta_library_id”), and up to 3 non-seqmeta key val pairs if there’s no seqmeta metadata, with the current “all metadata” pop up remaining the same, showing the real keys.
    - Result detail headers now title the page as pipeline name plus displayed Unique value, with no `Result detail` eyebrow and no isolated file-size summary block.
    - Inline run details are limited to `Last updated`, `Requester`, and `Operator`; pipeline version, displayed Unique, raw run key, file counts, total size, output directory, and command remain available from the `All details` popover.
    - Integrated metadata now shows seqmeta entries first with compact filter-style entity labels such as `Study`, `Sample`, `Library`, and `Run`; if no seqmeta metadata exists it falls back to the first three non-seqmeta pairs. The `All metadata` popover continues to show raw metadata keys.
    - Converted the repro Playwright file into a behavioral regression and added focused component/hydration regressions covering the inline detail set, compact metadata labels, non-seqmeta fallback, and raw-key metadata popover.
    - Review-fix cycle 2: the result detail route loading fallback no longer renders the removed `Result detail` eyebrow, with a regression added to the loading-shell test.
    - Post-fix evidence: `.tmp/agent/result-detail-header-postfix.png` and `.tmp/agent/result-detail-header-postfix-dom.json`.
- [ ] A recent change to have seqmeta sample names be called names in the frontend, disambiguating them from sample ids, has ended up calling them “seqmeta_name”, which is not precise enough. It should be “seqmeta_sample_name” or similar.
- [ ] A recent change allows filtering on sample supplier name, but clicking the filter button on of the direct metadata options (sample id, supplier name or lims id) results in “Rendering…” for 10s of seconds before. Fix it so they all work as quickly as the title filter button.

- [x] Want an env var to define a default `--server` for all `wa results` subcmds. Likewise for `--seqmeta-url`.
	- Updated `cmd/results.go` so shared results commands prefer `WA_RESULTS_BACKEND_URL` for `--server`, with `WA_SERVER_URL` kept as a fallback.
	- Added regression coverage in `cmd/results_get_test.go`; confirmed existing `--seqmeta-url` env fallback coverage remained valid.
- [x] Results dashboard is filled with explanatory instruction comments, instead of something suited for end-users. Eg. from the results page “Inspect the selected asset inline without leaving the result detail page.” line just shouldn’t be there at all. It’s an instruciton/explanation for the implementor of the feature, not something a user needs to see. There are many examples of this throughout the UI.
	- Removed the preview-pane instructional subcopy from `frontend/components/file-preview.tsx` and deleted the stats-card explanatory descriptions from `frontend/components/stats-cards.tsx`.
	- Added regression coverage in `frontend/tests/file-preview.test.ts` and `frontend/tests/dashboard.test.ts` to keep those internal phrases out of the results detail and dashboard UI.
- [x] Results dashboard Daily registrations “Last 30 days of result activity” box has 30 meaningless, possibly placeholder bar chart column things? Not actually showing any real result activity.
	- Replaced the placeholder-like shell bars with data-backed daily activity rendering and a real empty state in `frontend/components/daily-chart-panel.tsx`, `frontend/components/daily-chart-shell.tsx`, and `frontend/components/daily-chart.tsx`.
	- Added regressions in `frontend/tests/daily-chart.test.ts` and `frontend/tests/dashboard.test.ts` so zero-activity periods no longer show fake activity.
- [x] No buttons in the results dashboard seem to be clickable, except for links to latest result sets and “Download file”. So can’t add filters are do something with columns or collapse file browser tree.
	- Fixed hydration mismatches caused by locale-dependent date rendering in `frontend/lib/utils.ts`, `frontend/components/results-columns.tsx`, and `frontend/components/file-preview.tsx`, which restored client-side button handlers.
	- Added hydration regressions in `frontend/tests/dashboard.test.ts` and `frontend/tests/result-detail-hydration.test.ts` covering filter, column-toggle, and file-browser interactions.
- [x] Viewing a page and not interacting with it results in reload flashes which I think briefly show the top of the page then bring back to prior scroll position.
	- Stopped redundant seqmeta cookie rewrites by making identical cache writes a no-op in `frontend/lib/seqmeta-cache-core.ts`, which removed the idle refresh path on result detail pages.
	- Added regression coverage in `frontend/tests/seqmeta-badge.test.ts` for mount-time cache priming with an already-populated cookie.
- [x] File browser tree takes too much horizontal space with deeply nested directories. Directories wthat only have a single child directory should be merged with the child.
	- Collapsed single-child directory chains in `frontend/components/file-browser.tsx` so linear paths render as one folder segment without breaking expand/collapse state.
	- Added regression coverage in `frontend/tests/file-browser.test.ts` for merged-path display and toggling.
- [x] json file preview claims to be “Loading preview...” but it never does.
	- Fixed `frontend/components/result-detail-files.tsx` so re-selecting the already active file no longer resets preview state to loading without triggering a new fetch.
	- Added regression coverage in `frontend/tests/result-detail-files.test.ts` and `frontend/e2e/results.spec.ts` for settled JSON preview rendering and same-file re-selection.
- [ ] seqmeta_sampleid not showing additional info in the individul result view (“enrichment unavailable” for SANG205). Not sure if this is a failure of the feature or of the test setup when I have a real SAGA_API_TOKEN.
	- Investigated without code changes: `logs/seqmeta.log` shows the seqmeta service is skipped when `SAGA_API_TOKEN` is unset, which makes the current “enrichment unavailable” fallback expected in this environment.
	- Focused seqmeta frontend tests still pass; live verification still needs `SAGA_API_TOKEN` exported before starting `run-dev.sh`.
- [x] `make lint` returning frontend warnings. Fix as warnings suggest.
	- Replaced raw preview `<img>` tags with `next/image` in `frontend/components/file-preview.tsx`, keeping runtime preview behavior while satisfying the lint rule.
	- Verified `pnpm lint`, focused preview tests, and the repository `make lint` target all run cleanly.
	- Verified `CGO_ENABLED=0 go test -tags netgo --count 1 ./...` and `CGO_ENABLED=0 go build ./...` both succeed, so the repo does not require forcing CGo on.
	- Removed `CGO_ENABLED=1` from the top-level `Makefile` Go test target and from the Go test examples in `DEVELOPING.md`.
- [x] I think our go commands don’t need CGO_ENABLED=1. Update docs and Makefile it true.
- [x] cmd/results.go gets default server URL from WA_SERVER_URL. Should be WA_RESULTS_BACKEND_URL?
	- Resolved by the earlier `cmd/results.go` env-default change: shared results commands now prefer `WA_RESULTS_BACKEND_URL` and retain `WA_SERVER_URL` only as a legacy fallback.
	- Covered by the regression added for the results command env default behavior.
- [ ] SAGA_BASE_URL in .env.example, but not used by anything?
- [ ] Running tests is leaving behind lots of processes that look like `node /nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/cmd/testdata/run-dev-frontend-stub.mjs 37125` and some that look like `/nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/.tmp/agent/results-integration-6Vzoo2/wa results serve --port 34799 --db /nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/.tmp/agent/results-integration-6Vzoo2/results.sqlite`. Prevent this from happening, eg. do auto clean-up, even in failure/crash states.

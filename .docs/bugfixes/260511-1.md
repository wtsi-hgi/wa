- [x] In the file browser, it looks like there is a stray bottom left corner border visible of an otherwise invisible box underneath the bottom left corner of expanded folders. Stop displaying this. This was supposidly fixed in recent commits per the previous bugfixes md file, but all that “fix” acheived was to make the rounded corner non-rounded. It is still visible. Learn from the mistakes of the past agent and make the corner actually non-visible, proven by a screenshot before and after.
	- Files: frontend/components/file-browser.tsx, frontend/tests/file-browser.test.ts, frontend/e2e/file-browser-layout.spec.ts
	- Approach: the grouped-shell elbow under expanded folders still painted real `border-l` and `border-b` geometry, so the earlier fix only changed its shape. Collapsed that decorative elbow to a zero-size placeholder, added a unit regression for the leftover border classes, tightened the Playwright grouped-shell assertion, and captured before/after screenshots on the seeded rnaseq `qc/images` fixture path to prove the fragment is gone.

- [ ] `make dev-fixtures` uses real mlwh study, library and study examples, but the library and study result metadata fails to resolve in the frontend, resulting in the browser becoming non-responsive when a library value is clicked on in the result metadata section of a results page (wtsi/amplicon example) or shows “No enrichment matched this sanger sample id value.” (this also happens for runid in real testing). The seqmeta details for study 6568 only has 2 bits of direct metadata (name and accession) and no related metadata (library and samples in the study). Get the mlwh lookups to work properly and quickly (timed after doing a sync). A similar bug was supposedly fixed in recent recent commits per the previous bugfixes md file; first understand and explain in the notes for this checklist item how it’s possible that the previous agent thought it fixed this, learn from its mistakes, and add real tests that first prove this hasn’t been fixed, then fix it with test proof that it has. Tell your implementor subagents to not paper over/ work around/ hallucinate due to any difficulties or impossibilities found. Don’t just implement “something” with a test of “something” and pass it off as success. Report problems and admit if you can’t do it.

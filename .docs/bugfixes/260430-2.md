- [x] Visiting a result set page from `make run` makes the browser non-responsive. The page can't be interacted with and eventually Chrome pops up a warning offering to exit the page.
	- Re-read the adjacent bugfix notes (`260429-2`, `260429-4`, `260430-1`) and verified HEAD rather than assuming the prior attempt was correct.
	- Root cause remains the client seqmeta cache persistence path: the known bad commit called `onChange` synchronously from every `SeqmetaCache.set()`, forcing repeated full-cache snapshots, cookie serialization, and cookie writes while result-detail seqmeta enrichment was priming multiple identifiers. HEAD fixes this by coalescing change notifications into one microtask flush and short-circuiting equal values.
	- Regression coverage: `frontend/tests/seqmeta-cache-coalesce.test.ts` (`invokes onChange at most once per microtask flush across many synchronous set calls`, `makes set values visible synchronously via get/has/snapshot before flush`, `does not invoke onChange when a set is a no-op`, `coalesces multiple flush windows independently`). The first test would fail on the known bad implementation because 10 synchronous cache writes call the listener 10 times before the assertion that calls are `<= 1`.
	- Focused verification: `pnpm exec vitest run tests/seqmeta-cache-coalesce.test.ts` passed, 4 tests in 2.58 s.
	- Lint note: `pnpm lint` remains blocked by pre-existing Prettier warnings in `app/(results)/results/[id]/loading.tsx`, `e2e/file-browser-layout.spec.ts`, `e2e/result-id-copy-chip.spec.ts`, and `tests/result-detail-hydration.test.ts`.
	- Full verification: `make test` passed after stopping the manual `make run` stack so Playwright could own its harness: Go packages passed, Vitest passed 32 files / 218 tests, Playwright passed 16 with 2 intentionally skipped.
	- Browser verification against real `make run` on ports `3671/3672/3673`: result detail URL loaded with DOMContentLoaded in 244 ms, heading visible in 1.65 s, metadata visible in 1.65 s, enrichment settled measurement in 1.66 s, back-to-dashboard click completed in 269 ms, preview-density radio interaction completed in 168 ms, requestAnimationFrame responsiveness was 11.9 ms, no long tasks were observed in the enriched probe, and no console/page errors were reported.
	- Independent reviewer verification: focused regression passed in 2.75 s; `make test` passed; real browser `make run` showed detail DOMContentLoaded in 192 ms, heading visible in 471 ms, directory expand in 99 ms, preview-mode toggle in 98 ms, return to dashboard in 270 ms, requestAnimationFrame responsiveness of 23 ms, zero long tasks, and no console/page errors.
	- Residual follow-up noted by reviewer: after enrichment, Chromium reported `document.cookie.length === 0`, suggesting the enriched cache cookie may be too large and rejected instead of persisted. This did not reproduce the page freeze and is separate from responsiveness.

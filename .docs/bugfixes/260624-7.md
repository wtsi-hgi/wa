- [x] Check the other tiff shows the z selector as well. It should be re-using the same component, not duplicating code for that display.
    - Confirmed `OmeTiffPreview` and reusable thumbnail lightboxes both mount the shared `OmeTiffExpandedPreview`; no duplicated controls UI or implementation change was needed.
    - Extended the generated OME-TIFF file-browser e2e fixture with a nested small multi-channel, multi-Z stack and asserted enlarged subfolder previews show both Channel and Z selector controls.
    - Real proof screenshots: `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-subfolder-z-controls.png` shows `.tmp/ome.tiff` Channel plus Z controls; `/home/ubuntu/wa/.tmp/agent/real-ome-file-browser-subfolder-collapsed-no-z.png` shows `.tmp/ome_collapsed.tiff` without a Z selector.
    - Verification passed: focused Vitest, focused OME Playwright, temporary real-fixture Playwright proof, `make lint`, and `make test`; no `wa mlwh sync` was run.

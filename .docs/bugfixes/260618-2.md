- [x] Broad `make lint` fails because root Prettier reports `.docs/mlwh-overhaul/phase2.md`, `.docs/mlwh-overhaul/prompt.md`, and `.docs/mlwh-overhaul/spec.md`.
    - Formatted only the three reported `.docs/mlwh-overhaul` Markdown files
      with the existing frontend Prettier binary.
    - Verification: reviewer proved the previous HEAD versions failed
      Prettier, confirmed current files match Prettier output, and reran
      `timeout 10m make lint` successfully.
- [x] Broad `make test-go` and `make test` fail in `cmd/results_serve_test.go` around `TestResultsServeCommandA2`.
    - Fixed the test setup to explicitly chmod and assert the loose token file
      and ACME cache directory permissions that `TestResultsServeCommandA2`
      is meant to exercise, avoiding restrictive-umask false paths.
    - Verification: focused `TestResultsServeCommandA2`, `make test-go`,
      `make lint`, and `make test-frontend` passed. `make test` now reaches
      e2e and fails only on the separate MLWH fixture identifiers in item 3.
- [ ] Broad `make test-e2e` fails MLWH validation setup registrations for `Custom`, `48522`, and `SMP7607-0000`.

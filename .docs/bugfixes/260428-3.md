- [x] The “File Browser”, despite repeated requests over the last few commits (see previous bugfix md files) is still comprised of multiple sections with duplicated information everywhere. It must comprise only of what is currently the “Explorer” section, plus one and only one toggle for single vs multiple previews.. No seperate preview focus section! No duplication of directory or file info anywhere! Expanding a directory with files in it currently lists one file per row. The absolute path doesn’t need to be displayed. It is these things that should be divided in to 2 columns. In single preview mode the preview of the selected file is shown in a row-spanning column to the right. In preview-per-row mode, each file gets its preview displayed in the column to the right of the eixsting info such as size, date, type.
    - Reduced the browser back to a single explorer-led pane, removed duplicate preview chrome and absolute-path echoing, and kept the single-vs-row preview toggle as the only mode control.
    - Updated component, detail-page, and Playwright regression coverage for the deduplicated layout and confirmed `make test` passes; repo-wide lint still has an unrelated pre-existing Go issue in `seqmeta/enrich.go`.
- [x] 1 preview per row mode doesn’t actually show any preview thumbnails, though the empty boxes are clickabable to view images full size.
    - Updated the reusable image lightbox wrapper so row-preview thumbnails render inside a full-width flex container instead of an inline shrink-to-fit wrapper.
    - Added a focused `file-preview` regression for the row-preview thumbnail wrapper and tightened the Playwright thumbnail check to assert rendered image dimensions, then re-ran the touched unit/browser slices.
- [x] Changing the preview height takes 10s of seconds “Rendering…”. It should be ~instant.
    - Decoupled grid thumbnail fetch sizing from the preview-height slider so changing the display height no longer invalidates every thumbnail URL and forces server-side re-rendering.
    - Added a `result-detail-files` regression test that changes preview height in grid mode and asserts the thumbnail source stays stable while the rendered height updates.
- [x] “Seqmeta details” has a “Resolution” that takes minutes Rendering and “Looking up x”, sometimes timing out. Why does it take so long? We should be able to get ~instant lookups of related metadata. Once it loads, it displays some non-sensicle things, such as the “resolved seqmeta type” of a seqmeta_library being sanger_sample_id. This is not even something we need to figure out. We already know it’s a library! It also lists a single seqmeta_sampleid, then multiple linked_samples. The linked samples makes sense, because a library could be associated with many samples. But displaying just one sample or study associated with it doesn’t make much sense. On what basis and why would it pick one of the many?
    - Prioritized concrete seqmeta identifiers ahead of `seqmeta_library` during enrichment so library rows reuse fast related metadata instead of triggering broad slow lookups first.
    - Simplified library details to stay library-focused, removing misleading singular sample/study resolution fields while preserving linked sample context and confirming `make test` passes.

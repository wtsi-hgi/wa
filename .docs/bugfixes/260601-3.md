- [x] The file browser doesn’t show at all when logged out, when it should show results at least the parent directory but not let it be expandable. Or if that isn’t feasable, at least show in some way that the search found something but we don’t have access.
    - Added a logged-out combined-files locked state that renders for matching locked result sets, shows the shared parent/output directory markers, and keeps the markers non-expandable with no file previews or private file browser.
    - Updated the Playwright repro into a behavioral regression and added a server-render guard for locked search matches.
    - Verification passed for the focused dashboard regression, focused and full combined-browser Playwright spec, frontend lint, and frontend Vitest. Root `make lint` still fails on existing `.docs/bugfixes/260522-1.md` Prettier formatting; root `make test` still fails in existing Go cert/TLS command tests. No `wa mlwh sync` was run.
- [x] Do not show the “Matching result sets” box for the “result rows” view underneath the file browser.
    - Converted the Playwright repro into a behavioral regression that selects `Result rows`, asserts the combined browser is in rows mode, verifies the `Matching result sets` heading is gone, and keeps the normal result rows visible.
    - Hid only the results-table summary header when the combined browser is in `Result rows` mode, leaving the combined file-browser output-directory details and table rows untouched.
    - Verification passed for the focused regression, the full combined-browser Playwright spec, frontend lint, and frontend Vitest. Root `make lint` still fails on existing `.docs/bugfixes/260522-1.md` Prettier formatting; root `make test` still fails in existing Go TLS/cert command tests. No `wa mlwh sync` was run.
- [x] Solve the many next.js issues that pop up after looking through wtsi/galleries-demo pipeline search result file browser (“Encountered two children with the same key, `/nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/.docs/results-web/fixtures/files/galleries-demo/sample-a/blue-plot.svg`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.”).
    - Deduplicated overlapping combined search files by absolute path before rendering the shared file browser, preferring the registration whose output directory is most specific for the file while preserving the visible file path and result rows.
    - Added a dashboard regression for overlapping combined files and kept the Playwright console-warning regression for `wtsi/galleries-demo`; updated evidence shows zero duplicate-key messages in `.tmp/agent/search-combined-file-browser-galleries-duplicate-key.json` and `.png`.
    - Verification passed for the focused dashboard regression, the full combined-browser Playwright spec on a real `next dev` server, the focused duplicate-key Playwright regression after formatting, frontend lint, and frontend Vitest. Root `make lint` still fails on existing `.docs/bugfixes/260522-1.md` Prettier formatting; root `make test` still fails in existing Go TLS/cert command tests. No `wa mlwh sync` was run.
- [x] Make the `make dev-fixtures` change better so that we don’t have galleries-demo/sample-a, galleries-demo/sample-b but also galleries-demo (the parent) all registered as output directories of the same pipeline. Keep the original galleries-demo and have a new pipeline in a different parent directory to demonstrate this feature where the parent is not registered as an output directory. I also see sample-c in the search result file browser, and I’m not sure if that’s a bug because that’s doesn’t seem to be a registered result set?
    - Kept `wtsi/galleries-demo` as the original parent output-directory fixture, including its broad `sample-c` files; `sample-c` is not registered as its own output directory and appears only because that parent fixture contains `sample-c/*.svg`.
    - Moved the sibling-output-dir demo to `wtsi/combined-galleries-demo` under `.docs/results-web/fixtures/files/combined-galleries-demo/sample-a` and `sample-b`, with no registered `combined-galleries-demo` parent output directory, while preserving the stable `galleries_sample_a` and `galleries_sample_b` unique labels.
    - Added fixture assertions preventing parent/child output-directory overlap within any pipeline, updated the seeded Playwright coverage to search and narrow the new pipeline, and kept duplicate-key coverage meaningful with a synthetic overlapping dashboard regression.
    - Verification passed for focused Go seed tests, frontend Vitest, frontend lint, focused and full combined-browser Playwright specs; root `make lint` still fails on existing `.docs/bugfixes/260522-1.md` Prettier formatting, and root `make test` still fails in existing Go results cert/TLS command tests before frontend/e2e. No `wa mlwh sync` was run.
- [ ] The sample-a example has an “overview” subfolder with an svg in it, but for some reason there’s no option to turn on “subfolder previews” mode on the search results for sample-alpha, and this sub-sub folder isn’t seen in subfolder previews mode of a wtsi/galleries-demo pipeline search. This is also missing when viewing the results page for the 88205/galleries_sample_a result set.

- [x] On another machine with the following `.env.development.local` file, `make dev` is silently failing and then getting stuck so I have to kill it. Any idea why this could be?

    ```
    $ make dev
    Building Go binary at /nfs/hgi/wa/repo/.tmp/wa
    Starting results server on https://127.0.0.1:3672 (mode=dev)
    Timed out waiting for results server at https://127.0.0.1:3672/rest/v1/results/stats
    results server process status:
    2344331       00:32 SNl+  0.1  0.0 /nfs/hgi/wa/repo/.tmp/wa results serve --port 3672 --cert /nfs/hgi/wa/repo/.tmp/wa-dev-cert.pem --key /nfs/hgi/wa/repo/.tmp/wa-dev-k
    Log file /nfs/hgi/wa/repo/logs/results.log is empty.
    ^Cmake: *** [Makefile:88: dev] Interrupt

    $ cat .env.development.local
    WA_DEV_FRONTEND_PORT=3671
    WA_DEV_RESULTS_PORT=3672
    WA_DEV_SEQMETA_PORT=3673

    WA_RESULTS_DB_PATH='redacted'
    WA_RESULTS_DB_PASSWORD=redacted

    WA_STUDIES_CACHE_TTL_SECONDS=300

    WA_MLWH_DSN='redacted'
    WA_MLWH_PASSWORD=redacted

    WA_MLWH_CACHE_PATH='redacted'
    WA_MLWH_CACHE_PASSWORD='redacted'

    WA_RESULTS_LDAP_SERVER='redacted'
    WA_RESULTS_LDAP_DN='redacted'

    WA_DEV_ALLOWED_ORIGINS=
    ```

    - Changed `run-dev.sh` cleanup to terminate children with SIGTERM, escalate to SIGKILL if a child stays alive, and reap children so a timed-out results server cannot leave `make dev` attached to a sleeping process.
    - Added a regression in `cmd/run_dev_test.go` that stubs a results server which ignores SIGTERM after a readiness timeout, proving `run-dev.sh` now exits promptly and kills it.
    - Pinned the existing missing-LDAP run-dev test to empty LDAP vars so a local `.env.development.local` cannot mask the expected startup error.
    - Follow-up after the real root cause was identified: added preflight detection for occupied results, seqmeta, and frontend ports before building the Go binary. A non-WA process on `WA_DEV_RESULTS_PORT` now produces a quick “port is already in use” error naming `--results-port` and `WA_DEV_RESULTS_PORT`, while an already-healthy dev service can still be reused.

- [x] The login box top right should have space of its own, ie. it should push other elements on the page down, not be displayed over them. The “Back to dashboard” button on results pages can display on the same row as the login button so as to not lose too much, if any, vertical usage.
    - Changed the results layout auth control from a fixed overlay to a normal-flow header bar in `frontend/app/(results)/layout.tsx`.
    - Changed the anonymous login form in `frontend/components/auth-menu.tsx` from an absolute dropdown to an in-flow panel, so opening it increases the header height and pushes page content down.
    - Added `frontend/tests/auth-menu.test.ts` coverage that the auth bar is rendered before page content and is no longer the fixed overlay.
- [x] Once logged in on the frontend, the logout button has no effect. It should log the user out and deny access to all results again.
    - Changed `frontend/components/auth-menu.tsx` so the logout button calls the browser-visible `/api/auth/logout` route, letting the browser receive the cookie-expiry response directly before refreshing server-rendered result data.
    - Updated `frontend/tests/auth-menu.test.ts` to verify logout calls `/api/auth/logout`, switches the menu back to anonymous state, and refreshes the route so protected result views are fetched again without the JWT.
- [x] There doesn’t seem to be an env var to default `--cert` and `--key` options for server and register subcmds. If the `make dev` `.tmp/wa-dev*` defaults were actually put in `.env.devlopment` as env vars and not hardcoded somewhere else, then it would be more visible and easy to change, and using `wa results register --env dev` would automatically use the correct auto-generated cert without having to say `--cert .tmp/wa-dev-cert.pem`.
    - Added `WA_RESULTS_SERVER_CERT=.tmp/wa-dev-cert.pem` and `WA_RESULTS_SERVER_KEY=.tmp/wa-dev-key.pem` to the committed development and test env defaults.
    - Changed `run-dev.sh` to honor `WA_RESULTS_SERVER_CERT` / `WA_RESULTS_SERVER_KEY`, export them after generating TLS material, and start `wa results serve` without hardcoded `--cert` / `--key` flags.
    - Updated README and DEVELOPING to document the TLS env defaults used by `wa results --cert`, `wa results serve --key`, and `run-dev.sh`.
- [x] `wa results register --nextflow-workflow https://github.com/nf-core/sarek` fails with “Error: detect pipeline: read workflow file: open /nfs/users/nfs_s/sb10/src/go/github.com/wtsi-hgi/wa/https:/github.com/nf-core/sarek: no such file or directory”. It should work with nextflow repos online, not only those referenced by http\* urls, but also it should work with “seqeralabs/nf-hello-world” just like actual nextflow does, finding it on github if that didn’t correspond to something on the local disk.
    - Changed pipeline detection so GitHub URLs and owner/repo shorthands resolve through the GitHub API instead of being treated as local filesystem paths.
    - Kept owner/repo-shaped values local when they already exist on disk, matching the “find it on GitHub if local disk does not match” behavior.
    - Changed registration file tracking so remote workflows are not `stat`ed as local pipeline files, while local workflow files continue to be tracked as before.
    - Updated `wa results register` help and README examples to document local workflow files, GitHub URLs, and owner/repo shorthands.

- [x] There’s too much vertical white space between the “file browser” text and the box for the root folder. Minimise it so it looks good; probably it should match the margin/padding of the root folder to the sides/bottom of the file browser box.
    - Removed the extra file-browser header bottom padding and kept the root shell margin aligned with the surrounding inset.
    - Added a Playwright geometry regression that measures the visible title-to-root-folder gap against the side/bottom insets.
- [x] Address the 2 SQL-related review comments here: https://github.com/wtsi-hgi/wa/pull/8#pullrequestreview-4322384342 Make sure any changes don’t regress speed issues or cause “Rendering…” to appear for more than a second when clicking on seqmeta values and filters etc.
    - Added a results metadata `(meta_key, value)` index and kept distinct metadata suggestions sorted without forcing SQL to sort the indexed lookup.
    - Narrowed direct sample-metadata candidate discovery to canonical sample-name keys while preserving broader final OR-match filters.
    - Added Go regressions for the SQLite query plan/index and for avoiding non-name candidate values in `ResolveSampleName`.

- [x] If the eligible folder is the first folder in the file browser, as it is for the `make dev-fixtures` fixtures, then the new UI elements do not display at all on loading the results page. You have to close and re-open the folder.
	- Reused a shared eligible-parent directory helper in `frontend/components/file-browser.tsx` and `frontend/components/result-detail-files.tsx` so initial selection prefers the preview-eligible parent folder.
	- Added regressions in `frontend/tests/file-browser.test.ts` and `frontend/tests/result-detail-files.test.ts` for the uncontrolled and controlled initial-load paths.
	- Reviewer confirmed `make lint` and `make test` pass for this slice.
- [ ] Having done that the folder row itself incorrectly shows the `1 preview per row` selector which makes no sense in this context and does nothing when ticked. The Subfolder previews selector is displayed underneath; it should be displayed where the `1 preview per row` selector currently is. The `Preview file types` section takes up too much room, and should be integrated as a dropdown in the Subfolder previews selector. Preview height and page widget are duplicated.
- [ ] The various widgets that can display on a folder should move beneath the folder name and metadata, but still be part of the same row (on same background colour as the folder name), so that long folder names do not get truncated so easily when widgets are shown.
- [ ] Selecting to preview `Tables` works while `Images` are also selected, but unticking `Images` then makes the previews and the new UI elements disappear. Closing and reopening the folder no longer brings them back and you have to refresh the page, then close & reopen.
- [ ] In my non-fixtures test the new UI elements never appear. This is where the folder is a nested sub-folder, but I don’t know if its the nesting or something else that is stopping the triggering of the new UI.
- [ ] When subfolder previews are displayed, each preview image is in a ~screen wide box, resulting in wide horizontal scroll bars and tons of wasted space. The images should be in boxes only as wide as the images (which are as tall as the selected preview height), and should have their file names above. The row of previews should also be underneath the subfolder name on the row so as to not waste horizontal space on the first column currently used for the subfolder name.

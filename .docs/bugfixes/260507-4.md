- [x] If the eligible folder is the first folder in the file browser, as it is for the `make dev-fixtures` fixtures, then the new UI elements do not display at all on loading the results page. You have to close and re-open the folder.
    - Reused a shared eligible-parent directory helper in `frontend/components/file-browser.tsx` and `frontend/components/result-detail-files.tsx` so initial selection prefers the preview-eligible parent folder.
    - Added regressions in `frontend/tests/file-browser.test.ts` and `frontend/tests/result-detail-files.test.ts` for the uncontrolled and controlled initial-load paths.
    - Reviewer confirmed `make lint` and `make test` pass for this slice.
- [x] Having done that the folder row itself incorrectly shows the `1 preview per row` selector which makes no sense in this context and does nothing when ticked. The Subfolder previews selector is displayed underneath; it should be displayed where the `1 preview per row` selector currently is. The `Preview file types` section takes up too much room, and should be integrated as a dropdown in the Subfolder previews selector. Preview height and page widget are duplicated.
    - Moved the subfolder preview controls into the folder-row control slot in `frontend/components/file-browser.tsx`, replacing the generic `1 preview per row` controls whenever subfolder previews are available on the selected folder.
    - Collapsed the always-open `Preview file types` fieldset into a compact disclosure and suppressed the duplicate bottom file-preview controls for the same folder-row state.
    - Added regressions in `frontend/tests/file-browser.test.ts` and `frontend/e2e/file-browser-layout.spec.ts`, and the reviewer confirmed `make lint` and `make test` pass for this slice.
- [x] The various widgets that can display on a folder should move beneath the folder name and metadata, but still be part of the same row (on same background colour as the folder name), so that long folder names do not get truncated so easily when widgets are shown.
    - Removed the folder-row two-column layout in `frontend/components/file-browser.tsx` so expanded folder widgets stack beneath the directory heading without reserving a right-hand control column.
    - Left-aligned both generic preview controls and subfolder preview controls within the same directory row surface, keeping interactive controls outside the directory button.
    - Added a focused DOM regression in `frontend/tests/file-browser.test.ts` and a browser-layout regression in `frontend/e2e/file-browser-layout.spec.ts`, and the reviewer confirmed `make lint` and `make test` pass for this slice.
- [x] Selecting to preview `Tables` works while `Images` are also selected, but unticking `Images` then makes the previews and the new UI elements disappear. Closing and reopening the folder no longer brings them back and you have to refresh the page, then close & reopen.
    - Changed `frontend/components/file-browser.tsx` so subfolder preview control availability depends on overall eligible subdirectories, while the rendered gallery rows still respect the active preview-kind filter.
    - Added a regression in `frontend/tests/file-browser.test.ts` for the `Tables`-without-`Images` sequence and adjusted the ineligible-folder coverage to match the corrected gating rule.
    - Reviewer confirmed `make lint` and `make test` pass for this slice.
- [x] In my non-fixtures test the new UI elements never appear. This is where the folder is a nested sub-folder, but I don’t know if its the nesting or something else that is stopping the triggering of the new UI.
    - Updated `frontend/components/file-browser.tsx` so the initial subfolder-preview selection searches the directory tree recursively instead of only checking the first top-level node.
    - Added a regression in `frontend/tests/file-browser.test.ts` covering an eligible nested folder under a later top-level branch.
    - Reviewer confirmed `make lint` and `make test` pass for this slice.
- [x] When subfolder previews are displayed, each preview image is in a ~screen wide box, resulting in wide horizontal scroll bars and tons of wasted space. The images should be in boxes only as wide as the images (which are as tall as the selected preview height), and should have their file names above. The row of previews should also be underneath the subfolder name on the row so as to not waste horizontal space on the first column currently used for the subfolder name.
    - Reworked the subfolder gallery row in `frontend/components/file-browser.tsx` so the preview strip sits beneath the subfolder heading, each preview renders inside a compact content-sized card, and each card shows the file name above the preview.
    - Added layout regressions in `frontend/tests/file-browser.test.ts` and `frontend/e2e/file-browser-layout.spec.ts` covering the stacked row structure, filename labels, and compact card sizing.
    - Reviewer confirmed `make lint` and `make test` pass for this slice.

- [x] The seqmeta details for a runid show study and sample related data, but these are shown as 5 seperate boxes with different kinds of metadata for the same 2 entities. Instead there should be a single row for the related study, and a single row for the related sample. Each entitiy row can show multiple bits of metadata, eg. the sample one can show the name, id and sample_lims.
    - Run-id seqmeta details now build one grouped Study row and one grouped Sample row instead of separate flat metadata boxes for the same entities.
    - Added frontend regression coverage proving the grouped rows include multi-field key/value metadata such as study name/id/accession and sample name/id/sample_lims.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`.
    - Verification: implementor and reviewer confirmed the new regression fails before the fix; focused seqmeta badge tests, frontend tests, lint-go/lint-frontend, and `git diff --check` passed. Root `make test` and MLWH-sync paths were not run per caller constraint.
- [x] The seqmeta details for related data display entities inconsistnely. For the details of all kinds of seqmeta\_\* they should display the same, following the fix of the previous bug, showing 1 row with multiple bits of metadata in key value pairs per entity. Eg. Study rows should show `name: <name>`, `id: <id>`, `accession: <accession>` etc.
    - Related Study, Sample, Library, and Lane rows now render one row per entity with compact key/value metadata pairs such as `name`, `id`, `accession`, `sample_lims`, `type`, `library_lims`, `id_run`, `lane`, and `tag_index`.
    - Sample-style seqmeta details that only have the basic enrichment graph now still use grouped related Library and Study rows instead of falling back to separate flat related fields.
    - Added frontend regressions covering grouped metadata-pair rows for study-related libraries, sample-related library/lanes, and library-related samples; updated existing related-data section expectations to the grouped entity shape.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`, `frontend/tests/seqmeta-enrichment.test.ts`.
    - Verification: new focused regressions failed before the fix; focused seqmeta badge/enrichment tests, full frontend tests, full lint, and `git diff --check` passed. Root `make test` was not run because its Go tests include MLWH sync command paths, which the caller explicitly asked subagents to avoid.
- [x] A result registered with `--library 71046409 --sample 7607STDY14643771` has seqmeta details for the library showing no direct metadata, while the details for the sample show the related library data as “SQPP-47463-G:B1” with a filter button that takes multipe seconds to “Render” and then filters on “Custom”, which doesn’t find anything. The related data should show as and filter on library id 71046409. If “SQPP-47463-G:B1” is another identifer of 71046409, it should show up as direct metadata of the library. Fix all these kinds of inconsistencies so entities always show with all their direct metadata no matter the view.
    - Exact library ID enrichment now includes the direct `graph.library` object with `library_type`, `library_id`, and `id_library_lims`, so direct library metadata is not inferred only from related sample/study arrays.
    - Seqmeta details now gather library metadata consistently from `graph.library`, `graph.libraries`, `sample_detail.libraries`, and study library details, preferring `library_id` for display/copy/filter identity, then `id_library_lims`, then library type.
    - Sample related library rows now show `id: 71046409`, `library_lims: SQPP-47463-G:B1`, and `type: Custom`, and their filter link targets `seqmeta_libraryid=71046409` instead of `library=Custom`.
    - Added Go and frontend regressions proving exact library details expose direct ID/LIMS metadata and sample-related library rows use the exact library ID even when the ID is only present on `sample_detail.libraries`.
    - Files touched: `seqmeta/enrich.go`, `seqmeta/enrich_test.go`, `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`.
    - Verification: new focused regressions failed before the fix; focused seqmeta and frontend tests, the serial full frontend suite, `make lint`, standalone `golangci-lint run --fix`, and `git diff --check` pass. Root `make test` was not run because its Go target exercises MLWH sync paths, which the caller explicitly disallowed.
- [x] With the results sets from the `make run-fixtures` fixtures, it should be possible to search for study by ID or accession, but using the search builder and choosing “Study” field and entering a value of EGAS00001005445 (accession of study id 6568) gives no matching results.
    - Results Study search now canonicalizes MLWH-backed Study values such as accession `EGAS00001005445` to study LIMS ID `6568` before building the direct Study alias OR group and before hierarchical sample/lane expansion.
    - This preserves direct alias matching for `seqmeta_studyid=6568` and the existing hierarchical expansion that finds related sample- and lane-tagged result sets.
    - Added a Go regression with the fixture shape: an rnaseq result tagged `seqmeta_studyid=6568`, a sarek result tagged with a sample from that study, and an accession query `study=EGAS00001005445`; both fixture-like rows are returned and the unrelated row is excluded.
    - Files touched: `results/server.go`, `results/mlwh_search_resolver.go`, `results/server_test.go`, `cmd/results.go`.
    - Verification: the new regression failed before the fix because expansion received `EGAS00001005445` as if it were the study ID; focused `results` and touched `cmd` tests plus `golangci-lint run --fix` pass. Root `make test` was not run because its Go target includes MLWH sync command paths, which the caller explicitly disallowed.
- [ ] `wa results register` currently takes a `--runid` as part of the unique key to register with, but this is actually an abitrary string, not to be confused with `--run`. Rename it `--unique`, consider if we can then just get rid of `--additional-unique`, and update the display of this unique part of the key throughout the system, including in the result detail and having a by-default-displayed column for it in the “Recent registrations”, next to “pipeline name” column (2nd col). If additional-unique is removed, have backwards compatibiity for it so that an existing results datbase with a value for this doesn’t break.

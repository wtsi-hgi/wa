- [x] The seqmeta details for a runid show study and sample related data, but these are shown as 5 seperate boxes with different kinds of metadata for the same 2 entities. Instead there should be a single row for the related study, and a single row for the related sample. Each entitiy row can show multiple bits of metadata, eg. the sample one can show the name, id and sample_lims.
    - Run-id seqmeta details now build one grouped Study row and one grouped Sample row instead of separate flat metadata boxes for the same entities.
    - Added frontend regression coverage proving the grouped rows include multi-field key/value metadata such as study name/id/accession and sample name/id/sample_lims.
    - Files touched: `frontend/components/seqmeta-badge.tsx`, `frontend/tests/seqmeta-badge.test.ts`.
    - Verification: implementor and reviewer confirmed the new regression fails before the fix; focused seqmeta badge tests, frontend tests, lint-go/lint-frontend, and `git diff --check` passed. Root `make test` and MLWH-sync paths were not run per caller constraint.
- [ ] The seqmeta details for related data display entities inconsistnely. For the details of all kinds of seqmeta\_\* they should display the same, following the fix of the previous bug, showing 1 row with multiple bits of metadata in key value pairs per entity. Eg. Study rows should show `name: <name>`, `id: <id>`, `accession: <accession>` etc.
- [ ] A result registered with `--library 71046409 --sample 7607STDY14643771` has seqmeta details for the library showing no direct metadata, while the details for the sample show the related library data as “SQPP-47463-G:B1” with a filter button that takes multipe seconds to “Render” and then filters on “Custom”, which doesn’t find anything. The related data should show as and filter on library id 71046409. If “SQPP-47463-G:B1” is another identifer of 71046409, it should show up as direct metadata of the library. Fix all these kinds of inconsistencies so entities always show with all their direct metadata no matter the view.
- [ ] With the results sets from the `make run-fixtures` fixtures, it should be possible to search for study by ID or accession, but using the search builder and choosing “Study” field and entering a value of EGAS00001005445 (accession of study id 6568) gives no matching results.
- [ ] `wa results register` currently takes a `--runid` as part of the unique key to register with, but this is actually an abitrary string, not to be confused with `--run`. Rename it `--unique`, consider if we can then just get rid of `--additional-unique`, and update the display of this unique part of the key throughout the system, including in the result detail and having a by-default-displayed column for it in the “Recent registrations”, next to “pipeline name” column (2nd col). If additional-unique is removed, have backwards compatibiity for it so that an existing results datbase with a value for this doesn’t break.
